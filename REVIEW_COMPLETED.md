# Review Completed — Tasks Done

This file tracks all completed and verified implementations for the tenancy, security, and test architecture.

---

## 1. Summary of Completed Tasks

| Task ID | Description | Reference / Commit | GH Issue | Date Completed |
|---|---|---|---|---|
| **23.0.A** | Tenant Null Creation Guard (Strict Mode Boundary) | `a0e0841` (pre-workflow) | — | 2026-06-23 |
| **23.1 / 23.0.B** | Tenant Null Audit Command: `--table` validates against `ISOLATED_TABLES` | `194e9e6` | [#8](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/8) | 2026-06-23 |
| **23.2 / 23.0.C** | Test coverage: `--table` rejection of non-isolated and non-existent tables | `194e9e6` | [#9](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/9) | 2026-06-23 |
| **ENV.1** | Align environment variables in `.env` | (pre-workflow) | — | 2026-06-23 |
| **ENV.3** | Harden `.gitignore` for reports and logs | (pre-workflow) | — | 2026-06-23 |
| **T1** | Fix threat-hunting domain count mismatch (158 → 161) | `194e9e6` | [#4](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/4) | 2026-06-23 |
| **T2** | Rename 8 stale domain-count test methods to `supported_domains_count` | `194e9e6` | [#5](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/5) | 2026-06-23 |
| **T3** | Extract advisory-only constraint assertions into `AssertsAdvisoryOnlyConstraints` Trait (12 classes) | `194e9e6` | [#6](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/6) | 2026-06-23 |
| **BUG** | `SecretsValidationService`: use `getenv()` for `putenv()` compatibility | `194e9e6` | [#7](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/7) | 2026-06-23 |
| **INFRA-1** | Restrict docker-compose datastore ports from `0.0.0.0` → `127.0.0.1` | `2780f13` | [#10](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/10) | 2026-06-23 |
| **INFRA-2** | Move ClickHouse/Grafana/OpenSearch secrets to `${VAR:-default}` | `2780f13` | [#11](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/11) | 2026-06-23 |
| **DB-2** | Add `tenant_id` index to `advisory_findings` + 9 `shadow_soak_*` tables | `2780f13` | [#12](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/12) | 2026-06-23 |
| **DB-1** | Add `tenant_id` nullable column + index to `advisory_finding_events` and `dlq_normalization_events` | `2780f13` | [#13](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/13) | 2026-06-23 |
| **NW-1** | Propagate `tenant_id`, `demo_run_id`, `source_event_id`, `scenario_id` in all 11 type-specific normalizer helpers | `4d1d1d7` | [#14](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/14) | 2026-06-24 |
| **CORR-1** | Normalize `identity_provider`→`identity` and `saas_audit`→`saas` in correlate() and correlateIdentityCloud() | `4d1d1d7` | [#15](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/15) | 2026-06-24 |
| **DB-5** | Propagate `tenant_id` in `security_alerts` + `security_incidents` write paths; 13 new Python tests | `4d1d1d7` | [#16](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/16) | 2026-06-24 |
| **PROD-024** | Production runtime posture checker — 3 profiles (local/staging/production), 18 checks, 58 tests | `a7c5fa5` | — | 2026-06-24 |
| **INGESTION-025** | Ingestion-gateway backpressure hardening — async metrics poller (IG-1), per-tenant rate limiter (IG-2), bounded retry + circuit breaker (IG-3); 14 Go tests; D-01/D-02/D-03 resolved to INFO | `3027e08`, `e88c103`, `7fcdd41` | — | 2026-06-24 |
| **SCALE-026** | Ingestion hardening controlled load & soak validation — 6 scenarios (S-01–S-06), 10 metrics captured, 5 bounds checks; 65 Python tests; report PASS (11/11) | `204e152` | — | 2026-06-24 |
| **DR-027** | Backup/restore/recovery readiness — BACKUP_RESTORE_RECOVERY.md runbook + xdr_recovery_validate.py (8 checks + 4 advisory RPO/RTO); 50 Python tests; local PASS 8/0/0 | `cae4eea` | — | 2026-06-24 |
| **LIVE-028** | Full live regression & evidence freeze — xdr_live_regression_validate.py (5 stages: posture/recovery/lineage/remapping/registry); LIVE_028_EVIDENCE_FREEZE.md; 71 Python tests; all PASS | `880a8d5` | — | 2026-06-24 |
| **EASM-030** | Website Exposure & Passive Posture Monitoring — 3 migrations, EasmPassiveScanService, EasmScanCommand, EasmController, xdr_easm_passive_scan.py; 58 PHP + 62 Python tests; advisory-only, ownership-guarded, no active scanning | `9304185` | — | 2026-06-24 |
| **EASM-031** | Website Posture History & Risk Trend — 3 new tables (easm_posture_snapshots/finding_changes append-only; easm_asset_risk_scores mutable), EasmPostureHistoryService (score/tier/trend/diff/snapshot/upsert), updated EasmScanCommand+EasmController, 3 routes, 2 blade views, xdr_easm_posture_history.py; 60 PHP + 45 Python tests; advisory-only | `59b6d75` | — | 2026-06-24 |
| **OBS-029** | Runtime Observability & SLO Readiness — xdr_observability_slo_validate.py (OBS-01–08 readiness + SLO-01–17 metric evaluation, 3 profiles, PASS/WARN/FAIL); config/grafana/runtime-observability-slo.json (17-panel dashboard); RUNTIME_OBSERVABILITY_SLO.md; advisory EASM SLO (SLO-17); 71 Python tests; 684 Python total green | `b960c38` | — | 2026-06-24 |
| **PILOT-034** | Controlled Enterprise Pilot Readiness Matrix — 3 tables (1 mutable + 2 append-only), EnterprisePilotReadinessMatrixService (ADVISORY_ONLY, SELF_APPROVE_BLOCKED, PASS_THRESHOLD=0.80, MAX_GATES=20, 4 required gates), PilotReadinessMatrixController, 3 routes, 2 blade views, xdr_pilot_readiness_matrix.py (PASS/FAIL/ERROR, self-approve block), Grafana dashboard; +3 hunt domains → 164; 53 PHP + 45 Python tests; 3561 PHP + 729 Python green | `1b8d8db` | — | 2026-06-24 |
| **PILOT-LIVE-035** | Final Live Pilot Evidence Run — xdr_pilot_live_validate.py (8 offline stages: P/R/L/M/I/E/O/PM, all PASS; Stage C advisory WARN when services not running, prior LIVE_CAUSAL_PROOF=PASS at 3329c4); PilotEvidenceFreezeCommand (10 gates, 100% score, advisory-only, self-approve-blocked); LIVE_035_EVIDENCE_FREEZE.md; 31 PHP + 58 Python tests; 3592 PHP + 787 Python green | `6ce7914` | — | 2026-06-24 |
| **ENTERPRISE-039** | RBAC Audit Coverage — 2 self-approval guards (EndpointResponseCommandService + SocResponseController), 26-test RbacAuditCoverageTest, 3 existing tests fixed; 3618 PHP green | `7e27346` | [#\*](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues) | 2026-06-25 |
| **ATTR-001** | MITRE ATT&CK TTP tagging on security_alerts — migration (3 nullable columns), AlertMitreService (16 alert types: 12 staged_active + 4 cross-domain), TagAlertMitreTtpCommand (dry-run safe), SecurityAlertController + alerts.blade.php updated; 18 tests; 3636 PHP green | `57fe592` | [#20](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/20) | 2026-06-26 |
| **ATTR-002** | Alert attribution context — alert_attribution_context migration (append-only), AlertAttributionService (offline-first, idempotent), EnrichAlertAttributionCommand, controller + route + view, xdr_attribution_validate.py (10/10 PASS); 28 tests; 3664 PHP green | `4f8b008` | [#21](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/21) | 2026-06-26 |
| **ATTR-003** | GeoIP/ASN offline lookup — GeoAsnLookupService (CIDR matching, IPv4+IPv6), geo_asn_fixtures.json (RFC1918/5737 + 4 demo ASNs); bundled with ATTR-002; 3664 PHP green | `4f8b008` | [#22](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/22) | 2026-06-26 |
| **RBAC-1** | Add missing EASM + Pilot Readiness Matrix permissions to all roles in config/soc.php; fixes locked-out routes | `8e7ba02` | [#23](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/23) | 2026-06-26 |
| **EASM-1** | Enforce TenantContextAuthority in EasmController; cross-tenant spoof blocked via TCA::validateAndResolve | `8e7ba02` | [#24](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/24) | 2026-06-26 |
| **PILOT-1** | Scope PilotReadinessMatrixController by validated tenant context; cross-tenant run_id returns 404 | `8e7ba02` | [#25](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/25) | 2026-06-26 |
| **ENTERPRISE-045** | Detection Domain Promotion Readiness — DetectionPromotionReadinessService (5 categories, derived from registry), DetectionPromotionReadinessController, promotion_readiness blade view, xdr_promotion_readiness_validate.py (13/13 PASS); 24 tests; 3707 PHP green | `8524f3c` | — | 2026-06-26 |
| **ENTERPRISE-046** | Tenant Strict Mode & Null Backfill Closure — MUTABLE_TABLES + APPEND_ONLY_ISOLATED_TABLES constants in TenantBoundaryService, TenantNullBackfillCommand (dry-run/idempotent/batched), xdr_tenant_backfill_validate.py (13/13 PASS); 18 tests; 3725 PHP green | `eaafe83` | — | 2026-06-26 |
| **ENTERPRISE-047** | Shadow Ready Promotion Decision — ShadowReadyPromotionDecisionService (0.78/0.65 thresholds, DLQ-aware, advisory), EvaluateShadowPromotionCommand (--dry-run), shadow_promotion_decisions table (append-only), xdr_shadow_promotion_decision_validate.py (16/16 PASS); 35 tests; 3760 PHP green | `7704784` | — | 2026-06-26 |
| **ENTERPRISE-048** | Endpoint Shadow Domain Soak Plan — EndpointSoakPlanService (tier_1_soak_ready conf>=0.72: 80 rules, tier_2_evidence_collection: 13 rules, 5 gates), 3 append-only tables, GenerateEndpointSoakPlanCommand (--dry-run), xdr_endpoint_soak_plan_validate.py (16/16 PASS); 32 tests; 3792 PHP green | `d4a3ef1` | — | 2026-06-26 |
| **ENTERPRISE-049** | Stability Evidence Freeze v2 — StabilityEvidenceFreezeV2Service (12 gates EF-01–EF-12, 4 phase summaries E045–E048, STABLE_SCORE_THRESHOLD=0.80), 3 append-only tables, StabilityFreezeV2Command (--dry-run), xdr_stability_freeze_v2_validate.py (14/14 PASS); 25 tests; 3817 PHP green | `e7f73d3` | — | 2026-06-26 |
| **TC-1** | TenantContextAuthority advisory scoping — SecurityAlertController, SocIncidentController (fixed extra-brace syntax error), SocDashboardController, SocApiController; legacy/demo pass-through (requireTenantContext=false) | `bf5ca6e` | — | 2026-06-28 |
| **PTS-1** | Fastapi stub: added Depends/Header/HTTPException stubs in alert-writer and incident-builder Python test suites | `bf5ca6e` | — | 2026-06-28 |
| **DB-5-DEFECT** | Correlation-worker Alert struct: add top-level TenantID field + makeAlert() propagation; 2 new Go tests | `bf5ca6e` | — | 2026-06-28 |
| **IG-DOS** | Ingestion-gateway: lastSeen atomic.Int64 on tenantBucket + TTL-based eviction goroutine (XDR_TENANT_LIMITER_IDLE_MINUTES=30) | `bf5ca6e` | — | 2026-06-28 |
| **RESP-1** | SocAgentController + SocResponseController route through EndpointResponseCommandService via LEGACY_TYPE_MAP; unmappable types rejected | `bf5ca6e` | — | 2026-06-28 |
| **AGENT-API-1** | Advisory-only signature check in pollCommands(): logs SecurityHardeningEvent on invalid sig, does not block (deferred until endpoint 6h soak PASS) | `bf5ca6e` | — | 2026-06-28 |
| **INT-AUTH-1** | docker-compose.prod.yml: ports: !reset [] on all internal pipeline services (normalizer/correlation/alert-writer/incident-builder/ai-rag) | `bf5ca6e` | — | 2026-06-28 |
| **INT-AUTH-2** | alert-writer-service + incident-builder-service: X-Internal-Service-Token validation on GET /dlq; field truncation to 120 chars | `bf5ca6e` | — | 2026-06-28 |
| **TEST-1** | ExampleTest: PHP version minimum (8.1), login page 200, dashboard redirect; Unit ExampleTest: PHP_VERSION_ID >= 80100 | `bf5ca6e` | — | 2026-06-28 |
| **TEST-2** | 13 Feature test files migrated to AssertsAdvisoryOnlyConstraints trait; 5 duplicated inline advisory methods removed per file | `bf5ca6e` | — | 2026-06-28 |
| **ENTERPRISE-065** | Tenant Backfill + Strict Mode Readiness — TenantStrictModeReadinessService (7 gates, PASS_THRESHOLD=0.80, SELF_APPROVE_BLOCKED), 3 append-only tables, StrictModeReadinessCommand, 2 views, RBAC tenant.strict-mode.view/assess | `6688302` | — | 2026-06-28 |
| **ENTERPRISE-066** | Redpanda Topic Bootstrap + Runtime Recovery Hardening — RedpandaRecoveryHardeningService (9 topics, 6 consumer groups, advisory-only), 3 append-only tables, 2 views, RBAC redpanda.health.view/check | `6688302` | — | 2026-06-28 |
| **ENTERPRISE-067** | RAG Knowledge Base Seeding Runbook — AiKnowledgeSeedService (idempotent, 10 fixtures, 4 categories), rag_seed_fixtures (mutable) + 2 append-only tables, AiSeedKnowledgeCommand (--dry-run), view, RBAC ai.knowledge.view/seed | `6688302` | — | 2026-06-28 |
| **ENTERPRISE-068** | Container Resource Governance — `deploy.resources.limits` (memory + cpus) on 6 services in docker-compose.yml (dev) + docker-compose.prod.yml (prod); `xdr_container_resource_validate.py` 14 tests PASS | 0d6cfce | — | 2026-06-29 |
| **ENTERPRISE-069** | PostgreSQL RLS Policy Scaffolding (advisory-only) — `scaffold_rls_policies` migration (DO $$, idempotent, pgsql-only, no enforcement); `TenantRlsStatusCommand` (read-only, advisory); `xdr_rls_scaffold_validate.py` 14 tests PASS; `RlsPolicyScaffoldingTest.php` 19 tests PASS | 0d6cfce | — | 2026-06-29 |
| **ENTERPRISE-070** | Tenant Null Backfill Pre-Flight Validation — `TenantBackfillPreflightCommand` (6 CHK, advisory-only); GATE-08 `gateRlsPoliciesScaffolded()` in `TenantStrictModeReadinessService`; `xdr_tenant_backfill_preflight.py` 14 tests PASS; gate count 7→8 in test | 0d6cfce | — | 2026-06-29 |
| **ENTERPRISE-071** | RAG Knowledge Base Operational Integration — `RagOperationalCheckCommand` (`ai:knowledge-check`, read-only); `xdr_rag_operational_validate.py` 14 tests PASS; `RagOperationalIntegrationTest.php` 12 tests PASS | 0d6cfce | — | 2026-06-29 |
| **ENTERPRISE-072** | Shadow Domain Soak Pre-Flight — `DomainSoakHarnessService::getPreflightStatus()` (5 CHK, advisory); `ShadowSoakPreflightCommand` (`domain:soak-preflight`); `xdr_shadow_soak_preflight.py` 14 tests PASS; `ShadowSoakPreflightTest.php` 17 tests PASS | 0d6cfce | — | 2026-06-29 |
| **ENTERPRISE-073** | Redpanda Multi-Node HA Template — `docker-compose.ha.yml` (3-broker, seed discovery, RPC addr, healthcheck, resource limits); `xdr_redpanda_ha_validate.py` 14 tests PASS; `--replication-factor` + `--replicas` flag in `xdr_topic_bootstrap.py` | 0d6cfce | — | 2026-06-29 |
| **ENV-CACHE-DRIFT** | Map `XDR_INTERNAL_AUTH_SECRET` to `config/xdr.php` (`internal_auth_secret`); `InternalAuthService::secret()` uses `config()` not `env()` (config:cache bypass prevented); `InternalAuthConfigMappingTest.php` 7 tests | 4ee9675 | — | 2026-06-29 |
| **CMD-SHARED-HMAC** | Per-agent `hmac_secret` column on `endpoint_agents`; `EndpointResponseCommandService::verifyAgentSignature($sig,$raw,$agentId)` uses per-agent secret with shared-token fallback; `PerAgentHmacSecretTest.php` 11 tests | 4ee9675 | — | 2026-06-29 |
| **AGENT-TENANCY-GAP** | `tenant_id` on `endpoint_agents`; `TenantBoundaryService` ISOLATED+MUTABLE, removed from UNISOLATED; `EndpointAgentTenantScopingTest.php` 11 tests | 4ee9675 | — | 2026-06-29 |
| **TENANT-UNSCOPED-TABLES** | `tenant_id` on `investigations`, `response_plans`, `entities` (MUTABLE) + `threat_hunts` (APPEND_ONLY_ISOLATED); `TenantUnscopedTablesTest.php` 16 tests | 4ee9675 | — | 2026-06-29 |
| **RATE-LIMIT-BYPASS** | Parse payload before rate limiting; `extractPayloadTenantID()`; reject `tenant_id_header_mismatch` (400) when `X-Tenant-ID` ≠ payload `tenant_id`; rate-limit keyed on payload tenant; 8 Go tests | 4ee9675 | — | 2026-06-29 |
| **ENTERPRISE-074** | Consolidated Security Hardening Evidence Freeze — 9 append-only tables, `SecurityHardeningEvidenceFreezeService` (10 controls, ADVISORY_ONLY, SELF_APPROVE_BLOCKED, MIN_PASS_SCORE=0.85), command + controller + 5 views, +5 hunt domains → 177, 55 PHP tests, `xdr_security_hardening_evidence_freeze.py` 12/12 PASS | a44bfd8 | — | 2026-06-29 |
| **NOTIFY-TENANCY-GAP** | Tenant-aware SOC notification routing — `tenant_notification_settings` (mutable, isolated) + `tenant_id` on `notification_delivery_logs`; `TenantNotificationResolver` (null→global, configured→tenant w/ per-channel fallback, disabled→suppress); `SocNotifier::send($tenantId)`; `soc:sla-escalate` + `soc:notify-critical` per-tenant routing; 16 tests | 5db597c | — | 2026-06-29 |
| **PERF-IOC-LOOP** | IOC enrichment nested-loop → batched writes — `SocThreatIntelController::matchIocs()` accumulates hits + per-alert evidence, chunked `insertOrIgnore` (500) + one UPDATE per matched alert (was 1 insert + 1 UPDATE per match); fixes latent multi-match evidence loss; preserves no-dedup + hit count; 5 tests (`PerfIocLoopTest`) | (this batch) | — | 2026-06-30 |
| **PERF-ALERT-TUNE** | Suppression apply N+1 → bulk — `SocTuningController::applyActiveSuppressions()` resolves matching alert ids once per rule, single bulk UPDATE + batched history insert (was 1 UPDATE + 1 INSERT per alert); retains 500-cap + unsuppressed-only semantics; 4 tests (`PerfAlertTuneTest`) | 1e4bc3c | — | 2026-06-30 |
| **PERF-AGENT-UPDATE** | Agent command retrieval N+1 → bulk — `AgentIngestionController::config()` marks all retrieved queued commands sent in one `whereIn` UPDATE (status/sent_at/attempts+1) instead of per-command; 2 tests (`PerfAgentN1Test`) | (this batch) | — | 2026-06-30 |
| **PERF-AGENT-HEALTH-N1** | Agent health check N+1 → eager-load — `AgentHealthCheckCommand` eager-loads `agent_policies` (keyed map) + failure-breaching `endpoint_agents` (keyed map), and batches stale-agent offline status into one bulk UPDATE; staleness/alert semantics preserved; 3 tests (`PerfAgentN1Test`) | 4a0f1d9 | — | 2026-06-30 |
| **TZ-AGENT-STALE** | Timestamptz round-trip skew fix (discovered during PERF-AGENT-HEALTH-N1) — PG server session was +07 while app.timezone=UTC, so naive query-builder timestamps read back ~7h off in PHP, making every agent appear stale/offline across dashboard, `SocAgentController`, `SocApiController`, `EndpointController`, `OpsHealthController` + the health-check command. Fix: pin pgsql connection `timezone`→UTC (systemic class fix) + in-DB staleness comparison in `AgentHealthCheckCommand` (defense-in-depth). 3 lock tests (`TimezoneRoundTripTest`); full suite 4526 green | 90ea4a1 | — | 2026-06-30 |
| **GIT-RM-PYC** | Untracked 147 compiled `*.pyc`/`__pycache__` files + 3 Go `*.exe` service binaries from the git index (`git rm --cached`); added `*.exe` rules to `.gitignore` (`*.py[cod]`/`__pycache__/` already present). Removes perpetual `git status` noise; working files retained | 317f1a0 | — | 2026-06-30 |
| **PERF-TRANSACTION-GAP** | SLA escalation atomicity — `SocSlaEscalationCommand` wraps incident UPDATE + activity INSERT + audit log in one `DB::transaction`; notifications (external I/O) stay after commit; 2 tests (`PerfTransactionGapTest`) | 9800281 | — | 2026-06-30 |
| **AI-CONTEXT-EMPTY** | LLM context enrichment — `AiAnalystManager::compactContext()` now includes bounded alert details (type/severity/detector/score/ip/evidence, top 8), IOC hit values (top 8), and retrieved knowledge text (title+excerpt+score, top 6) instead of only counts; existing count keys retained; 3 tests (`AiContextEnrichmentTest`) | 34d7b0f | — | 2026-06-30 |
| **PERF-PYTHON-HTTP** | HTTP session pooling — alert-writer + incident-builder now route all outbound HTTP (Pandaproxy/OpenSearch put/post/get/delete) through a module-level `SESSION = requests.Session()` (keep-alive connection reuse) instead of per-call transient connections; tests repointed to patch `SESSION`; +2 lock tests; 23 service + 1340 Python tests pass | d2db826 | — | 2026-06-30 |
| **PERF-SUBPROCESS-POLL** | ClickHouse sync daemon in-process — `clickhouse_sync_daemon.py` imports `sync_postgres_to_clickhouse` and calls `main([])` per cycle (extracted testable `run_once()`, errors logged not raised) instead of spawning a Python interpreter subprocess each loop; `sync` `main`/`parse_args` now accept explicit `argv`; 5 tests (`tests/scripts/test_clickhouse_sync_daemon.py`) | 64c6886 | — | 2026-06-30 |
| **PERF-IOC-STR-LOWER** | IOC matching micro-opt — `SocThreatIntelController::matchIocs()` pre-lowercases each IOC value once (`$iocNeedles`) before the alert loop instead of `strtolower($ioc->ioc_value)` once per (alert × IOC); +1 case-insensitivity test | (refine batch) | — | 2026-06-30 |
| **REFINE-AGENT-STATUS** | DRY refinement — extracted the duplicated online/offline staleness expression (`last_seen_at && diffInSeconds ≤ threshold`, copied across `SocAgentController`/`SocApiController`/`SocDashboardController` + 3 spots in `EndpointController`) into `EndpointAgentService::computeStatus()`; centralizes the tz-sensitive comparison; 6 tests (`EndpointAgentStatusHelperTest`) | (refine batch) | — | 2026-06-30 |
| **IOC-HITS-IDEMPOTENCY** | Data-integrity fix — added unique index `ioc_hits_ioc_alert_unique (ioc_id, alert_id)` (dedup-first migration so it's safe on existing data); `matchIocs()` `insertOrIgnore` now truly de-dups → re-running enrichment is idempotent; updated docblock + test (`test_repeated_enrich_is_idempotent` now asserts 1 row) | migration `2026_07_01_010001`, `SocThreatIntelController.php` | — | 2026-07-01 |
| **AGENT-SECRET-DECRYPT-500** | Robustness fix — `AgentIngestionController::verifiedAgent()` wraps `Crypt::decryptString()` in try/catch → `recordFailure('secret_decrypt_failed')` + return null (graceful 401) instead of an unhandled 500 on corrupt secret / APP_KEY rotation; +1 test | `AgentIngestionController.php`, `PerfAgentN1Test` | — | 2026-07-01 |
| **RESP-POLICY-FAIL-OPEN** | Security fix — `SecurityResponsePolicy::recordIsActive()` now fails **closed** (returns false) on an unparseable non-empty `expires_at` instead of active-forever; null/empty still = permanent; +1 test | `SecurityResponsePolicy.php`, `SecurityResponsePolicyTest` | — | 2026-07-01 |
| **ENV-CACHE-DRIFT-BATCH** (runtime slice) | config:cache-safety — migrated the 4 integration adapters (Jira/PagerDuty/ServiceNow/Slack) to a new `config/integrations.php`, and `TrustProxies`/`AppServiceProvider` force_https to `config/xdr.php` keys; identical env vars+defaults, but no longer null under `php artisan config:cache` (was silently dropping credentials/trusted-proxies). 4 tests (`IntegrationConfigCacheTest`). CLI/advisory-tooling env() calls remain (tracked in backlog). | `config/integrations.php`, `config/xdr.php`, `app/Services/Integrations/*`, `TrustProxies.php`, `AppServiceProvider.php` | — | 2026-07-01 |
| **RATE-LIMIT-DOS** | Enterprise DoS hardening (ingestion-gateway) — bounded the number of distinct per-tenant rate-limiter buckets (`XDR_INGEST_MAX_TENANT_BUCKETS`, default 10000). Existing tenants always keep their own bucket; beyond the cap, new tenant IDs share a single lazily-created overflow bucket (still rate-limited) so an authenticated client can't exhaust memory by flooding distinct tenant_ids. Refiller decrements the count on TTL eviction + refills the overflow bucket; `/metrics` exposes `tenant_bucket_count`/`tenant_bucket_max`. Cap is opt-in (`<=0` = legacy). 4 Go tests; `go vet` clean (race detector unavailable — no gcc — but design is atomic-CAS/atomic-counter, race-free). | `services/ingestion-gateway/main.go`, `main_test.go` | — | 2026-07-01 |
| **PERF-GO-HOT-HTTP** | Enterprise hot-path perf (correlation-worker) — added a bounded, TTL, thread-safe `iocCache` in front of `lookupIOC` so repeated indicators in a batch don't re-issue a synchronous HTTP GET each time. Split `lookupIOC` (cache) from `fetchIOC` (HTTP): only definitive HTTP-200 outcomes (match / explicit no-match) are cached; transient errors/non-200 are never cached (retried). `XDR_IOC_CACHE_TTL_SECONDS`=60, `XDR_IOC_CACHE_MAX`=10000; `/metrics` exposes `ioc_cache_hits`. 5 Go tests (get/set/expiry, negative cached, bounded, cache-repeated-via-httptest, no-cache-on-error); `go vet` clean. | `services/correlation-worker/main.go`, `ioc_cache_test.go` | — | 2026-07-01 |
| **AUTH-TIMING-CMP** | Security — constant-time internal-token compare. `verify_internal_token()` in alert-writer + incident-builder replaced `token == expected` (short-circuit, timing side-channel leaking prefix-match length) with `hmac.compare_digest(token.encode(), expected.encode())` (both sites each; `import hmac` added). Now consistent with Go `hmac.Equal` + Laravel `hash_equals`. Behavior-neutral for valid/invalid tokens. +10 Python tests (5/service: correct/wrong-same-length/wrong-length/permissive/uses-compare_digest). 33 service + 1340 topic-bootstrap tests pass. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py` + tests | — | 2026-07-04 |
| **ENV-CACHE-DRIFT-BATCH** (CLI slice — closes task) | Migrated the remaining *safe* CLI env() reads to config: `TenantBackfillPreflightCommand` strict-mode check → `config('xdr.tenancy.strict_mode')` (was the lone outlier reading env() vs the app's canonical config source; fixes config:cache + config()-test divergence); `EndpointSoakPlanService` GATE-02 → new `config('xdr.shadow_consumer_enabled')`. **Intentionally left as live env()** (documented, not a defect): `SecretsValidationService` (getenv, putenv-tested), `SecurityHardeningController::serviceAuthStatus` (live posture display, putenv-tested), and `Phase1SoakExecutionService`/`RealDomainSoakPlanService` correlation-engine gates (load-bearing `''`/`'legacy'` defaults probing raw env state). +1 test (`IntegrationConfigCacheTest::test_cli_slice_reads_config_not_env`). | `config/xdr.php`, `TenantBackfillPreflightCommand.php`, `EndpointSoakPlanService.php` | — | 2026-07-02 |
| **MEM-UNBOUNDED-STATE** | Reliability — bounded the unbounded in-memory state in both Python services (memory growth / OOM at sustained enterprise volume). `SEEN` (alert-writer) is now a bounded LRU (`OrderedDict` + `_seen_add()` evicting oldest, cap `XDR_ALERT_WRITER_SEEN_MAX`=100000) — the DB `ON CONFLICT` upserts remain the source of truth for idempotency, so `SEEN` is a safe-to-bound fast-path. `DLQ` in **both** services is now a fixed-size `deque(maxlen=…)` ring (`XDR_ALERT_WRITER_DLQ_MAX` / `XDR_INCIDENT_BUILDER_DLQ_MAX`, default 1000); the `/dlq` view's `DLQ[-20:]` slice → `list(DLQ)[-20:]` (deque isn't sliceable). Behavior-neutral for the valid path; append-only tables untouched. +6 Python tests (ring bound + overflow-drops-oldest both services; SEEN LRU evict + idempotent add). 39 service tests pass. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py` + tests | — | 2026-07-05 |
| **FASTAPI-LIFESPAN** | Modernization — replaced the deprecated `@app.on_event("startup"/"shutdown")` hooks (removed in modern FastAPI) with a `@contextlib.asynccontextmanager lifespan(...)` passed as `FastAPI(lifespan=…)` in both Python services. The prior startup/shutdown bodies (validate secrets, launch consumer threads / set `STOP`) became plain `_startup_tasks()`/`_shutdown_tasks()` invoked by the lifespan; consumer semantics unchanged. Prevents a silent break on a future `fastapi`/`uvicorn` bump (`requirements.txt` pins only `>=0.110`). +6 Python tests (no-on_event source guard, lifespan/helpers exist, shutdown sets STOP — both services). 45 service tests pass; both modules byte-compile. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py` + tests | — | 2026-07-05 |
| **PY-CONTAINER-HARDENING** | Infra hardening — all 3 Python service Dockerfiles (`alert-writer-service`, `incident-builder-service`, `ai-rag-service`) now build a non-root `app` user (`groupadd`/`useradd -r` + `COPY --chown=app:app` + `USER app`) and add a `HEALTHCHECK` hitting `/health` via stdlib `urllib` (no curl dependency, no extra apt install). `requirements.txt` in all 3 pinned to exact versions (`fastapi==0.139.0`, `uvicorn[standard]==0.50.0`, `pydantic==2.13.4`, `psycopg[binary]==3.3.3`, `requests==2.34.2` — alert-writer/incident-builder; first 3 for ai-rag) instead of open `>=` ranges, freezing current dependency resolution. Verified by installing the exact pins into a disposable venv and importing each service's real (non-stubbed) `main.py`, then driving `/health`+`/metrics` through the real FastAPI lifespan (also re-confirms FASTAPI-LIFESPAN works against real, not stubbed, FastAPI). `docker compose config --quiet` exit 0. **Documented limitation:** full hash-locking (`pip-compile --generate-hashes`) deferred — needs a Linux-matching build environment to verify safely; Docker daemon unavailable in this session (engine not running) so no actual image build could be performed. | `services/alert-writer-service/Dockerfile`, `services/incident-builder-service/Dockerfile`, `services/ai-rag-service/Dockerfile`, 3× `requirements.txt` | — | 2026-07-05 |
| **AI-KB-FEEDBACK-LOOP** | Closed-loop analyst feedback — added `AiAnalystManager::ingestApprovedFeedback()`: when an analyst reviews an AI suggestion as `accepted` (`SocAiController::review()`), the approved suggestion (summary + recommended steps/responses + analyst note) is persisted into `soc_knowledge_base` as a new `ai_suggestion_feedback` entry type (idempotent — re-review/re-call returns the existing `kb_id` via a tags-based lookup), with an embedding upserted via the existing `SocKnowledgeRetriever`. Rejections are a no-op (no KB write). New entry type registered in `SocKnowledgeBaseController`'s validator and the knowledge-base filter dropdown. Closes the loop from AI-suggests → analyst-approves → durable, retrievable knowledge (previously dead-ended in `ai_analyst_suggestions.status`). +4 PHP tests (creates KB entry on accept incl. content/tag assertions, no entry on reject, idempotent double-ingest, no-op when not accepted). 7/7 `SocAiKnowledgeMaturityTest` pass. | `app/Support/AiAnalystManager.php`, `app/Http/Controllers/SocAiController.php`, `app/Http/Controllers/SocKnowledgeBaseController.php`, `resources/views/soc/knowledge/index.blade.php` + test | — | 2026-07-05 |
| **PIPE-CONSUMER-AUTH-500** | **Critical bug fix** — the internal consumer event loop (`process_alerts()` in both services) called the FastAPI route function directly (`write()`/`build()`), leaving `x_internal_service_token` at its literal Python default — the real `fastapi.params.Header` marker object under real FastAPI (the test stub's `Header` mock masked this by returning `None`). Under the `docker-compose.prod.yml` posture (`XDR_ENFORCE_INTERNAL_AUTH=true` + a configured per-service token), this made `verify_internal_token()` call `.encode()` on a `Header` object → **`AttributeError`** on every polled batch → the event loop's generic exception handler treated it as a transient error, endlessly recreating the consumer with zero alerts/incidents ever written — pipeline silently and permanently stalled. Fix: extracted `_write_alerts_core(request)` / `_build_incidents_core(request)` (no token parameter at all) out of the routes; `write()`/`build()` now check `verify_internal_token()` then delegate to the core function; `process_alerts()` calls the core function directly, never the route. Reproduced the exact original `AttributeError` and confirmed the fix resolves it end-to-end against real (non-stubbed) FastAPI/pydantic in a disposable venv (both services). +8 unit tests (route delegates to core not itself, core has no token parameter, core succeeds under enforced+token env, route source still contains the auth check). 53/53 service tests pass. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py` + tests | — | 2026-07-05 |
| **AW-DEDUPE-BEFORE-COMMIT** | Correctness fix (alert-writer) — `_write_alerts_core()` used to call `_seen_add(fp)` *before* attempting `write_postgres()`, so a Postgres failure still poisoned the durable `SEEN` LRU; any later redelivery/retry of the exact same batch was silently dropped as a "duplicate" despite never actually being persisted (a false-success recovery path). Fix: fingerprints are now staged in a local `seen_in_batch` set (still deduping repeats within the same batch) and only committed into `SEEN` after `write_postgres()` succeeds (`postgres_ok`) or in pure dry-run (no backing store configured at all — the intended demo behavior). Reproduced against real pydantic: a failed write followed by a retry once the store recovers now succeeds instead of being dropped. +4 unit tests (SEEN not marked on failure, marked on success, retry-after-failure not dropped, intra-batch duplicate still deduped) + 1 real-dependency reproduction script. | `services/alert-writer-service/main.py` + test | — | 2026-07-05 |
| **PY-POISON-RECORD-BATCH** | Resilience fix (both services) — `AlertPayload(**row)` in `normalize_records()` was unwrapped, so a single malformed record (e.g. missing a required field) raised an exception that propagated through `event_loop()` and aborted the **entire poll batch** — including every other valid alert/incident in it — then looped the consumer recreate-from-earliest logic forever on the same poison record. Fix: wrapped the construction in a per-record `try/except`; a malformed record is isolated to the bounded `DLQ` ring as `alert_payload_invalid: …` (mirrors the existing envelope-unwrap failure pattern already used one line above it) and the loop continues to the next record. Reproduced against real pydantic with a mixed valid/invalid batch: the malformed record is isolated and the valid record in the same batch is still recovered and processed. +6 unit tests (3/service: no-raise, DLQ isolation, try/except present in source) + 1 real-dependency reproduction. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py` + tests | — | 2026-07-05 |
| **IG-HMAC-FAIL-OPEN** | Security posture fix (ingestion-gateway) — `XDR_INGEST_SECRET` empty or left at the `dev-secret-change-me` default previously only logged a WARN and continued running with HMAC verification effectively disabled, even under `docker-compose.prod.yml`'s `XDR_ENFORCE_INTERNAL_AUTH=true` posture. Added a pure, testable `shouldRefuseIngestSecret(secret, enforced) bool` + `envBool()` helper; `validateStartupSecrets()` now `log.Fatalf`s (refuses to start) when enforced and the secret is empty/dev-default — mirroring the existing fail-fast pattern already used by correlation-worker/normalizer-worker for their own internal tokens. Permissive/demo posture (`XDR_ENFORCE_INTERNAL_AUTH` unset) is unchanged (WARN only). +5 Go tests. `go build`/`go vet`/`go test` green. | `services/ingestion-gateway/main.go` + test | — | 2026-07-05 |
| **GO-BASEIMAGE-EOL** | Tech-currency fix (all 3 Go services) — `go.mod` bumped `go 1.22` → `go 1.24`; Dockerfiles bumped the EOL `golang:1.22-alpine` builder → `golang:1.26-alpine` and EOL `alpine:3.20` runtime → `alpine:3.22` (both tags verified to exist on Docker Hub before pinning, since the local Docker daemon was unavailable to build-verify). Complements TECH-EOL-UPGRADE (PHP-only). `go build`/`go vet`/`go test` all green on all 3 services against the local go1.26.3 toolchain; `docker compose config --quiet` exit 0. | `services/{ingestion-gateway,correlation-worker,normalizer-worker}/{Dockerfile,go.mod}` | — | 2026-07-05 |
| **GO-GRACEFUL-SHUTDOWN** | Reliability fix (ingestion-gateway, correlation-worker) — the SIGTERM handler called `server.Close()`, which immediately terminates active connections/in-flight requests despite logging "shutting down gracefully". Replaced with `server.Shutdown(ctx)` using a 15s drain timeout, so in-flight requests complete (or the ctx times out) instead of being dropped mid-deploy. Added `context` import to correlation-worker (ingestion-gateway already had it). Behavior-neutral for the non-shutdown path. `go build`/`go vet`/`go test` green on both services. | `services/ingestion-gateway/main.go`, `services/correlation-worker/main.go` | — | 2026-07-05 |
| **AIRAG-STUB-CITATIONS** | Groundedness labelling fix (ai-rag-service) — `/v1/retrieve` returned 2 hardcoded fake citations with realistic-looking scores and no indication they were fabricated placeholder data. Confirmed no live caller depends on the response shape (`AiRagServiceProvider.php` only calls `/v1/analyze`; `/v1/retrieve` was reachable on port 8094 but unused by the app). Added explicit `"provider": "stub"` and `"grounded": false` fields to the response so it can never be mistaken for a real, evidence-backed retrieval once something starts calling it. +1 test (new `tests/ai_rag/` suite). | `services/ai-rag-service/main.py` + new test | — | 2026-07-05 |
| **IB-DLQ-NOT-DURABLE** | Reliability parity fix (incident-builder) — failures previously lived only in the in-memory `DLQ` ring (lost on restart), asymmetric with alert-writer's durable `xdr.alert_write_failed`. Added `write_incident_failure()` (mirrors `write_alert_failure()`), wired into both the Postgres-write failure path and the generic `event_loop_error` path (also feeding the `should_recreate` decision via `not dlq_ok`, matching alert-writer). Registered `xdr.incident_write_failed` in `xdr_topic_bootstrap.py` `REQUIRED_TOPICS` and `docker-compose.yml`. Extended alert-writer's unified `normalize_pipeline_dlq_records()` classification (`incident_write_failed`+`postgres_write_failed` → replayable) and added a third `pipeline_dlq_consumer_event_loop` instance gated by `XDR_INCIDENT_WRITE_DLQ_CONSUMER_ENABLED`, so incident failures land in the same `dlq_records` SOC review surface as correlation/alert-write failures. Discovered and fixed a **pre-existing, unrelated** latent bug in 2 `xdr_topic_bootstrap` tests along the way (mock functions missing the `replication_factor` parameter present in the real `create_topic()` signature — dormant until a genuinely new required topic needed real creation; confirmed pre-existing via `git stash` against HEAD). +5 Python tests; full `xdr_topic_bootstrap` suite (1342 tests) green; alert-writer/incident-builder/ai-rag suites (69 tests) green. | `services/incident-builder-service/main.py`, `services/alert-writer-service/main.py`, `scripts/xdr_topic_bootstrap.py`, `docker-compose.yml` + tests | — | 2026-07-05 |
| **IG-HMAC-REPLAY** | Security hardening (ingestion-gateway + endpoint-agent) — ingest HMAC signed the request body only, so a captured `(signature, body)` pair was replayable forever. `verifySignature()` now accepts an optional `X-XDR-Timestamp` header; when present, the signature covers `timestamp + "." + body` (sigv2) and is rejected if the timestamp falls outside a `XDR_INGEST_SIG_TOLERANCE_SECONDS` window (default 300s) — bounding the replay window instead of leaving it open indefinitely. Legacy body-only (sigv1) signatures are still accepted by default for backward compatibility with any sender not yet upgraded; `XDR_INGEST_SIGV2_REQUIRED=true` forces the gateway to reject sigv1 once all senders have migrated. `endpoint-agent`'s `sign_body()` gained an optional `timestamp` parameter (legacy behavior preserved when omitted) and `GatewayClient._send_raw()` now sends `X-XDR-Timestamp` + a sigv2 signature by default. +8 Go tests (no-secret bypass, legacy v1 accepted/rejected-when-required, v2 valid/stale/future/wrong-sig/malformed-timestamp) + 5 Python tests (v1/v2 signing, header wiring, no-secret path). `go build`/`go vet`/`go test` and endpoint-agent's 191-test suite green. | `services/ingestion-gateway/main.go`, `services/endpoint-agent/agent.py` + tests | — | 2026-07-05 |
| **PY-PRINT-LOGGING** | Best-practice modernization (alert-writer, incident-builder) — replaced all ~48 production `print(..., flush=True)` calls in both services' event-loop/consumer/startup-validation code with a shared `logging.Logger` (`log`) emitting structured JSON-line records (`ts`/`level`/`service`/`message`) via a small `_JsonLogFormatter`. Level mapping preserved each call site's existing semantic: `WARN`/`SECURITY-WARN` → `log.warning`, `ERROR`/`SECURITY-FATAL` → `log.error`, everything else (ready/starting/consumed/started/`SECURITY` notices) → `log.info`. Message text left byte-identical (including existing `[component]`/`WARN:`/`ERROR:` tags) to avoid any log-parsing/grep breakage — only the emission mechanism and added structure changed. `ai-rag-service`'s 2 `print()` calls were deliberately left as-is (CLI usage hints printed only when run directly via `python main.py`, not service log lines — converting to structured logging would make them harder for a human to read at a terminal, not easier). +6 tests (logger instance check, no-`print(`-in-source guard on event-loop/startup functions, JSON formatter output shape — both services). 75/75 alert-writer+incident-builder+ai-rag tests green; full `xdr_topic_bootstrap` suite (1342 tests) unaffected. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py` + tests | — | 2026-07-05 |
| **SIM-LAYER-REALITY-GATE** (Track A only) | Credibility/labelling fix — 35 HA/scale/chaos/soak/pilot validation-run tables across `EnterpriseScaleHaService`, `TelemetryScalePilotService`, `SoakChaosValidationService`, `PilotExecutionService` previously wrote a `PASS`/score record indistinguishable from a real, measured validation result (Redpanda is single-node — GAP-004 — so none of these validators exercise real distributed infrastructure). One migration adds `is_simulated BOOLEAN DEFAULT true` + `evidence_basis VARCHAR(20) DEFAULT 'computed'` to all 35 tables (additive-only, safe on append-only tables). All 35 models gained the 2 fields in `$fillable`/`$casts`, **plus a `protected $attributes` model-level default** — discovered mid-implementation that Eloquent does not re-fetch DB column defaults after `INSERT`, so without this the in-memory object would show `null` immediately after `create()` even though the DB row was correct; fixed by setting the default at the model layer instead of relying on the DB round-trip. No service (`::create()` call-site) changes were needed — every current write path already gets the correct default. Track B (backing the key validators with a real multi-node harness so `evidence_basis` can become `'measured'`) remains open in REVIEW_BACKLOG.md. +8 PHP tests (representative coverage across all 4 services, one covering all 7 write points in a single pilot run). Full suite: 4556 passed (11632 assertions), up from 4548 — zero regressions. | 35× `app/Models/*.php`, 1 new migration, 4× `tests/Feature/*Test.php` | — | 2026-07-06 |
| **ASSET-INVENTORY** | Asset inventory / CMDB + advisory asset-criticality context — new `asset_inventory` (hostname/ip/owner/environment/asset_type, unique `external_id`) and `asset_criticality` (one row per asset, `criticality_tier`: crown_jewel/high/medium/low, FK to `asset_inventory.id`) tables, both mutable (not append-only — living CMDB records). `AssetContextService` (`ADVISORY_ONLY`/`NO_AUTO_RESPONSE` constants) provides `registerAsset()` (validated upsert by `external_id`), `setCriticality()`, bounded `importCsv()` (`MAX_CSV_ROWS=5000`, invalid environment/asset_type fall back to safe defaults per-row rather than failing the whole import), `findAssetForAlert()`/`assetContextForIps()` (tenant-scoped lookup by IP/hostname), and `dashboardStats()`. New `AssetInventoryController` + `/asset-inventory` UI (manual register form, CSV upload, per-asset criticality-tier form) gated by new `soc:assetinventory.view` (admin/analyst/viewer) and `soc:assetinventory.manage` (admin/analyst) permissions; new `php artisan asset:import {tenant} {path}` console command. `SocIncidentController::show()` now matches an incident's alert IPs against `asset_inventory` and the incident-detail view renders an advisory "Asset Context" panel (hostname/environment/criticality) — display-only, never gates workflow transitions or response. Added 2 new `ThreatHuntingService` hunt domains (`asset_inventory`, `asset_criticality`), bringing the total from 177 to 179 — required a 30-file cascade update of hardcoded `assertCount(177/179, ...)` domain-count assertions across the test suite (2 of the 30 used a historically-flagged multi-line `assertCount(\n    177,\n    ...)` form that a naive single-line find/replace would have missed; found via a `MULTILINE:` grep check before bulk-editing). Caught and fixed a real bug during test-writing: `importCsv()` used `$record['key'] ?: null` on CSV columns that may not exist in the header row — `?:` still evaluates the (undefined) left operand and throws in this app's error handling, unlike `??`; fixed to `($record['key'] ?? '') ?: null`. +39 PHP tests (service validation/upsert/CSV/lookup behavior, RBAC, route protection, hunt-domain registration, and — since no existing test actually rendered the incident-detail blade view — 2 new tests that GET `/soc/incidents/{id}` and assert the asset-context panel renders correctly). Updated `README.md`, `claude.md`, `docs/architecture/FEATURE_REGISTRY.md`, and `docs/architecture/plantuml/threat_hunting_flow.puml` hunt-domain-count/test-count references; left `docs/CHANGELOG.md` and other point-in-time snapshot docs (`FINAL_VALIDATION_SUMMARY.md`, `RELEASE_NOTES.md`, portfolio docs) untouched as historical records. Full suite: 4595 passed (11705 assertions), up from 4556 — zero regressions. | 2 new migrations, `app/Models/AssetInventory.php`, `app/Models/AssetCriticality.php`, `app/Services/AssetContextService.php`, `app/Http/Controllers/AssetInventoryController.php`, `app/Console/Commands/AssetImportCsvCommand.php`, `app/Http/Controllers/SocIncidentController.php`, `resources/views/asset-inventory/index.blade.php`, `resources/views/soc/incident.blade.php`, `config/soc.php`, `routes/web.php`, `app/Services/ThreatHuntingService.php`, `tests/Feature/AssetInventoryTest.php` + 30-file domain-count cascade | — | 2026-07-06 |
| **SIEM-SEARCH** | Read-only free-form search over raw alert telemetry — `SiemSearchService` queries the `xdr-alerts` OpenSearch index via a bounded `query_string` DSL (tenant_id term filter + detected_at range filter, `MAX_RESULTS=500`/`DEFAULT_MAX_RESULTS=100`/`MAX_QUERY_WINDOW_DAYS=30`/`MIN_QUERY_LENGTH=2`), falling back to a bounded Postgres `ILIKE` search over `security_alerts` (`alert_type`/`ip`/`detector_name`/`evidence::text`/`raw_event::text`) when OpenSearch is unreachable or returns non-2xx — mirrors the platform's existing OpenSearch graceful-degradation posture rather than introducing a new failure mode. Neither Laravel nor any prior service had a direct OpenSearch/ClickHouse query client (only health-check pings existed via `XdrStorageValidateCommand`); built a hand-rolled `Http::` query DSL client since no SDK was installed. Caught a real redaction gap while implementing: `TraceRedactor::row()` only deep-redacts `JSON_PAYLOAD_FIELDS` (`evidence`/`raw_event`/etc.) when the value arrives as a JSON **string** (the Postgres column shape) — OpenSearch hits decode those fields to arrays already, which would have silently bypassed redaction for the OpenSearch path only; fixed by re-encoding those two fields back to JSON strings before calling `TraceRedactor::row()`, restoring the same redaction contract for both sources. New `SiemSearchController` + single server-rendered `/siem-search` GET route (no separate JSON API — form submits `?q=` as a query string) gated by new `soc:search.view` permission (admin/analyst/viewer — read-only, granted broadly like other `.view` abilities). No new hunt domain (this is a distinct free-form surface, not a bounded/allowlisted `ThreatHuntingService` domain) and no new tables (fully stateless, read-only). +23 PHP tests (bounds clamping, OpenSearch success/failure/exception paths via `Http::fake()`, Postgres fallback matching/tenant-scoping/window-days/redaction, RBAC, route protection). | `app/Services/SiemSearchService.php`, `app/Http/Controllers/SiemSearchController.php`, `resources/views/siem-search/index.blade.php`, `config/soc.php`, `routes/web.php`, `tests/Feature/SiemSearchTest.php` | — | 2026-07-06 |
| **SECRETS-VAULT** | Pluggable secret-provider abstraction — `App\Contracts\SecretProvider` interface, `EnvSecretProvider` (default, `getenv()`-based, behavior-identical to pre-existing code), `VaultSecretProvider` (HashiCorp Vault KV-v2 read/write over `Http::`, testable via `Http::fake()`, never throws — falls back to default on any failure), `SecretProviderManager` (resolves backend from `config('secrets.backend')`, default `env`), bound in `AppServiceProvider`. `InternalAuthService::secret()` consults the provider only as a fallback layer *between* `config('xdr.internal_auth_secret')` (kept as the literal, first-checked call — required by `InternalAuthConfigMappingTest`'s source-string + behavioral assertions, which mutate `config()` directly and expect immediate effect) and the `APP_KEY` fallback, so the existing test contract is untouched and the provider only matters when the primary config value is empty. New `php artisan security:rotate-internal-token` (`--dry-run`) generates a strong random secret; `env` backend cannot durably persist a rotated value from a running process (prints it + operator instructions), `vault` backend writes it directly — every attempt recorded as a new `SecurityHardeningEvent::EVENT_SECRET_ROTATION` (extends the existing enum, doesn't touch existing rows). `SecretsValidationService::validate()` gained a `secret_backend` field and warns (never errors) if `vault` is configured but unreachable. New `config/secrets.php`, `.env.example` vars (`XDR_SECRET_BACKEND`, `XDR_VAULT_*`), full runbook at `docs/security/SECRET_ROTATION_RUNBOOK.md`. +26 PHP tests. | `app/Contracts/SecretProvider.php`, `app/Services/Secrets/*.php`, `app/Console/Commands/RotateInternalTokenCommand.php`, `app/Services/InternalAuthService.php`, `app/Services/SecretsValidationService.php`, `app/Models/SecurityHardeningEvent.php`, `app/Providers/AppServiceProvider.php`, `config/secrets.php`, `docs/security/SECRET_ROTATION_RUNBOOK.md`, `tests/Feature/SecretsVaultTest.php` | — | 2026-07-06 |
| **ASSET-TENANT-OVERWRITE** | Fixed tenant-isolation bug in `AssetContextService::registerAsset()` — `external_id` was globally unique, so a CSV import for tenant B supplying tenant A's `external_id` matched A's row and flipped its `tenant_id` to B (silent cross-tenant asset overwrite). New migration drops the global unique index and adds `unique(['tenant_id', 'external_id'])`; the upsert key in `registerAsset()` now includes `tenant_id`. +1 regression test proving the same `external_id` under two tenants creates two separate rows. | `app/Services/AssetContextService.php`, `database/migrations/2026_07_08_010001_fix_asset_inventory_tenant_scoped_external_id.php`, `tests/Feature/AssetInventoryTest.php` | — | 2026-07-08 |
| **SIEM-QUERYSTRING-DOS** | Fixed availability risk in `SiemSearchService::searchOpenSearch()` — raw analyst input was fed to OpenSearch's `query_string` DSL with no leading-wildcard guard or server-side timeout, letting a single `*abc`-style query force an expensive full-index scan on the shared OpenSearch node (the tenant filter bounds *which* data is scanned, not the cost of scanning it). Switched to `simple_query_string` (no regex/leading-wildcard DoS surface, never throws on malformed syntax), added `"timeout":"2s"` to the request body, and added a defensive `MAX_QUERY_LENGTH=256` check in the service itself (previously enforced only by controller-level validation that didn't actually exist, so a direct service call had no length bound at all). +2 tests. | `app/Services/SiemSearchService.php`, `tests/Feature/SiemSearchTest.php` | — | 2026-07-08 |
| **SIEM-ASSET-CONTEXT-UNUSED** | Fixed spec-drift finding — `AssetContextService`/`AssetInventoryController` docblocks and UI copy claimed asset criticality "ranks the analyst queue," but no code path actually orders any listing by `criticality_tier` (it's display-only on the incident detail panel). Softened the wording across the service docblock, controller docblock, `FEATURE_REGISTRY.md`, and the asset-inventory index view to accurately describe current behavior ("surfaces asset context for analyst triage... display-only, does not order or filter any listing") rather than implementing sort-by-criticality against the core incident-listing query (judged out of proportion for a Low-priority documentation-accuracy fix). | `app/Services/AssetContextService.php`, `app/Http/Controllers/AssetInventoryController.php`, `resources/views/asset-inventory/index.blade.php`, `docs/architecture/FEATURE_REGISTRY.md` | — | 2026-07-08 |
| **ENT-SEC-WEAK-DEFAULT-SECRETS** | Fixed enterprise blocker — the base `docker-compose.yml` falls back to well-known weak defaults for datastore credentials (`${DB_PASSWORD:-postgres}`, `${CLICKHOUSE_PASSWORD:-detector}`, `${GF_SECURITY_ADMIN_PASSWORD:-admin}`), and `docker-compose.prod.yml` didn't override any of them — a production deploy could silently succeed on dev defaults if the operator forgot to set the corresponding env vars. `docker-compose.prod.yml` now overrides `postgres`/`clickhouse`/`grafana` `environment:` blocks with Docker Compose's fail-closed `${VAR:?message}` syntax (verified via `docker compose config` — fails when unset, succeeds when set); `.env.production.example` gained `CLICKHOUSE_PASSWORD`/`GF_SECURITY_ADMIN_USER`/`GF_SECURITY_ADMIN_PASSWORD` placeholder entries. New validator check `PDP-15` (`xdr_production_profile_validate.py`) confirms the compose overrides use fail-closed syntax and that the production env file doesn't leave these on a placeholder/weak value — checks PDP-01 through PDP-15 now (was 14). +10 Python tests (script) — base compose (local/dev) behavior is unchanged; verified via `docker compose config --quiet` on both the base file alone and the base+prod overlay. | `docker-compose.prod.yml`, `.env.production.example`, `scripts/xdr_production_profile_validate.py`, `tests/xdr_topic_bootstrap/test_xdr_production_profile_validate.py`, `docs/operations/PRODUCTION_DEPLOYMENT_PROFILE.md` | — | 2026-07-08 |
| **TEST-SHARED-DEV-DB** | Test-infra fix (implemented directly by the user) — `phpunit.xml` previously had no `DB_DATABASE` override, so `php artisan test` ran against the live `detector` Postgres database; a `migrate:fresh` in that state would wipe real dev data, and the shared DB also blocked safe parallel test workers. `phpunit.xml` now overrides `DB_CONNECTION=pgsql`/`DB_DATABASE=detector_test`, a dedicated database `RefreshDatabase` migrates once at suite start and rolls back per-test; new `.env.testing.example` and `docs/guides/TESTING.md` document the one-time `createdb detector_test` setup. Verified in this session: created the `detector_test` database (one-time setup) and confirmed the full suite runs clean against it without any `migrate:fresh` prefix (4647 passed, 0 failures). | `phpunit.xml`, `.env.testing.example`, `docs/guides/TESTING.md`, `claude.md` | — | 2026-07-08 |
| **ENT-SEC-OPENSEARCH-OPEN** (security plugin + auth only — RBAC roles/audit logging remain open) | The base `docker-compose.yml` runs OpenSearch with `plugins.security.disabled: "true"` for local/demo convenience — fully unauthenticated, plain HTTP. `docker-compose.prod.yml` now overrides this to `"false"` and requires `OPENSEARCH_INITIAL_ADMIN_PASSWORD` via fail-closed `${VAR:?message}` syntax. Enabling the plugin also switches OpenSearch to its bundled self-signed demo certificate on the HTTP layer, so `alert-writer-service`'s `write_opensearch()` (previously fully unauthenticated, no TLS handling at all) now sends HTTP Basic Auth via `XDR_OPENSEARCH_USER`/`XDR_OPENSEARCH_PASSWORD` and a `verify=` flag driven by `XDR_OPENSEARCH_VERIFY_TLS` (default `true`; must be `false` against the demo cert). `SiemSearchService`/`XdrStorageValidateCommand` (PHP) already had conditional Basic Auth wired from the SIEM-SEARCH/earlier work — they gained the matching `verify_tls` config toggle (`config/xdr.php`). New validator checks: **PDP-16** confirms `docker-compose.prod.yml` explicitly sets `plugins.security.disabled: "false"` (checks the prod overlay in isolation, matching the PDP-15 pattern, since the validator doesn't merge the base compose file); **PDP-15** extended to also fail-close `OPENSEARCH_INITIAL_ADMIN_PASSWORD`/`XDR_OPENSEARCH_PASSWORD`. Checks now run PDP-01 through PDP-16. **Explicitly NOT done in this pass** (kept open, see REVIEW_BACKLOG.md): fine-grained least-privilege RBAC roles (everything currently shares the built-in `admin` superuser — a dedicated write-only role for alert-writer-service and read-only role for Laravel is real defense-in-depth, not yet configured) and OpenSearch audit-logging config (`plugins.security.audit.*`). **Also explicitly not verified**: live container boot — the Docker daemon was not running in this environment, so this was validated only statically (`docker compose config` against the base file alone and the base+prod overlay, both with required vars set and unset, confirming fail-closed behavior and that local/dev is unaffected) plus unit tests of the code-level auth/TLS wiring; a real deployment must run `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d opensearch alert-writer-service` to confirm the security plugin actually initializes and the demo admin password meets its strength policy before relying on this. +5 Python tests (`write_opensearch` auth/verify wiring) + 6 Python tests (PDP-15/16 validator) + 2 PHP tests (verify_tls config). | `docker-compose.prod.yml`, `.env.example`, `.env.production.example`, `config/xdr.php`, `services/alert-writer-service/main.py`, `app/Services/SiemSearchService.php`, `app/Console/Commands/XdrStorageValidateCommand.php`, `scripts/xdr_production_profile_validate.py`, `docs/operations/PRODUCTION_DEPLOYMENT_PROFILE.md`, `tests/alert_writer/test_opensearch_auth.py`, `tests/xdr_topic_bootstrap/test_xdr_production_profile_validate.py`, `tests/Feature/SiemSearchTest.php` | — | 2026-07-08 |
| **META-DOC-MISREPRESENT** | Documentation reframing (wording-only, per explicit user direction: keep every technical disclaimer, Non-Goals, and capability-boundary claim intact — only remove language that frames the platform as an academic-thesis-defense demo). Covered `docs/thesis/THESIS_POSITIONING.md` (full rewrite: "Thesis Positioning" → "Platform Positioning", "research prototype" → "platform"/"current-phase platform" throughout, "academically justified" → "documented", "Research Contributions" → "Technical Contributions"), `docs/thesis/DEFENSE_PREPARATION.md` (title → "Platform Positioning — Reviewer Q&A", every "thesis"/"academic scope"/"master's-level research prototype" reference reworded, Splunk-comparison table's "Research: demonstrate..." / "Research prototype" cells reworded), `docs/thesis/LIMITATIONS_FUTURE_WORK.md` (scope-boundary table + "Next Research Phase" heading), `docs/thesis/SUBSYSTEM_EXPLAINABILITY.md` (one "research prototype" mention in the resilience-simulation tradeoff note), `docs/guides/LIMITATIONS_AND_CLAIMS.md` (5 spots: header line, ML-classifier claim, SOAR claim, live-causal-proof claim, footer cross-reference), `docker-compose.prod.yml` header comment ("Does NOT claim full HA, multi-region, or commercial readiness" → states the same technical fact — single-region/single-node, full HA tracked by ENT-REL-SIMULATED-HA — without the self-deprecating framing), and `docs/operations/PRODUCTION_DEPLOYMENT_PROFILE.md` ("controlled production-pilot posture for academic thesis demonstration" → "...for enterprise deployment evaluation"). `docs/thesis/*.docx` (the actual submitted internship-report deliverables) and code-level literal identifiers (`demo_feed.py`, `demo_run_id`, `demo_scenario_runs` table, etc.) were deliberately left untouched — out of scope for a wording pass, and in the `.docx` case not something a text-editing pass can meaningfully touch anyway. Documentation-only change (no test assertions reference the reworded strings, confirmed by grep beforehand); verified via targeted `DocumentationFreezeTest` run (34 passed) and `docker compose config` (both the base file alone and the base+prod overlay parse cleanly) rather than a full suite run, per CLAUDE.md's documentation-only policy. | `docs/thesis/THESIS_POSITIONING.md`, `docs/thesis/DEFENSE_PREPARATION.md`, `docs/thesis/LIMITATIONS_FUTURE_WORK.md`, `docs/thesis/SUBSYSTEM_EXPLAINABILITY.md`, `docs/guides/LIMITATIONS_AND_CLAIMS.md`, `docker-compose.prod.yml`, `docs/operations/PRODUCTION_DEPLOYMENT_PROFILE.md` | — | 2026-07-08 |
| **TEST-UNTRAITED** | Audit of every `tests/Feature/*Test.php` (+ `tests/Unit/*Test.php`) missing `RefreshDatabase`/`DatabaseTransactions` — the backlog finding worried these could leak DB writes across test runs (the flakiness a mandatory `migrate:fresh` prefix used to paper over). Found **7** untraited files, not the 5 originally named: `DocumentationFreezeTest`, `EndpointAgentStatusHelperTest`, `IntegrationConfigCacheTest`, `InternalAuthConfigMappingTest`, `XdrRuleRegistryValidatorTest`, plus `tests/Feature/ExampleTest.php` and `tests/Unit/ExampleTest.php` (the Laravel/PHPUnit scaffold defaults, not in the original component list). Verified each by grepping for `DB::`, `::create(`, `assertDatabaseHas`/`Missing`, `->insert(`, and `factory(` (zero hits in all 7) and reading every file in full: all 7 are genuinely DB-free — config mutation, static-method calls, file-content assertions, and unauthenticated route checks only. `tests/Unit/ExampleTest.php` additionally extends plain `PHPUnit\Framework\TestCase`, never booting Laravel at all. **No trait added** — a `RefreshDatabase`/`DatabaseTransactions` wrap on tests that never touch the DB would be pure overhead, not a safety improvement. Instead, added a one-line docblock to each of the 6 files lacking one (`XdrRuleRegistryValidatorTest` already documented its DB-free intent) so a future contributor doesn't need to re-audit. Clears the way for TEST-NO-SCHEMA-DUMP (still open) to drop the mandatory `migrate:fresh` prefix. Documentation/comment-only change — no test behavior altered; verified via `php -l` on all touched files plus a targeted run (5 + 3 tests, all green). | `tests/Feature/DocumentationFreezeTest.php`, `tests/Feature/EndpointAgentStatusHelperTest.php`, `tests/Feature/IntegrationConfigCacheTest.php`, `tests/Feature/InternalAuthConfigMappingTest.php`, `tests/Feature/ExampleTest.php`, `tests/Unit/ExampleTest.php` | — | 2026-07-08 |
| **ENT-SEC-OPENSEARCH-OPEN** (RBAC + audit logging — closes the finding fully) | Completes the remaining scope left open by the earlier ENT-SEC-OPENSEARCH-OPEN entry above (security plugin + shared-`admin` auth). New `infra/opensearch/security/roles.yml` + `roles_mapping.yml` (committed, no secrets) define two least-privilege roles: `xdr_alert_writer_role` (the `INDEX` action group on `xdr-alerts*` — deliberately excludes delete) for alert-writer-service, and `xdr_reader_role` (`READ` + `CLUSTER_MONITOR` on `xdr-alerts*`) for Laravel. `internal_users.yml.example` is the committed template (a valid-format bcrypt hash for the literal placeholder password `REPLACE-BEFORE-DEPLOY`, generated via PHP's `password_hash()` since no live OpenSearch was available to run its own `hash.sh`); the real `internal_users.yml` is gitignored like `.env` (added to `.gitignore`) since it holds real password hashes — an operator must copy the template and regenerate real hashes with `plugins/opensearch-security/tools/hash.sh` before deploying. All three files are bind-mounted read-only into `/usr/share/opensearch/config/opensearch-security/` via a new `volumes:` entry on the `opensearch` service in `docker-compose.prod.yml`, where OpenSearch loads them once on first boot. `alert-writer-service`'s compose environment now sources `XDR_OPENSEARCH_WRITER_USER`/`XDR_OPENSEARCH_WRITER_PASSWORD` (new, distinct fail-closed vars) instead of the shared `XDR_OPENSEARCH_USER`/`PASSWORD`, so it authenticates as `xdr_alert_writer`; Laravel keeps the existing var names but `.env.production.example` now points them at `xdr_reader` instead of `admin`. Audit logging added directly as `opensearch` service environment variables (no extra file needed — the same mechanism the image already uses for `discovery.type`/`plugins.security.disabled`): `plugins.security.audit.type=internal_opensearch`, REST-layer only (`enable_rest=true`, `enable_transport=false` — transport-layer audit is very high-volume on every internal call and unnecessary on a single node), `log_request_body=false` (avoids persisting raw alert payloads into the audit index), default `disabled_rest_categories` to avoid flooding the audit index with every successful call. New validator check **PDP-17** confirms the RBAC config files exist and that `roles_mapping.yml` doesn't map either app role to OpenSearch's reserved `admin`/`all_access` superuser roles (regex-anchored to YAML top-level keys, not a naive substring match, so the file's own explanatory comments mentioning "admin" don't trigger a false FAIL); `_DATASTORE_CREDENTIAL_ENV_KEYS` (PDP-15) extended with `XDR_OPENSEARCH_WRITER_PASSWORD`. Checks now run PDP-01 through PDP-17. +7 Python tests (PDP-17: missing files, admin-role FAIL, all_access-role FAIL, clean PASS, comment-false-positive guard, local-profile WARN, pass-against-real-repo-files). **Also explicitly not verified** (same caveat as the earlier entry): live container boot and the RBAC bootstrap-on-first-boot sequence specifically — the Docker daemon was not running in this environment; run `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d opensearch alert-writer-service` (after generating a real `internal_users.yml`) to confirm both the security plugin and the new roles actually take effect. No PHP code changed in this pass — verified via `docker compose config` (base file alone and base+prod overlay, both with required vars set/unset) and full Python `xdr_topic_bootstrap` discovery (1365 passed) rather than a full PHP suite run, since nothing PHP-side was touched. | `infra/opensearch/security/roles.yml`, `infra/opensearch/security/roles_mapping.yml`, `infra/opensearch/security/internal_users.yml.example`, `.gitignore`, `docker-compose.prod.yml`, `.env.production.example`, `scripts/xdr_production_profile_validate.py`, `tests/xdr_topic_bootstrap/test_xdr_production_profile_validate.py`, `docs/operations/PRODUCTION_DEPLOYMENT_PROFILE.md` | — | 2026-07-08 |
| **DATA-RESIDENCY-ERASURE** | Adds per-tenant retention overrides + a GDPR right-to-erasure workflow, closing the gap where `SecurityRetentionCommand` only had a fixed global 30/90-day prune and never touched `security_incidents` at all. New `tenant_retention_policies` table (mutable, unique per tenant) overrides the global `--events-days`/`--alerts-days`/`--incidents-days` defaults; `security_events` has no `tenant_id` column (documented platform-wide gap, `TenantBoundaryService::UNISOLATED_TABLES`) so it stays globally-pruned by design, while `security_alerts`/`security_incidents` are now pruned per-tenant with legacy `tenant_id IS NULL` rows falling back to the global default. `security_incidents` pruning is new — it was never pruned before this change. New `DataResidencyErasureService` implements the erasure request lifecycle: `pending` → `approved`/`rejected` (self-approval blocked — approver must differ from requester) → `executed`. Approving a request only flips its status; **real deletion is CLI-only** via new `php artisan data-erasure:execute {request_id}`, mirroring the platform's existing `php artisan dlq:replay` pattern of never triggering destructive operations over HTTP. A request-level `dry_run` flag (default `true`) makes execution a read-only row count with no approval required; real deletion needs `dry_run=false` AND `status=approved`. Execution is tenant-scoped and only ever touches `security_alerts`/`security_incidents` (never `security_events`, which has no tenant column to scope by). Every request/approve/reject/dry-run/execute action is recorded in the new append-only `data_erasure_audit_events` table. New RBAC abilities: `retention.manage` (admin-only — edit policy, approve/reject) and `erasure.request` (admin+analyst — view page, submit requests); viewer gets neither. New UI at `/data-residency`. Threat hunting domains: +2 (`tenant_retention_policies`, `data_erasure_requests`) → 181 total. Found and fixed one bug during testing: `DataErasureExecuteCommand`'s `orWhere('id', $requestId)` crashed with a Postgres `invalid input syntax for type bigint` error for any non-numeric `request_id` (the command's actual primary identifier) — fixed by only adding that clause when the input is `ctype_digit`. +32 tests (`DataResidencyErasureTest`) covering policy resolution, full request lifecycle, self-approval blocking, dry-run vs. real deletion, tenant scoping, retention-command tenant-awareness, RBAC, and routes. Also attempted `php artisan schema:dump` as an unrelated light task while the full suite ran in the background — it broke 74 tests (serial-column defaults applied inconsistently on `RefreshDatabase` load) and was fully reverted; see `TEST-NO-SCHEMA-DUMP` in `REVIEW_BACKLOG.md` for the retained findings. Full suite green after the revert. | `database/migrations/2026_07_09_010001_create_data_residency_erasure_tables.php`, `app/Models/{TenantRetentionPolicy,DataErasureRequest,DataErasureAuditEvent}.php`, `app/Services/DataResidencyErasureService.php`, `app/Console/Commands/{SecurityRetentionCommand,DataErasureExecuteCommand}.php`, `app/Console/Kernel.php`, `app/Http/Controllers/DataResidencyController.php`, `resources/views/data-residency/index.blade.php`, `routes/web.php`, `config/soc.php`, `app/Services/ThreatHuntingService.php`, `tests/Feature/DataResidencyErasureTest.php`, `docs/architecture/FEATURE_REGISTRY.md`, `claude.md` | — | 2026-07-09 |
| **TEST-NO-PARALLEL** | Installed `brianium/paratest` (`^7.3` — the version compatible with this project's PHP `^8.1`; the package's own latest `v7.23` requires PHP `~8.4`/`~8.5`) as a committed dev dependency, so `php artisan test --parallel --recreate-databases` no longer needs a one-off local `composer require` — it now works out of the box for anyone who clones the repo and runs `composer install`. Verified rather than assumed: ran the full suite in parallel (12 workers, matching this machine's core count) against freshly recreated per-worker databases — confirmed exactly 12 `detector_test_test_1..12` databases were created, and the result was identical to the serial baseline (4681 tests, 11843 assertions, `OK`), in 332s vs. 613s serial (~46% faster). `claude.md`/`docs/guides/TESTING.md` updated to drop the "requires: composer require" caveat (no longer true) and record the verified worker count/timing. Also fixed a directly-related CI bug found while reading the test-database docs: `.github/workflows/ci.yml`'s "Migration check" step ran `php artisan migrate:fresh --force` immediately before `php artisan test` — since `RefreshDatabase` already runs its own `migrate:fresh` at suite boot, this was the exact double-migration pattern `claude.md` already warns against for local runs, just present in CI instead. Removed the redundant step (route caching, the next step, does not touch the database, so nothing downstream depended on it). CI itself was **not** switched to `--parallel` — no local way to verify a GitHub Actions runner's Postgres service container behaves identically for per-worker DB creation, so left as a separate, deliberately-unclaimed follow-up rather than risk an unverified CI break. | `composer.json`, `composer.lock`, `claude.md`, `docs/guides/TESTING.md`, `.github/workflows/ci.yml` | — | 2026-07-09 |
| **NORM-ASYNC-COMMIT-LOSS** | Fixed the gap where `normalizer-worker`'s Pandaproxy consumer implicitly commits offsets on the *next* `GET .../records` call, while the just-fetched batch's normalized events were only handed to an async producer channel (non-blocking send) — the fetch loop would immediately call `consumerPoll` again (committing) without any regard for whether those events had actually reached Kafka, so a crash between "committed" and "published" could lose up to the full `XDR_NORMALIZER_QUEUE_DEPTH` (default 200,000) buffered events. Fix: each normalized event is now wrapped in a `queuedEvent{event, wg *sync.WaitGroup}`; `consumeOnce` creates a fresh `sync.WaitGroup` per fetch batch, `Add(1)`s it before enqueueing each event, and calls `wg.Wait()` — blocking the next `consumerPoll` (and therefore the implicit commit) — until every event from that batch has been through a completed publish attempt in `producerLoop` (success, or failure recorded to the DLQ; `producerLoop` calls `wg.Done()` in both cases so a permanently-broken produce path can't stall the pipeline forever, matching the existing poison-message-isolation precedent of "isolate and continue" rather than block indefinitely). The HTTP ingestion path (`normalizeHTTP`, `/v1/normalize`) passes a `nil` WaitGroup since it is intentionally fire-and-forget (returns 202 before publish, with its own existing queue-depth backpressure check). Also fixed a related bug surfaced by the same code path: on a producer flush failure, the DLQ record previously included only `{"error": ..., "count": N}` — the actual failed event payloads were never written anywhere, making them permanently unreplayable; the DLQ record now includes the full `events` array. Verified with a genuine regression test (`TestCommitDoesNotRaceAheadOfPublish`, using an `httptest.Server` mock of the Pandaproxy consumer/produce API) that specifically proves the *ordering* guarantee — the mock's second-poll handler snapshots an event log and asserts `"publish"` is already present; confirmed this test actually catches the old bug by temporarily removing the `wg.Wait()` call and re-running (failed with the expected message), then restored the fix and re-ran clean. +3 more tests: nil-WaitGroup doesn't block (HTTP path), WaitGroup blocks until publish completes (success path, using a held-open mock HTTP handler), and the DLQ full-events fix. All 4 new tests pass; `go build`/`go vet` clean. `go test -race` unavailable in this environment (`CGO_ENABLED` requires gcc, absent) — tested without the race detector; the WaitGroup/channel pattern used has no unsynchronized shared-memory access outside of already-atomic counters, so this is a tooling gap rather than an unverified concurrency risk, but should be re-run with `-race` in an environment with a C toolchain before relying on it further. Pre-existing `gofmt` non-compliance in this file (inconsistent struct-field alignment) predates this change (confirmed via `git stash`) and was left alone — out of scope for a focused bugfix. | `services/normalizer-worker/main.go`, `services/normalizer-worker/main_test.go` | — | 2026-07-09 |
| **CONSUMER-GROUP-EPHEMERAL** | Fixed all 5 Pandaproxy consumer loops across `alert-writer-service` (`event_loop`, `shadow_event_loop`, `dlq_consumer_event_loop`, `pipeline_dlq_consumer_event_loop`) and `incident-builder-service` (`event_loop`) that minted a brand-new ms-timestamp consumer **group** name (`f"{base}-{int(time.time()*1000)}"`) on every process start *and* on every in-process reconnect (consecutive-error or DLQ-write-failure recovery, not just genuine offset errors) — since Kafka/Redpanda consumer groups own committed-offset state, a fresh group name has no prior offset to resume from and `auto.offset.reset=earliest` reprocesses the entire topic history every single time. Split each loop's identity generation into three concerns: `_stable_group()` (the env-configurable base name, e.g. `alert-writer-v1`, with **no** timestamp suffix — reused across restarts and across non-offset-error reconnects so Redpanda resumes from the last real committed offset), `_new_instance_id()` (still ms-based, regenerated on every connect/reconnect — this is the separate Pandaproxy REST *instance* id, kept unique specifically to avoid instance-id collisions on the proxy side, distinct from the group), and `_fresh_group_for_offset_reset()` (ms-suffixed, used **only** when `is_offset_err` is true — the one legitimate case where the previously-committed offset is actually invalid and a clean group is needed to re-apply `auto.offset.reset`). The existing three-way `should_recreate` trigger (offset error OR 3 consecutive errors OR DLQ-write failure) is unchanged — only the *group* is now conditionally recreated (`if is_offset_err: group = _fresh_group_for_offset_reset()`), while the instance id always regenerates. All existing tests (74 across both services) passed unchanged, confirming no test depended on the old ms-timestamp group behavior — added 6 new tests (3 per service, `TestConsumerGroupStability`) that lock in the new contract: group stays identical across a 3-consecutive-error recreation, group changes (with a `-reset-` suffix derived from the stable base) only on a genuine offset error, and the default initial group has no timestamp suffix at all. Full alert-writer + incident-builder suite: 80 tests, all green. | `services/alert-writer-service/main.py`, `services/incident-builder-service/main.py`, `tests/alert_writer/test_alert_writer.py`, `tests/incident_builder/test_incident_builder.py` | — | 2026-07-09 |
| **CODE-STRUCT-DECOMPOSE** (1st seam only — `internal/ioc`; 3 more seams + normalizer/alert-writer remain open) | Extracted the IOC threat-intel lookup+cache from `correlation-worker/main.go` into a new `internal/ioc` package — the smallest, most self-contained unit in the file (no dependency on `Worker`, the consume loop, or any other correlation state), matching the backlog's own "one seam at a time" guidance rather than attempting the full `internal/rules`/`internal/kafka`/`internal/correlate` split in one pass. New `services/correlation-worker/internal/ioc/ioc.go` exports `Cache`/`NewCache`/`Get`/`Set`/`Len`, `Lookup`, `Severity`, `Confidence`, and a `Configure(ttl, max)` entry point called once at the top of `main()` (replacing the old package-var-init pattern that read the same two env vars). The 3 call sites (`ruleIOCIPMatch`/`ruleIOCDomainMatch`/`ruleIOCHashMatch`) now call `ioc.Lookup(...)`/`ioc.Severity(...)`/`ioc.Confidence(...)`; each site's local variable was renamed `iocResult` (was `ioc`) to avoid shadowing the new package name. Metrics endpoint updated (`ioc.LookupTotal`/`ioc.MatchTotal`/`ioc.CacheHits` in place of the old package-level atomics). `ioc_cache_test.go`'s 5 tests moved into `internal/ioc/ioc_test.go` as white-box tests of the new package (unexported `cacheEntry`/`entries` fields stay accessible since the tests moved with the type), +2 new tests for `Severity`/`Confidence` default-value behavior. Pure code movement — no logic changed, confirmed identical: `go build ./...`, `go vet ./...`, and `go test ./...` all clean across both the `main` and `internal/ioc` packages; `main.go` 2950→2836 lines (114 lines extracted). **Not run**: the live-pipeline verifier CLAUDE.md calls for on correlation-hot-path changes — the Docker daemon is unavailable in this environment (same limitation noted throughout this session's other infra work). Given this is provably behavior-identical code movement (same tests, same logic, just relocated + renamed), risk is low, but a live run is recommended before the next deploy to correlation-worker. | `services/correlation-worker/main.go`, `services/correlation-worker/internal/ioc/ioc.go`, `services/correlation-worker/internal/ioc/ioc_test.go` | — | 2026-07-09 |
| **CODE-STRUCT-DECOMPOSE** (seam 2 — `internal/shadowrules`; core endpoint/cross-domain/streaming rules + `internal/kafka` transport still open) | Extracted the `EndpointAlert` foundation (`EndpointAlert` type, `epStr`/`epInt64`/`makeEndpointAlert`/`dedupeEndpointAlerts`) and the network shadow rules (`correlateNetworkShadowAll` + 9 `ruleNetwork*` DNS/proxy/firewall functions — the most isolated rule group, zero relation to the process/endpoint rule tables and zero use of `internal/ioc`) into a new `internal/shadowrules` package, all exported (`EpStr`, `EpInt64`, `MakeEndpointAlert`, `DedupeEndpointAlerts`, `CorrelateNetworkShadowAll`). Since `EndpointAlert` is the return type of all 44 `rule*`/`correlate*` functions in the file — not just the 9 physically moved — every remaining rule signature in `main.go` had to be mechanically updated to `shadowrules.EndpointAlert` regardless of which specific rule group moved; used a one-off Python script (deleted after use, not committed) to do the ~40 substitutions precisely rather than 40 manual edits, then verified with `go build` — which caught a real bug from an overlapping string-replace pattern (`[]EndpointAlert{a}` briefly became `[]shadowrules.shadowrules.EndpointAlert{a}` because two sequential replace rules both matched the same substring; fixed the one occurrence directly). Zero tests existed for any of this code before (confirmed by the research pass: no test covered `EndpointAlert`, any `rule*` function, or the helper functions) — added 9 new tests in `internal/shadowrules/shadowrules_test.go` covering the foundation helpers (`EpStr` nested-path/missing-field behavior, `EpInt64` type coercion, `MakeEndpointAlert` deterministic ID + fallback event-ID logic, `DedupeEndpointAlerts`) and a representative slice of the network rules (`CorrelateNetworkShadowAll` empty-input and aggregation behavior, `ruleNetworkSuspiciousDnsTld` fire/no-fire cases) — not all 9 network rules individually, which remains a separate, larger test-writing effort. `main.go` 2836→2438 lines; new package 423 lines. **Also fixed a real, previously-undetected bug** found while double-checking Docker build parity for this change: `services/correlation-worker/Dockerfile` only ran `COPY main.go ./`, which never copied the `internal/` directory at all — meaning the Docker build has been broken since seam 1 (`internal/ioc`) was introduced in the prior commit, and nothing caught it because the Docker daemon is unavailable in this environment. Fixed with `COPY internal ./internal`, which covers both `internal/ioc` and `internal/shadowrules`. `go build`/`go vet`/`go test ./...` clean across all 3 packages (`main`, `internal/ioc`, `internal/shadowrules`). **Not run**: the live-pipeline verifier and an actual `docker build` (daemon unavailable) — given this session already found one real Dockerfile bug that only a live build would have caught, a live verification pass before the next correlation-worker deploy is especially recommended here, not just a formality. | `services/correlation-worker/main.go`, `services/correlation-worker/internal/shadowrules/shadowrules.go`, `services/correlation-worker/internal/shadowrules/shadowrules_test.go`, `services/correlation-worker/Dockerfile` | — | 2026-07-10 |
| **ENT-SDLC-NO-SUPPLYCHAIN** (base-image digest pinning only; SBOM/scan/sign remain open — need syft/trivy/cosign, unavailable in this environment) | Verified the "no dep pinning/lock" half of this finding was already largely false: all 3 Python `requirements.txt` files (`ai-rag-service`, `alert-writer-service`, `incident-builder-service`) already pin every direct dependency to an exact version (`fastapi==0.139.0`, etc.), and all 3 Go services (`correlation-worker`, `ingestion-gateway`, `normalizer-worker`) have zero external dependencies (stdlib-only `go.mod`, no `go.sum` needed or missing). The real, previously-unaddressed gap was all 6 Dockerfiles floating on **mutable** base-image tags (`python:3.12-slim`, `golang:1.26-alpine`, `alpine:3.22`) with no digest pin — meaning a future upstream tag update could silently change what gets built/deployed without any corresponding code change, a real supply-chain risk vector. Fetched the current manifest-list digest for each of the 3 tags directly from the Docker Hub registry v2 API (`auth.docker.io` token exchange + `registry-1.docker.io` manifest HEAD request with `Accept: application/vnd.oci.image.index.v1+json,...`, which returns the multi-arch manifest-list digest, not a single-platform one — verified all 3 digests are exactly 64 hex chars) since the Docker daemon itself is unavailable in this environment (no `docker pull`/`docker inspect` path). Pinned all 6 Dockerfiles to `image:tag@sha256:<digest>` format (keeps the tag for human readability alongside the enforced digest). Verified with `docker compose config --quiet` (parses cleanly) — **not verified**: an actual `docker build`/pull against these digests, since the daemon is unavailable; if a digest is ever wrong (e.g. this environment's network path returned something registry-mirror-specific), the build would fail loudly and obviously rather than silently, so this is a fail-safe risk, not a silent one. SBOM generation (syft/cyclonedx), vulnerability scan gating (trivy), and signed builds (cosign) all remain out of scope — none of those tools are installed or installable-and-verifiable in this environment. | `services/ai-rag-service/Dockerfile`, `services/alert-writer-service/Dockerfile`, `services/correlation-worker/Dockerfile`, `services/incident-builder-service/Dockerfile`, `services/ingestion-gateway/Dockerfile`, `services/normalizer-worker/Dockerfile` | — | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE** (seam 3 — cross-domain shadow correlation; core endpoint/streaming rules + `internal/kafka` transport still open) | Moved `correlateEndpointShadowCrossDomain` + its 5 rule functions (`ruleCrossDomainIdentityEndpoint`, `ruleCrossDomainIdentityPersistence`, `ruleCrossDomainSaaSBeacon`, `ruleCrossHostSharedDestinationLolbin`, `ruleCrossDomainAttackProgression`) from `correlation-worker/main.go` into the `internal/shadowrules` package established in seam 2, exporting the aggregator as `CorrelateEndpointShadowCrossDomain`. Discovered mid-move (via a `go build` failure) that this block — and several rule groups *not yet moved* — share 3 static lookup tables (`linuxShellNames`, `downloaderNames`, `lolbinNames`, originally defined once in `main.go`); rather than duplicating them into `shadowrules` and leaving a second, divergence-prone copy behind in `main.go`, moved all 3 tables into `shadowrules` as exported vars (`LinuxShellNames`, `DownloaderNames`, `LolbinNames`) and updated the ~11 remaining `main.go` call sites that still reference them. Used a second one-off Python script (not committed) for the extraction plus a corrective third pass after the first script's insertion accidentally kept the moved table definitions lowercase while renaming only their *usages* to the exported form — caught immediately by `go build: undefined: LinuxShellNames` etc., fixed directly. +4 new tests (`CorrelateEndpointShadowCrossDomain` empty-input/aggregation, `ruleCrossDomainIdentityEndpoint` fire-on-identity-failure-then-shell and no-fire-without-prior-failure cases) — this rule group had zero test coverage before, same as every other shadow rule group in this file. `main.go` 2438→2185 lines; `internal/shadowrules/shadowrules.go` 423→686 lines. `go build`/`go vet`/`go test ./...` clean across all 3 packages. **Not run**: live-pipeline verifier / `docker build` — same standing limitation (Docker daemon unavailable in this environment) as every other correlation-worker change this session; the Dockerfile's `COPY internal ./internal` fix from seam 2 already covers this addition automatically, no further Dockerfile change needed. | `services/correlation-worker/main.go`, `services/correlation-worker/internal/shadowrules/shadowrules.go`, `services/correlation-worker/internal/shadowrules/shadowrules_test.go` | — | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE** (seam 4 — streaming shadow rules; core endpoint rules + `internal/kafka` transport still open) | Moved `correlateEndpointShadowStreaming` + its 5 rule functions (`ruleStreamRapidShellChain`, `ruleStreamRapidPersistenceCreation`, `ruleStreamBurstOutboundActivity`, `ruleStreamShortLivedSuspiciousProcess`, `ruleStreamRapidExecutionBeaconPattern`) into `internal/shadowrules`, exported as `CorrelateEndpointShadowStreaming`. This block was fully self-contained (only 1 external call site, no shared static tables needed moving this time — unlike seam 3), so the extraction script was a clean single pass with no corrective follow-up required. +4 new tests (`CorrelateEndpointShadowStreaming` empty-input/aggregation, `ruleStreamRapidShellChain` fires-at-3-same-host / no-fire-below-threshold). `main.go` 2185→1963 lines; `internal/shadowrules/shadowrules.go` 686→912 lines. `go build`/`go vet`/`go test ./...` clean across all 3 packages. **Not run**: live-pipeline verifier / `docker build` — same standing limitation. Remaining: core endpoint rules (`ruleParentChildProcess` et al., ~750 lines) and Pandaproxy transport (`internal/kafka`); normalizer/alert-writer decomposition untouched. | `services/correlation-worker/main.go`, `services/correlation-worker/internal/shadowrules/shadowrules.go`, `services/correlation-worker/internal/shadowrules/shadowrules_test.go` | — | 2026-07-10 |
| **DATA-TIERING** (phase 1 — archive-before-delete; full hot/warm/cold ClickHouse/object-storage tiering remains open) | Replaced hard-delete with archive-then-prune in `SecurityRetentionCommand`: new `SecurityRetentionArchiveService` writes every row about to be pruned to a gzip-compressed JSONL file (`storage/app/archives/{table}/{tenant-or-global}/{timestamp}.jsonl.gz`) *before* deleting it, so retention no longer means the data is simply gone — a durable local archive exists first. Scoped deliberately to what's testable without new infra: no ClickHouse warm tier or object-storage cold tier (both need live infrastructure this environment can't provision or verify) — this is the bounded "never delete without archiving" step the backlog's own proposed fix listed as a prerequisite ("Replace hard-delete with archive-then-prune"). Handles the `security_events` vs. `security_alerts`/`security_incidents` asymmetry correctly: the former has no `tenant_id` column at all (a documented platform-wide gap), so archiving must skip tenant filtering entirely there rather than treating it as `tenant_id IS NULL` (which would be a different, wrong query) — added an explicit `hasTenantColumn` parameter to keep these two cases from being conflated. New command options: `--archive-dir=` (default `storage/app/archives`) and `--no-archive` (opt out, preserving the exact old fast-delete behavior — used by the 5 pre-existing `DataResidencyErasureTest` retention tests, which were updated to pass it so they don't start writing real files into `storage/app/archives` on every run). Uses `chunkById` for memory-safe archiving of large result sets; skips the archive-file write entirely when zero rows match (no empty files). Used a unique per-run timestamp filename (`Y-m-d_His_u`) rather than one shared per-day file to sidestep any dependency on gzip append-mode semantics across repeated same-day runs. Caught and fixed one real test-harness bug while writing tests: passing Windows temp-dir paths (`C:\Users\...`) as a raw interpolated string to `$this->artisan("... --archive-dir={$path}")` truncated/mis-tokenized the path (colon + backslashes), silently causing `0` rows to be archived where `1` was expected — switched to the array-argument form of `$this->artisan()`, which passes option values without string tokenization. +10 new tests (`SecurityRetentionArchiveTest`): gzip content matches archived rows, `security_events` no-tenant-column path, empty-match no-op (no file created), tenant-scope isolation, command-level default-archives and `--no-archive`-skips-archiving behavior. All pre-existing `DataResidencyErasureTest` retention tests (32) still pass unchanged aside from the added `--no-archive` flag. Full suite: 4687 passed, 0 failures. | `app/Services/SecurityRetentionArchiveService.php`, `app/Console/Commands/SecurityRetentionCommand.php`, `tests/Feature/SecurityRetentionArchiveTest.php`, `tests/Feature/DataResidencyErasureTest.php` | — | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE** (seams 4-6 — rule engine fully decomposed; Pandaproxy transport + normalizer/alert-writer remain) | Completed the remainder of the correlation-worker shadow-rule-engine decomposition in three more passes. Seam 4: streaming rules (fully self-contained, clean single-pass extraction). Seam 5: the 9 core endpoint rules (`ruleParentChildProcess` et al.) plus their static lookup tables — exported as `shadowrules.RuleXxx` since `correlateEndpointShadow` (which calls them) stayed in `main.go` at that point. Seam 6: the 13 remaining behavioral-visibility/analytics/threat-hunting rules **and** the `correlateEndpointShadow` aggregator itself, moved together once it had zero remaining dependency on `Worker`/`internal/ioc` — exported as `CorrelateEndpointShadow`. One extraction-script bug caught immediately by `go build: undefined: shadowrules` during seam 6: internal references to the already-exported `LinuxShellNames`/`DownloaderNames`/`LolbinNames` tables (moved in seam 3) still carried a leftover `shadowrules.` prefix from when this code lived in a different package — fixed directly (13 occurrences) since the tables are now local, same-package references. +9 more tests across the three seams (`ruleParentChildProcess`, `ruleFailedLoginBurst`, `ruleParentChildChain` fire/no-fire pairs; `CorrelateEndpointShadow` empty-input/telemetry-type-filter/aggregation cases). **Net result: `main.go` 2185→1165 lines — a 60% reduction from the original 2950-line file this backlog item started from.** The entire endpoint/network shadow-rule engine (IOC cache, all 44 `rule*`/`correlate*` functions, every static lookup table) now lives in `internal/shadowrules` + `internal/ioc`, each with their own tests. What remains in `main.go`: Worker/HTTP/Pandaproxy transport, the active identity/cloud correlation path (`correlate`/`correlateIdentityCloud` — a genuinely different concern, never in scope here), and 4 thin IOC-bridging functions (`ruleIOCIPMatch`/`ruleIOCDomainMatch`/`ruleIOCHashMatch`/`correlateEndpointShadowAll`) kept as intentional composition-root glue — they're the one place that needs both `internal/ioc` and `internal/shadowrules` together, so moving them into either package would just relocate the coupling, not remove it. `go build`/`go vet`/`go test ./...` clean across all 3 packages at every step. **Not run, same standing limitation as every correlation-worker change this session**: the live-pipeline verifier and an actual `docker build` (Docker daemon unavailable). This backlog item's rule-engine-decomposition scope is now considered effectively closed; the remaining Pandaproxy-transport, normalizer-worker, and alert-writer-service decomposition is different enough in shape (transport/IO code, not a rule engine with a shared alert type) that it continues as separate, lower-priority future work rather than "more seams of the same kind." | `services/correlation-worker/main.go`, `services/correlation-worker/internal/shadowrules/shadowrules.go`, `services/correlation-worker/internal/shadowrules/shadowrules_test.go` | — | 2026-07-11 |
| **TEST-NO-SCHEMA-DUMP** (root-caused and working — retry succeeded) | Root-caused the earlier failed attempt (74 NOT NULL violations on `id` columns) rather than leaving it permanently reverted. Hypothesis-driven investigation confirmed `PostgresSchemaState::load()` invokes a single sequential `psql --file=` run (no statement splitting/reordering) and `dump()` uses unremarkable `pg_dump --no-owner --no-acl --schema-only` — the load/dump mechanism itself was never the bug. The actual cause: `schema:dump` targets whatever `DB_DATABASE` the connection currently has, and the earlier attempt dumped the long-lived **dev** `detector` database, which had drifted from a clean `migrate:fresh` state (e.g. via an out-of-band `psql` session or an odd raw-SQL migration run) — `pg_dump` faithfully reproduced that drift onto a subset of tables. Verified the fix by: (1) recreating `detector_test` clean and running `migrate:fresh` against it explicitly (`DB_DATABASE=detector_test`), (2) generating the dump from *that* database, (3) mechanically diffing the output — 468 `bigint id NOT NULL` tables vs. 469 `ALTER TABLE ... SET DEFAULT nextval` statements (the 1 extra is the `migrations` table's `integer` `id`, using a different type, correctly excluded from the bigint count but still correctly defaulted) — zero missing, (4) replaying Laravel's exact `psql --file=` load command into a disposable database and confirming every `id` column, including the previously-broken `live_pilot_runs`, got its correct `nextval(...)` default, (5) running the **full test suite from an empty `detector_test`** (schema dump auto-loaded by `RefreshDatabase`, zero manual migration): **4687 passed, 0 failures.** Local Postgres client/server versions were confirmed matched (17.7/17.7), ruling out the classic pg_dump/psql version-mismatch failure mode as a contributing factor here. Updated `claude.md`/`docs/guides/TESTING.md` to replace the "tried and reverted, do not retry" caution with the actual root cause and a concrete regeneration rule (always dump from a database you just ran `migrate:fresh` against, never the ambient dev DB). Re-added the schema-dump-existence assertion to `DocumentationFreezeTest` (a prior identical assertion was removed when the first attempt was reverted). Migration files were **not** pruned — no `--prune` flag used, matching the project's established preference to keep migration history intact even with a working dump in place. | `database/schema/pgsql-schema.sql`, `claude.md`, `docs/guides/TESTING.md`, `tests/Feature/DocumentationFreezeTest.php` | — | 2026-07-11 |
| **IDENTITY-SSO-MFA** (TOTP MFA done; SSO/SAML/OIDC federation remains open — needs a real external IdP) | Implemented per-user opt-in TOTP as the MFA half of this finding, scoped down from the full SSO+MFA proposal since SAML/OIDC federation genuinely needs a real external IdP (Okta/Azure AD) to configure and test against, which isn't available in this environment. Built dependency-free (no `laravel/fortify`/`pragmarx/google2fa`): new `TotpService` implements RFC 6238 directly against stdlib `hash_hmac` — `generateSecret()` (160-bit, base32-encoded), `provisioningUri()` (standard `otpauth://` URI any authenticator app — Google Authenticator, Authy, 1Password — can scan/import), `verify()` (±1 period drift tolerance for clock skew). Critically verified against the **RFC's own published test vector**, not just internal self-consistency (generate-a-code-then-verify-the-same-code would pass even with a subtly wrong HMAC construction or truncation offset): independently cross-checked RFC 6238 Appendix B's secret/counter/SHA1 combination in Python first (confirmed the RFC's published 8-digit code, 94287082), then confirmed this service's 6-digit truncation produces the matching last-6-digits value (287082) for the identical inputs. New `users` columns: `mfa_secret` (encrypted at rest via Laravel's built-in `encrypted` cast — never stored in plaintext), `mfa_enabled`, `mfa_confirmed_at`; `mfa_secret` added to the model's `$hidden` array so it's never serialized. New `MfaController` with `setup`/`enable`/`disable` (self-service, requires being logged in) and `challenge`/`verify` (reached mid-login, while logged out, gated purely by a `mfa_pending_user_id` session key). Modified `AuthenticatedSessionController::store()`: after `Auth::attempt()` succeeds, if `$user->mfa_enabled`, immediately reverts that login (`Auth::guard('web')->logout()`) and redirects to the code-entry step — only a verified code completes `Auth::login()` for real. Because `mfa_enabled` defaults `false` and is strictly per-user opt-in, the existing password-only login/demo walkthrough is completely unaffected for every account that hasn't explicitly turned MFA on. +21 tests: 10 `TotpServiceTest` (RFC vector match, base32 round-trip, drift-window accept/reject, malformed-input rejection, secret uniqueness) + 11 `MfaTest` (unaffected-without-MFA regression check, setup page, enable/disable with valid/invalid credentials, full login→challenge→verify happy path, challenge-without-pending-session redirect, secret hidden from serialization). New migration `2026_07_11_010001_add_mfa_columns_to_users_table` also validated the just-fixed schema-dump workflow end-to-end: regenerated `database/schema/pgsql-schema.sql` to include these columns, re-verified the id-default parity check (still 0 missing across 468 tables), and ran the full suite from an empty `detector_test` to confirm a schema dump plus one migration layered on top works exactly as Laravel intends. **Not done**: real SSO/SAML/OIDC federation, and *mandatory* MFA enforcement on specific roles/routes (`soc:response.*`/`soc:admin.*`) — the capability exists but nothing currently requires any user to enable it; that's a policy decision for a future pass, not a technical blocker. | `app/Services/TotpService.php`, `app/Http/Controllers/Auth/MfaController.php`, `app/Http/Controllers/Auth/AuthenticatedSessionController.php`, `app/Models/User.php`, `routes/auth.php`, `resources/views/auth/mfa-setup.blade.php`, `resources/views/auth/mfa-challenge.blade.php`, `database/migrations/2026_07_11_010001_add_mfa_columns_to_users_table.php`, `tests/Unit/TotpServiceTest.php`, `tests/Feature/Auth/MfaTest.php` | — | 2026-07-11 |
| **CAP-DETECT-AS-CODE-SIGMA** (metadata/catalog compiler done — executable detection logic remains a separate, larger effort) | Built a SigmaHQ YAML → `registry.v1.json` compiler, with an honest scope correction found during research rather than an implementation that silently overstates what it does. New `SigmaImportService::parse()` (Symfony YAML — already a transitive Laravel dependency, no new composer package added) + `compile()`: domain inferred from Sigma `logsource.product/category/service` via a lookup table (`product: okta`→`identity`, `product: aws`→`cloud`, `category: process_creation`→`endpoint`, etc., falling back to `endpoint`), `severity` from `level` (Sigma's `informational` renamed to the platform's `info`), `mitre_attack` from `tags: [attack.txxxx]` (prefix-stripped, uppercased, deduplicated), `shadow_only`/`output_topic` auto-set correctly for the protected domains (`endpoint`/`threat-intel`/`network` get `shadow_only:true` + `xdr.alerts.shadow.<domain>`), `confidence` fixed at the validator's 0.65 floor (the minimum for any non-draft/non-deprecated status), `rule_id` slugified from `title` with collision-dedup against existing IDs. **Always emits `status: "shadow"`, never `staged_active`** — verified with a dedicated test (`test_compile_never_produces_staged_active`) that a source Sigma rule claiming `status: stable` still compiles to `shadow`, since `staged_active` requires a real domain-specific 6h soak PASS that an import can never satisfy on its own. New `detection:import-sigma {files*} {--dry-run} {--registry=}` artisan command; the `--registry=` override exists purely so tests never touch the real registry file. **Scope correction discovered during research** (the single most important finding of this task): `registry.v1.json` is a metadata/governance catalog everywhere in this codebase — all 133 existing rules have their actual match logic hand-coded in Go (`internal/shadowrules`) or Python (`scripts/xdr_correlation_detector.py`), never driven by the JSON, which has no field-condition/expression language at all (confirmed: zero programmatic writer existed for this file before this task — it was purely hand-edited). So this compiler produces a catalog entry (MITRE mapping, severity, suppression scaffolding, platform field-mapping hints) with the original Sigma `detection:` block preserved verbatim in a new `sigma_source` field for a human to hand-implement the real shadow-rule logic from later — consistent with how every other rule in the registry already works, not a shortfall unique to this importer, but meaning "so community/vendor rulesets can be ingested" (the original finding's phrasing) overstates the result: this bootstraps the catalog/governance side of onboarding a ruleset rapidly, not live detection coverage. Verified end-to-end against 2 realistic fixture rules (`tests/fixtures/sigma/powershell_encoded_command.yml` — Windows/`process_creation`, mirrors this platform's own existing hand-coded `powershell_encoded_command` shadow rule for direct field-mapping comparison — and `okta_mfa_bypass_attempt.yml` — `product: okta`): compiled both, merged them into a full copy of the real 133-rule registry, and confirmed `xdr_rule_registry_validate.py` still passes all 21 checks at 135 rules — then reverted the demo import (`git checkout --`) rather than permanently changing the canonical 133-rule count with 2 arbitrary demonstration entries. +15 tests: 11 unit (`SigmaImportServiceTest` — domain/severity/MITRE mapping correctness, required-field completeness, dedup, malformed-YAML rejection, scalar-vs-list `falsepositives` handling) + 4 feature (`DetectionImportSigmaCommandTest` — dry-run doesn't write, missing file fails closed, real import writes correct shadow entries, and critically one test that shells out to the actual Python validator against an isolated temp registry copy so the validator's own exit code is the source of truth, not just the compiler's self-assessment). Full suite: all tests passing (see full-suite run alongside this entry). | `app/Services/SigmaImportService.php`, `app/Console/Commands/DetectionImportSigmaCommand.php`, `tests/fixtures/sigma/powershell_encoded_command.yml`, `tests/fixtures/sigma/okta_mfa_bypass_attempt.yml`, `tests/Unit/SigmaImportServiceTest.php`, `tests/Feature/DetectionImportSigmaCommandTest.php` | — | 2026-07-11 |
| **CAP-MITRE-COVERAGE-NAV** | Added a MITRE ATT&CK Navigator layer.json export over the rule registry's existing MITRE coverage map. Research found the finding's premise partly stale: `RuleRegistryService::mitreCoverageMap()` already aggregated `technique → {rules[], domains[]}` from the registry and was already wired into `DetectionLifecycleController::attackMap` — coverage aggregation was not, in fact, missing. What genuinely was missing: (a) any Navigator-compatible `layer.json` export, and (b) the view only showed a flat list capped at 15 techniques with no download path. New `RuleRegistryService::navigatorLayer(array $coverageMap, string $domain = 'enterprise-attack'): array` builds a MITRE ATT&CK Navigator layer v4.5 structure (`techniques[]` with `techniqueID`/`score`/`comment`/`metadata`, `gradient`, `versions`) — confirmed via the registry that 95 of 191 MITRE tags across the 133 rules are sub-techniques (contain a dot, e.g. `T1078.004`) often listed alongside their parent in the same rule (`["T1078","T1078.004"]`), so scores are computed per exact `techniqueID` (parent and child independently), not rolled up — Navigator itself resolves the technique→tactic relationship from its own loaded ATT&CK data, so no tactic-mapping table was needed on this side. New `DetectionLifecycleController::downloadNavigatorLayer()` action + route (`detection.lifecycle.attack-map.navigator-layer`), reusing the existing `response($json, 200, ['Content-Disposition' => 'attachment; ...'])` idiom already established by `ExportController` — no new download pattern invented. Attack-map Blade view updated with a download link + a "count covered, showing first 15" note pointing at the official Navigator web app for the full interactive matrix (no new heatmap/grid UI was built — the existing flat list plus a real Navigator-compatible export was judged sufficient without inventing a bespoke grid renderer). +9 tests (`MitreCoverageNavigatorTest`): layer structure/required-fields, parent-vs-subtechnique independent scoring (synthetic + against the **real** 133-rule registry, confirming at least one real sub-technique is present), domain metadata per technique, empty-coverage edge case (gradient `maxValue` floored at 1 to avoid a degenerate 0-0 gradient), auth/RBAC-gated download route (`soc:rules.govern`), and the attack-map view rendering the new download link. Read-only throughout — no writes, no autonomous action. | `app/Services/RuleRegistryService.php`, `app/Http/Controllers/Detection/DetectionLifecycleController.php`, `routes/web.php`, `resources/views/detection/attack-map.blade.php`, `tests/Feature/MitreCoverageNavigatorTest.php` | — | 2026-07-11 |
| **CAP-DECEPTION-HONEYTOKEN** | Implemented seeded-honeytoken detection: `Honeytoken`/`HoneytokenHit` models over new `honeytokens` (mutable catalog) + `honeytoken_hits` (append-only, unique-indexed `(honeytoken_id, alert_id)` from creation — deliberately not retrofitted later the way `ioc_hits` was) tables. Research found `SocThreatIntelController::matchIocs()` was the closest existing analog (substring-scan alert evidence for seeded values, batched `insertOrIgnore` into a hit table, accumulate-then-write-once evidence update) and the task itself named a Laravel `HoneytokenService`, so `HoneytokenService::scanForHits()` mirrors that method's shape deliberately rather than inventing a second scanning strategy or routing through correlation-worker/Go. **Bug found and fixed during testing** (a real defect, not a hypothetical): the mirrored scan builds its match haystack via `json_encode([..., $alert->evidence, $alert->raw_event])`, and PHP's `json_encode()` escapes `/` to `\/` by default — a plain-text honeytoken needle containing `/` (exactly what `file_path` and `url` token types — 2 of the 4 supported types — routinely look like, e.g. `C:/decoy/passwords.txt`) would silently never match. Fixed by decoding the stored evidence/raw_event JSON strings back into arrays before a single re-encode pass, with `JSON_UNESCAPED_SLASHES` on that pass; caught by a dedicated multi-token test that deliberately included a file path — the original `matchIocs()` has the same latent risk but no test coverage exposes it since none of its existing IOC test fixtures contain slashes; that pre-existing gap was noted but left alone as out of scope for this task. New `honeytoken:scan` artisan command, scheduled `everyFifteenMinutes()` in `Kernel.php` (matching `soc:sla-escalate`'s cadence) — no existing scheduled job did value-matching before this. New `honeytoken.view`/`honeytoken.manage` permissions (admin + detection_engineer get manage; analyst/viewer/scenario_operator get view or nothing, matching the `advisory.*`/`dlq.*` precedent). New `SocHoneytokenController` (seed/list/deactivate) + minimal Blade view, RBAC-gated identically to existing SOC controllers. Purely detective throughout: no offensive deployment to any third-party system, no active response ever triggered from a hit — a hit only writes an append-only row for analyst review. +11 tests (`HoneytokenScanTest`): single/multi-token match persistence, inactive-token exclusion, idempotent re-scan, no-honeytokens-no-hits, invalid-token-type rejection, deactivate-preserves-history, command output, and RBAC-gated HTTP routes (index/store/deactivate). | `database/migrations/2026_07_12_010001_create_honeytoken_tables.php`, `app/Models/Honeytoken.php`, `app/Models/HoneytokenHit.php`, `app/Services/HoneytokenService.php`, `app/Console/Commands/HoneytokenScanCommand.php`, `app/Console/Kernel.php`, `app/Http/Controllers/SocHoneytokenController.php`, `routes/web.php`, `config/soc.php`, `resources/views/honeytoken/index.blade.php`, `tests/Feature/HoneytokenScanTest.php` | — | 2026-07-12 |
| **ENT-SDLC-NO-SUPPLYCHAIN** (SBOM generation — scan/sign still open) | Second half of this finding: SBOM generation (syft/cyclonedx), which the base-image-digest-pinning pass above left open since neither tool is installed in this environment. New `scripts/xdr_generate_sbom.py`: hand-builds a valid CycloneDX 1.5 JSON SBOM per service directly from data already in the repo and already trusted — each service's `requirements.txt`/`go.mod` (exact-pinned versions) and its Dockerfile's already-digest-pinned `FROM` line (base image + real sha256) — a pure, deterministic data transform, not a filesystem/binary scan, so no `syft` binary was needed. Generated `docs/security/sbom/<service>.cyclonedx.json` for all 10 services (the original 6 + the 4 `log-connector-*` services added later this session). Python services get `pkg:pypi/...` purls with exact pinned versions; Go services get a `go` framework component (from `go.mod`'s `go` directive) plus digest-pinned container base-image components; all currently have zero external Go deps so no `pkg:golang/...` purls exist yet (the parser supports `require` blocks for when that changes). 11 embedded unittest checks (`--test`, default invocation): service discovery, pinned-version extraction, extras-stripping (`uvicorn[standard]` → `uvicorn`), Go-version capture, digest-pinned base-image extraction with real 64-hex-char SHA-256 validation, CycloneDX shape validation, deterministic serial numbers, Go-only services excluding pypi purls and vice versa, full-coverage across all 10 services, and purl syntax validation — all passing. Vulnerability scan gating (trivy) and signed builds (cosign) remain explicitly out of scope — both need real, licensed/network-dependent tooling this environment cannot install or verify, unlike SBOM generation which only needed files already in the repo. | `scripts/xdr_generate_sbom.py`, `docs/security/sbom/*.cyclonedx.json` | [#29](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/29) | 2026-07-10 |
| **PERF-GO-LIMITER** | Replaced ingestion-gateway's per-tenant token bucket (IG-2) — a buffered channel refilled by a background goroutine ticking every second and touching EVERY live tenant bucket regardless of demand — with a lock-free atomic time-delta limiter: `tokens`/`lastRefill` are `atomic.Int64` fields, refilled lazily via a CAS loop computed from elapsed wall time exactly when a request needs it (`tenantBucket.allow()`). The background goroutine (`startTenantBucketRefiller`) now only does idle-bucket eviction, once a minute (down from once a second) — it no longer touches token state of any bucket at all. Behavior preserved exactly: all existing black-box tests (burst-then-throttle, per-tenant isolation, bucket-cap/overflow-sharing from `TestTenantRateLimitIsolation`/`TestTenantBucketCapBoundsDistinctBuckets`/`TestOverflowTenantsShareRateLimit` etc.) pass unmodified. +3 new tests exercising the refill mechanism directly: refill happens correctly from elapsed time with no background goroutine running (`TestTenantBucketRefillsOverElapsedTimeWithoutBackgroundGoroutine`), refill never exceeds `rps` capacity even after a long idle period (`TestTenantBucketRefillNeverExceedsCapacity`), and 200 concurrent goroutines hitting the same bucket produce a race-free, capacity-bounded result (`TestTenantBucketConcurrentRefillIsRaceFree`). `go build`/`go vet`/`go test ./...` clean. **Not run**: `go test -race` — requires cgo, and gcc is not installed in this environment, the same standing limitation noted for every other Go change this session; the concurrent-access test above exercises the CAS retry path without the race detector as a partial substitute. | `services/ingestion-gateway/main.go`, `services/ingestion-gateway/main_test.go` | [#27](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/27) | 2026-07-10 |
| **PERF-GO-OVERCONCURRENT** | `normalizeBatch()` spawned a fresh set of `workers` goroutines plus 2 new channels (jobs, results) on EVERY poll-batch cycle, then discarded them once the batch completed — real per-batch goroutine-spawn/teardown GC churn at high sustained RPS. Introduced `normalizerPool`, started once lazily via `sync.Once` (so any `Worker` construction path — `main()`, tests, one-off file processing — picks it up without an explicit init step) and reused across every subsequent `normalizeBatch` call. `Worker` gained a `pool *normalizerPool` field; the free `normalizeBatch()` function became `(w *Worker) normalizeBatch()`, delegating to the persistent pool. Only the small per-batch results channel is still allocated per call (unavoidable — each batch needs its own completion signal); the actual GC-churn source (goroutine spawn+teardown every batch) is eliminated. Both call sites (the poll loop, `normalizeHTTP`) updated; no test called the old free function directly, so no test signature changes were needed. `go build`/`go vet`/`go test ./...` clean. +4 new tests: pool-instance-reused-across-calls (`TestNormalizeBatchReusesSamePoolAcrossCalls`), correctness preserved through the persistent pool (malformed/normalized split), 20 concurrent calls hitting the shared pool without corruption, and empty-input handling. **Not run**: `go test -race` — requires cgo, unavailable in this environment (same standing limitation as `PERF-GO-LIMITER`, #27); the concurrent-calls test exercises the shared pool under concurrent access without the race detector as a partial substitute. | `services/normalizer-worker/main.go`, `services/normalizer-worker/main_test.go` | [#28](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/28) | 2026-07-10 |
| **ENT-SEC-NO-TLS-INTERNAL** (phase 1 — ingestion-gateway cert tooling + mTLS scaffolding, off by default; other 9 services/Pandaproxy/OpenSearch/Postgres remain) | Built the internal mTLS mechanism end-to-end on one representative service rather than attempting all 10 services + 3 datastores in one pass. New `scripts/xdr_generate_internal_mtls_certs.py`: shells out to the system `openssl` CLI (no new Python crypto dependency) to generate a self-signed local dev/test CA plus one server cert (SAN covering every internal docker-compose hostname) and one client cert, both CA-signed, written to `storage/certs/internal-mtls/` (gitignored — private keys never committed). **Caught and fixed a real bug during smoke testing, not left to a live deploy to discover**: the first CA generation (plain `openssl req -x509 ...`, no explicit extensions) verified fine under `openssl verify` and Go's own `crypto/tls` client, but was rejected outright by Python's `ssl` module on OpenSSL 3.x ("CA cert does not include key usage extension") — a stricter TLS client than either of the first two tools exercised. Root cause: the default `openssl req -x509` policy on this environment doesn't set a critical `keyUsage=keyCertSign,cRLSign` extension. Fixed by adding explicit `-addext basicConstraints=critical,CA:TRUE -addext keyUsage=critical,keyCertSign,cRLSign`, with a new regression test (`test_ca_cert_has_critical_key_usage_and_basic_constraints`) asserting the extension content directly, not just that some other tool's `verify` command happens to accept it. New Go package `services/ingestion-gateway/internal/mtls`: `ServerConfig()`/`ClientConfig()` build a `*tls.Config` requiring+verifying a client cert (server side) or presenting a client cert + verifying the server (client side) against the given CA — both return `(nil, nil)` when disabled, meaning "use plain HTTP," so every existing deployment is byte-for-byte unaffected by default. Wired into `ingestion-gateway/main.go`: the server's `http.Server.TLSConfig` and the shared package-level `httpClient`'s `Transport.TLSClientConfig`, gated behind `XDR_INTERNAL_MTLS_ENABLED` (default `false`) — `server.ListenAndServeTLS("", "")` is used when enabled (cert/key already loaded into `TLSConfig.Certificates`), `server.ListenAndServe()` otherwise, identical to the pre-existing behavior. **Verified two ways, not just unit tests**: (1) +11 Go tests in `internal/mtls` including a genuine end-to-end handshake over a real `tls.Listen` — proves both that a valid client cert succeeds and that an untrusted (non-CA-signed) client cert is rejected, not just that the `tls.Config` object has the right fields set; (2) a manual smoke test — built the real binary, ran it with `XDR_INTERNAL_MTLS_ENABLED=true` and real generated certs, confirmed via Python's `ssl`/`http.client` (a third, independent TLS implementation from both Go's and openssl's) that a request with no client cert is connection-reset and a request with the valid client cert returns `200 {"status":"ok",...}` from the real `/health` endpoint. `docker compose config --quiet` confirmed clean after the `.env.example` documentation addition. **Explicitly a phase 1, not "any internal hop"**: the other 9 services (normalizer-worker, correlation-worker, alert-writer-service, incident-builder-service, ai-rag-service, and the 4 `log-connector-*` services), Pandaproxy/OpenSearch's own plaintext HTTP, Postgres `sslmode`, and the docker-compose network config needed to actually run this between containers are all deferred, separate future phases — this pass proves the mechanism genuinely works on one service, it doesn't close the finding. | `scripts/xdr_generate_internal_mtls_certs.py`, `services/ingestion-gateway/internal/mtls/mtls.go`, `services/ingestion-gateway/internal/mtls/mtls_test.go`, `services/ingestion-gateway/main.go`, `.env.example`, `.gitignore` | [#31](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/31) | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE** (UEBABaselineService seam) | Extracted UEBABaselineService's pure statistical math (`robustZScore`/`percentileRank`/`computeMAD`/`computeMedian`/`computeStats`/`percentileRankFromBaseline`/`computeConfidence`, ~113 lines, zero Eloquent/DB dependency) into a new `UEBAStatistics` class (static methods), following the same "pure logic out of a stateful service" pattern already applied to `ThreatHuntingService`→`ThreatHuntQueryAllowlist` and `ReportExportService`→`ReportRenderer`. The 4 previously-public methods stayed on `UEBABaselineService` as 1-line delegating passthroughs (`return UEBAStatistics::robustZScore(...)`) so all 11 existing tests calling them as `$this->svc->method()` needed zero changes — same backward-compat precedent as `ThreatHuntingService::SUPPORTED_DOMAINS`. The 3 previously-private methods (`computeStats`/`percentileRankFromBaseline`/`computeConfidence`) had zero external references (confirmed by grep), so they moved outright with the 5 internal call sites repointed to `UEBAStatistics::`. `UEBABaselineService` 806→730 lines; new `UEBAStatistics.php` 117 lines. All 58 pre-existing `UEBABaselineAnalyticsTest` tests passed unmodified — the strongest confirmation behavior is byte-identical. **Had zero isolated unit test coverage before extraction** for `computeStats`/`percentileRankFromBaseline`/`computeConfidence` (only reachable through the full DB-backed scoring path) — +20 new direct-import tests (`UEBAStatisticsTest`, plain `PHPUnit\Framework\TestCase`, no DB, matching the `TotpServiceTest`/`ThreatHuntQueryAllowlistTest`/`ReportRendererTest` precedent) covering the zero-range division guard in `percentileRankFromBaseline` (confirms no NAN/INF), the confidence cap at 0.97, and the absolute-value symmetry of `computeConfidence`. Full `php artisan test --parallel --recreate-databases`: 4934 tests, 13934 assertions, 0 failures. | `app/Services/UEBABaselineService.php`, `app/Services/UEBAStatistics.php`, `tests/Unit/UEBAStatisticsTest.php` | [#30](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/30) | 2026-07-10 |
| **AI-KB-FEED-INGEST** | Re-scoped from "live MITRE/RSS threat-intel feed ingest" (conflicts with the platform's offline-first posture) to a bundled offline dataset import, reusing the existing ENTERPRISE-067 seeding mechanism (`AiKnowledgeSeedService`) rather than adding a new service or live network dependency. Derived one KB entry per unique MITRE ATT&CK technique already referenced across the 133-rule detection registry (`docs/detection/rules/registry.v1.json`) — 103 unique techniques — each listing which of this platform's own rules cover it (rule_id, title, domain, severity). Content is drawn entirely from already-existing, already-reviewed data in this repo (the rule registry + `AlertMitreService`'s existing 10-entry technique_id→name map, used only where it already covers a technique — the remaining 93 show the ID without a name, deliberately not hand-typing MITRE technique names from memory to avoid asserting unverified facts). Every entry's content ends with an explicit "bundled offline snapshot ... not a live MITRE ATT&CK API/TAXII feed or RSS ingestion" disclaimer. Shipped as 103 additional entries appended to the existing `database/seeders/data/rag_knowledge_fixtures.json` (10→113 total, category=`threat_intel`, source=`detection-rule-registry-v1-bundled-offline` distinguishing them from the original hand-authored 10), imported via the existing idempotent `AiKnowledgeSeedService::seed()` — zero new PHP service, table, or route. Verified none of the 4 existing tests referencing fixture counts hardcode an exact number (all use `assertGreaterThanOrEqual`), so this is additive with zero regression risk. +3 new tests (`AiKnowledgeSeedTest`): all 103 MITRE fixtures present with correct category/source, content correctly labelled bundled-not-live and traceable to a real rule ID (not fabricated), and the fixtures seed into `soc_knowledge_base` via the existing pipeline. Full suite: 4914 tests, 13898 assertions, 0 failures. | `database/seeders/data/rag_knowledge_fixtures.json`, `tests/Feature/AiKnowledgeSeedTest.php` | [#26](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/26) | 2026-07-10 |
| **CAP-MSSP-TENANCY** | Implemented MSSP parent/child tenant hierarchy + advisory rollups. Research found the finding's premise understated the prerequisite work: `tenant_id` was a bare string convention scattered across ~19 tables with **no `tenants` table at all** — no first-class tenant registry existed, so a `tenant_hierarchy` FK would have had nothing authoritative to reference. Built the registry first: new `tenants` table (id/tenant_id/name/status), then `tenant_hierarchy` (`parent_tenant_id`→`child_tenant_id`, both FK to `tenants.tenant_id`, `is_active` for reversible unlink mirroring `user_tenant_memberships`' granted/revoked pattern — never hard-deleted, so history is preserved). **Deliberately did not reuse `TenantBoundaryService`'s null-tenant-context convention** for rollup access control: research found that convention is *already* an accidental cross-tenant leak (`assertAccess()`/`scopeQuery()` both treat a null tenant_id as "unscoped, allow everything," and the only three controllers wired to it default to null whenever a caller simply omits the `X-Tenant-ID` header, since `XDR_TENANT_STRICT_MODE` defaults false) — building a new rollup feature on top of that pattern would have compounded an existing gap rather than adding a safe new capability. Instead, `TenantHierarchyService::rollupSummary()` is driven exclusively by an explicit, enumerated whitelist from `tenant_hierarchy` (`childrenOf()` → `whereIn('tenant_id', $children)` against `security_alerts`/`security_incidents`) — an unlinked or unrelated tenant is never included, verified by dedicated tests seeding an alert for a tenant that was never linked to the parent and asserting it's absent from the rollup. New `mssp_analyst` role (added to `Rbac::$validRoles` and `config/soc.php`, view-only: `dashboard.view` + `mssp.rollup.view`) and new `mssp.rollup.view`/`mssp.hierarchy.manage` permissions (admin gets both; MSSP-analyst rollup access additionally requires an active `user_tenant_memberships` row for the requested parent tenant — reusing the existing membership mechanism rather than inventing a third parallel authorization concept, so an MSSP analyst can't view an arbitrary parent's rollup just by holding the generic permission). New `MsspTenantController` (rollup view, link/unlink — manage-gated) + minimal Blade view. Advisory/read-only throughout — no autonomous cross-tenant action. +15 tests (`TenantHierarchyTest`): link/unlink lifecycle, self-parent rejection, rollup counts, unrelated-tenant exclusion, unlinked-child exclusion, empty-parent edge case, RBAC role recognition, membership-gated HTTP access (analyst without membership sees empty rollup even for a parent with real linked data; analyst with membership sees it; admin bypasses the membership check), manage-permission-gated link/unlink routes. | `database/migrations/2026_07_13_010001_create_tenant_hierarchy_tables.php`, `app/Models/Tenant.php`, `app/Models/TenantHierarchy.php`, `app/Services/TenantHierarchyService.php`, `app/Support/Rbac.php`, `config/soc.php`, `app/Http/Controllers/MsspTenantController.php`, `routes/web.php`, `resources/views/mssp/rollup.blade.php`, `tests/Feature/TenantHierarchyTest.php` | — | 2026-07-13 |
| **CODE-STRUCT-DECOMPOSE** (EntityRiskScoringService seam) | Extracted `EntityRiskScoringService`'s pure risk-factor scoring math (`WEIGHTS` — a ~78-entry weight table — `LEVEL_THRESHOLDS`, `MAX_SCORE`, `scoreToLevel`, `aggregateScore`, `makeFactor`, zero Eloquent/DB dependency) into a new `EntityRiskFactorScoring` class (static methods), following the same "pure logic out of a stateful service" pattern already applied to `UEBABaselineService`→`UEBAStatistics` and `ThreatHuntingService`→`ThreatHuntQueryAllowlist`. Extraction performed via `sed` line-range extraction (not the Edit tool) given the ~78-entry `WEIGHTS` array's size made manual transcription risky; every extracted section verified byte-for-byte against the original before wiring anything up. The 3 constants stay on `EntityRiskScoringService` as const aliases (`public const WEIGHTS = EntityRiskFactorScoring::WEIGHTS;`) — PHP allows a class constant's value to reference another class's constant, the exact technique already proven for `ThreatHuntingService::SUPPORTED_DOMAINS` — so all 11 external test files referencing `EntityRiskScoringService::WEIGHTS`/`LEVEL_THRESHOLDS` needed zero changes. `scoreToLevel`/`aggregateScore`/`makeFactor` kept as 1-line delegating passthroughs. `EntityRiskScoringService` 989→886 lines; new `EntityRiskFactorScoring.php` 149 lines. All 34 pre-existing `EntityRiskScoringTest` tests plus all 587 tests across the 10 external files referencing the constants (`AdvancedHuntingInvestigationTest`, `CrossDomainCorrelationTest`, `EndpointFleetHardeningTest`, `EndpointFleetSimulationTest`, `EndpointLowLevelTelemetryTest`, `MultiTenantIsolationTest`, `PilotReadinessTest`, `SoakChaosValidationTest`, `SoarOrchestrationTest`, `UEBABaselineAnalyticsTest`) passed unmodified. **Had zero isolated unit test coverage before extraction** (only reachable through the full DB-backed `calculateRisk()` path) — +12 new direct-import tests (`EntityRiskFactorScoringTest`, plain `PHPUnit\Framework\TestCase`, no DB, matching the `UEBAStatisticsTest`/`ThreatHuntQueryAllowlistTest` precedent) covering `scoreToLevel` boundary values, `aggregateScore`'s `MAX_SCORE` cap and missing-contribution-defaults-to-zero behavior, `makeFactor`'s unknown-factor-name weight-1.0 fallback, negative-weight factors (mitigation lowers risk), and optional-field omission/inclusion. Full `php artisan test --parallel --recreate-databases`: 4946 tests, 13963 assertions, 0 failures. | `app/Services/EntityRiskScoringService.php`, `app/Services/EntityRiskFactorScoring.php`, `tests/Unit/EntityRiskFactorScoringTest.php` | [#32](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/32) | 2026-07-11 |
| **ENT-SEC-NO-TLS-INTERNAL** (phases 2-3 — normalizer-worker + correlation-worker mTLS, off by default; remaining services/datastores still open) | Extended phase 1's proven `internal/mtls` mechanism (#31, ingestion-gateway) to the other 2 core Go pipeline services. Duplicated `mtls.go`/`mtls_test.go` verbatim into `normalizer-worker/internal/mtls` and `correlation-worker/internal/mtls` — matching this codebase's established per-service duplication convention for small pure Go packages (`internal/traceparent`, `internal/cef`); confirmed byte-identical via `diff`. Wired into each service's HTTP server (`http.Server.TLSConfig`) and outbound `httpClient` (`Transport.TLSClientConfig`), gated behind the same `XDR_INTERNAL_MTLS_ENABLED` env var already documented in `.env.example` from phase 1 — no new env vars needed since all 3 services intentionally share the same variable names (`XDR_INTERNAL_MTLS_CA`/`_SERVER_CERT`/`_SERVER_KEY`/`_CLIENT_CERT`/`_CLIENT_KEY`). `normalizer-worker`'s `main()` previously called `http.ListenAndServe` directly with no `*http.Server`/graceful-shutdown wrapper — switched to an explicit `&http.Server{TLSConfig: ...}` plus a TLS-vs-plain dispatch (`ListenAndServeTLS("","")` vs `ListenAndServe()`), matching the pattern phase 1 already established on `ingestion-gateway`; `correlation-worker` already used `&http.Server{}` with graceful shutdown, so only the `TLSConfig` field and the same dispatch needed adding. Both Dockerfiles already do `COPY internal ./internal` (whole-directory copy), so no Dockerfile change was needed. **Verified two ways per service**: (1) the existing 11 Go tests in `internal/mtls` (unchanged, just duplicated) plus `go build`/`go vet`/`go test ./...` clean for both services; (2) a manual smoke test per service — built the real binary, ran with `XDR_INTERNAL_MTLS_ENABLED=true` and the real certs generated in phase 1, confirmed via Python's `ssl` module (independent from both Go's and openssl's TLS implementations) that a request with no client cert is rejected and a request with the valid client cert returns a real `200` from `/health`. All 3 core Go pipeline services (ingestion-gateway → normalizer-worker → correlation-worker) now have the mTLS mechanism proven end-to-end. **Explicitly still phases, not "any internal hop"**: alert-writer-service, incident-builder-service, ai-rag-service (Python), the 4 `log-connector-*` services, Pandaproxy/OpenSearch's own plaintext HTTP, Postgres `sslmode`, and the docker-compose network config needed to actually run this between containers all remain deferred. `docker compose config --quiet`: clean. | `services/normalizer-worker/internal/mtls/`, `services/normalizer-worker/main.go`, `services/correlation-worker/internal/mtls/`, `services/correlation-worker/main.go` | [#33](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/33) | 2026-07-11 |
| **ENT-SEC-NO-TLS-INTERNAL** (phases 4-7 — all 4 log-connectors + 3 Python services + Postgres sslmode config; Pandaproxy/OpenSearch TLS + docker-compose network wiring remain, genuinely blocked by no live Docker daemon) | Phase 4: duplicated `internal/mtls` into all 4 `log-connector-*` Go services (syslog/cloudtrail/guardduty/gcp-audit) — each already ran its own `/health`+`/metrics` server *and* an outbound `httpClient` forwarding to `ingestion-gateway`, both wired, disabled by default. Had to add a missing `envBool()` helper to all 4 (didn't exist there before). Verified via `go build`/`go vet`/`go test` (all clean across all 4) plus a real end-to-end smoke test on `log-connector-syslog` (binary built, real certs, no-cert rejected / valid-cert 200 OK). Phases 5-6: `alert-writer-service`/`incident-builder-service`/`ai-rag-service` are started via a **static Dockerfile `CMD`** (`python -m uvicorn main:app ...`), not a programmatic `uvicorn.run()` call — no Go-style code hook point exists. New `docker-entrypoint.sh` (POSIX `sh`, disabled by default, conditionally adds uvicorn's `--ssl-certfile`/`--ssl-keyfile`/`--ssl-ca-certs`/`--ssl-cert-reqs 2` flags) duplicated into all 3. These 3 services never call each other directly (only Pandaproxy/OpenSearch, the separate infra-TLS item) — so only server-side mTLS applies, no client wiring needed. **Found and fixed a real correctness gap during this work, not left for a live deploy**: enabling mTLS would have broken the existing Docker `HEALTHCHECK` (a plain `http.request` presenting no client cert against a listener that now requires one) — new `docker-healthcheck.py` mirrors the same env-var toggle, building an SSL context with the client cert when mTLS is enabled; `build_request()` is a pure function, unit tested directly (13 tests total in `scripts/xdr_validate_uvicorn_mtls_entrypoint.py`, including one against the real generated certs). **Found and fixed a second real bug during testing**: this repo's git checkout has `core.fileMode=false`, so a committed script's executable bit is **not preserved** — `CMD ["./docker-entrypoint.sh", ...]` (direct exec) would fail with "permission denied" on a real Linux build; fixed to `CMD ["sh", "docker-entrypoint.sh", ...]` (sh interprets the file directly, no exec bit needed), locked in with a dedicated regression test (`test_dockerfile_invokes_entrypoint_via_sh_not_direct_exec`) so this can't silently regress. **Found and fixed a pre-existing, unrelated gap that was blocking this feature**: `ai-rag-service`'s `docker-compose.yml` block had **zero** `env_file`/`environment` wiring at all (unlike its 2 sibling Python services) — `.env` vars, including the new mTLS ones, would never have reached that container; added `env_file: - ./.env`, matching the other services. Phase 7 (partial): Postgres `sslmode` was hardcoded to `'prefer'` — made configurable via `DB_SSLMODE` (default unchanged, byte-for-byte identical behavior). Full Postgres mutual TLS (client cert via `sslcert`/`sslkey`/`sslrootcert`) deliberately **not** wired — verifying that DSN path needs a live Postgres server with real certs configured, which this environment cannot provision or verify, and guessing at the DSN internals without verification was judged too risky for a file this central (every DB connection in the app goes through it). **Verified**: full `php artisan test --parallel --recreate-databases` re-run after the `config/database.php` change (the highest-risk file touched this round, since every test in the suite depends on it) — 4946 tests, 13963 assertions, 0 failures; all 4 Go connectors' test suites clean; all 3 Python services' existing per-directory suites clean (82+77+1 passed, unmodified — zero regression since only new sibling files were added, `main.py` itself untouched); `docker compose config --quiet` clean. **All 10 custom services now have the app-layer mTLS mechanism, disabled by default.** **Explicitly still open — genuinely infra-blocked, not just undone**: Pandaproxy (Redpanda REST proxy) TLS, OpenSearch TLS, Postgres full mTLS, and the docker-compose network wiring to actually run mTLS between live containers all need a running Docker daemon to configure and verify (`docker info` fails in this environment: "failed to connect to the docker API") — this is a genuine tooling/environment limitation, not something more code can resolve from here. | `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/internal/mtls/`, `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/main.go`, `services/{alert-writer-service,incident-builder-service,ai-rag-service}/docker-entrypoint.sh`, `services/{alert-writer-service,incident-builder-service,ai-rag-service}/docker-healthcheck.py`, `services/{alert-writer-service,incident-builder-service,ai-rag-service}/Dockerfile`, `scripts/xdr_validate_uvicorn_mtls_entrypoint.py`, `config/database.php`, `docker-compose.yml`, `.env.example` | [#34](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/34) | 2026-07-11 |
| **SYSLOG-TCP-ADMISSION** | External review finding on `log-connector-syslog`'s TCP listener: one goroutine per accepted connection, no connection ceiling, no read/idle deadline (a slow/silent client held its goroutine+fd forever), a temporary `Accept()` error killed the entire listener goroutine outright, and no clean way to stop the listener on shutdown. New `internal/tcpadmit` package: `Limiter` (bounded connection admission — `TryAcquire`/`Release`/`Active`/`Max`, `0` disables the cap, matching the existing `maxTenantBuckets` convention) and `NextBackoff` (exponential backoff computation mirroring net/http's own `Server.Serve` accept-retry idiom) — both pure, fully unit tested (9 tests) without a real socket. Wired into `serveTCP`/`handleTCPConn`: `XDR_SYSLOG_TCP_MAX_CONNS` (default 1000, connections beyond this refused immediately, `tcp_rejected_conns` metric), `XDR_SYSLOG_TCP_IDLE_TIMEOUT_SECONDS` (default 300, `SetReadDeadline` refreshed before each line read, `tcp_timeouts` metric), a temporary `Accept()` error now retries with exponential backoff instead of killing the listener, and `ln.Close()`/`conn.Close()` wired into `main()`'s existing SIGTERM handler for clean shutdown — already-active connections are deliberately left open to drain naturally rather than force-closed (force-closing would drop in-flight buffered lines), verified by a test that writes through an active connection *after* shutdown and confirms it's still ingested. New `tcp_active_conns`/`tcp_rejected_conns`/`tcp_timeouts` counters on `/metrics`. README documents the two new env vars plus production source-IP allowlisting and TLS-termination expectations (per the finding's required outcome). +4 real integration tests dialing actual TCP connections against a real `serveTCP` listener (connection-limit saturation, idle-timeout closure, active-conn-count lifecycle, clean-shutdown-with-active-clients) — the finding is specifically about real socket/fd/goroutine behavior, which only a real listener proves. Also fixed a cosmetic bug caught during review: the startup log line re-read `envInt("XDR_SYSLOG_TCP_MAX_CONNS", ...)` instead of reflecting the actual configured `Limiter`'s capacity (would have shown the wrong number for any caller constructing a `Connector` directly, e.g. tests) — added `Limiter.Max()` and used it instead. `go build`/`go vet`/`go test ./...` clean across all packages in the service (34 tests total). | `services/log-connector-syslog/internal/tcpadmit/tcpadmit.go`, `services/log-connector-syslog/main.go`, `services/log-connector-syslog/README.md` | [#35](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/35) | 2026-07-11 |
| **CONN-UNTENANTED-INGEST** | External review finding: connector tenant environment variables default to empty and are absent from Compose — events omit `tenant_id`, send no `X-Tenant-ID`, and are accepted because `ingestion-gateway.tenantAllowed("")` returns `true`. `ingestion-gateway`: new `XDR_INGEST_REQUIRE_TENANT` (default `false`). When `true`, a batch with no payload `tenant_id` AND no `X-Tenant-ID` header is rejected outright (`400 tenant_required`) rather than silently accepted with unrestricted rate limiting — `tenantAllowed("")`'s existing "unrestricted" behavior for rate-limiting is a separate, deliberately unchanged concern; this adds an independent admission check ahead of it. All 4 `log-connector-*` services: new `validateTenantConfig(tenantID, requireTenant)` pure function (directly unit tested, no need to exercise real `log.Fatal`), wired into `main()` via a new `XDR_<SERVICE>_REQUIRE_TENANT` env var — in strict mode with no configured tenant, the connector refuses to start (`log.Fatalf`) rather than silently forwarding unattributed telemetry. `docker-compose.yml`: added explicit `XDR_<SERVICE>_TENANT_ID`/`XDR_<SERVICE>_REQUIRE_TENANT` passthroughs to all 4 connector blocks plus `XDR_INGEST_REQUIRE_TENANT` to `ingestion-gateway`'s block — these were completely absent from Compose before, meaning even an operator setting them in `.env` would never have reached the containers (no `env_file`, no explicit `environment` entry); all default to the pre-existing behavior, zero change for existing deployments. **Verified event-level tenant identity was already trustworthy once configured, not the actual gap**: all 4 connectors already stamp `tenant_id` into the JSON payload (not an untrusted header — none of them ever sent `X-Tenant-ID` at all), and the whole payload is HMAC-signed before forwarding, so "signed tenant attribution" was structurally already correct; the actual gap was purely the missing enforcement/defaults/compose-wiring this fixes. READMEs updated for all 4 connectors documenting the new var. +20 new tests: 4 on `ingestion-gateway` (default-mode allows untenanted, strict-mode rejects untenanted, strict-mode accepts payload tenant, strict-mode accepts header-only tenant) + 4 per connector × 4 connectors (default-allows-empty, strict-rejects-empty, strict-allows-configured, two-instances-stay-isolated) — covering every "Required tests" scenario from the finding except header/payload-mismatch, already covered by a pre-existing test. Tenant identity is derived exclusively from the connector's own trusted deployment configuration or ingestion-gateway's payload-tenant extraction — never from untrusted Syslog payload fields or cloud-export record contents, per the finding's constraint. `go build`/`go vet`/`go test ./...` clean across `ingestion-gateway` + all 4 connectors. `docker compose config --quiet`: clean. | `services/ingestion-gateway/main.go`, `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/main.go`, `docker-compose.yml`, `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/README.md` | [#36](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/36) | 2026-07-11 |
| **CONN-DELIVERY-LOSS** | External review finding (Critical): every connector cleared its in-memory buffer before `forward()` was confirmed to succeed. The 3 file-based connectors also persisted `processedFiles[path] = true` (and saved the state file) before any batch derived from that file was acknowledged — so a transient gateway outage (network error, timeout, HTTP 429/5xx) permanently discarded telemetry, and on restart the source file was skipped forever even though it was never actually delivered. New shared `internal/deliver.WithRetry(maxAttempts, base, max, fn)` bounded-exponential-backoff primitive, duplicated per-service (no shared Go module in this repo, matching the `internal/mtls`/`internal/tcpadmit` precedent), fully unit tested (6 tests per copy). **`log-connector-syslog`** (ephemeral UDP/TCP, no source file to re-read): `flush()` redesigned to retry the forward through `deliver.WithRetry` before clearing the buffer; if every attempt fails, the batch is explicitly dropped and counted (`delivery_dropped` metric) rather than silently discarded. Deliberately **not** built: a disk-backed pending-batch spool, which would be needed for true restart-safe (crash-safe) delivery of ephemeral telemetry — scoped and documented honestly in the README as retry-then-explicit-drop only, closing the "transient failure loses data" gap without overclaiming crash-safety. **`log-connector-cloudtrail`/`-guardduty`/`-gcp-audit`** (file-based): redesigned from a shared cross-file buffer to per-file independent batching inside `processFile` — each file's derived batches are forwarded with bounded retry synchronously, and `processedFiles[path] = true`/`saveState()` are committed only after **every** batch from that file is confirmed delivered; a file that fails any batch is left entirely unprocessed and naturally retried on the next scan cycle via the pre-existing state-file mechanism (fixing checkpoint-write *timing*, not the mechanism itself). New `delivery_failed_files` metric on all 3. Shutdown-waits-for-acknowledged-flush required no new code — `flush()`/`processFile()` are now synchronous through the full retry sequence, so the pre-existing SIGTERM handler already blocks on it; proven with a dedicated shutdown test. Env vars added per service: `XDR_<SERVICE>_FORWARD_MAX_RETRIES` (default 3), `XDR_<SERVICE>_FORWARD_RETRY_BASE_MS` (default 200), `XDR_<SERVICE>_FORWARD_RETRY_MAX_MS` (default 2000) — documented in all 4 READMEs. +~20 new tests, all real integration tests (actual TCP dials, real `httptest.Server`s, `http.Hijacker` to simulate a genuine connection reset rather than just a non-2xx status) per the finding's own emphasis on real socket/delivery behavior: gateway unavailable then recovers, non-2xx then success, process restart retries an unacknowledged file/batch, multi-batch file where a middle batch fails (first batch's success is not falsely persisted), shutdown during flush waits for retries. `go build`/`go vet`/`go test ./...` clean across all 4 connectors + `ingestion-gateway`. `docker compose config --quiet`: clean. **`CONN-UNBOUNDED-FILE`** (streaming/size-bounding the same 3 file parsers) is a related but distinct finding, deliberately left in `REVIEW_BACKLOG.md`, not addressed here. | `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/internal/deliver/`, `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/main.go`, `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/main_test.go`, `services/{log-connector-syslog,log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/README.md` | [#37](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/37) | 2026-07-11 |
| **IDENTITY-SSO-MFA** (mandatory MFA enforcement on approval routes — remaining sub-scope after TOTP) | Finding: "enforcing MFA as mandatory for specific roles/routes (`soc:response.*`/`soc:admin.*`) is a policy decision, not yet wired." New `App\Http\Middleware\EnsureMfaVerified` (alias `mfa.required`): when `soc.mfa_enforcement_enabled` (`SOC_MFA_ENFORCEMENT_ENABLED`, default `false`) is on, blocks the request with 403 unless the acting user has `mfa_enabled=true`, and audit-logs the block (`AuditLogger`, action `mfa.enforcement_blocked`) — same pattern as `EnsureSocPermission`'s RBAC denial logging. Applied to the highest-consequence write actions matching the finding's language: response-plan approve/reject (web + API), active-response approve/reject, and data-erasure approve/reject — the erasure route group was split so the routine policy-update route (`data-residency.policy.update`) stays unaffected, since only approve/reject carry the deletion-consequence risk the finding is about. Off by default — flipping `SOC_MFA_ENFORCEMENT_ENABLED` on is an explicit operator decision, since enabling it before every account with approval permission has MFA configured would lock them out of approving. Deliberately did not touch `soc:rules.govern` (detection promotion) — that group mixes read-only dashboards with a handful of POST actions and cleanly splitting it out is separate scope not explicitly named in the finding. Real SSO/SAML/OIDC federation to a corporate IdP remains deferred (needs a real external IdP unavailable in this environment) — unaffected by this task. +8 new tests (`MfaEnforcementTest`): disabled-by-default allows action, enabled blocks non-MFA user, enabled allows MFA-enabled user, blocked attempt is audit-logged, plus route-registration assertions confirming the middleware is present on all 3 target route groups and absent from the sibling policy-update route. `php artisan test --filter=MfaEnforcementTest`: 8 passed. `php artisan test --filter=MfaTest`: 11 passed (no regression). `php artisan test --filter="ResponsePlanningTest|ActiveResponse|DataResidency"`: 159 passed (no regression — enforcement off by default). `php artisan test --filter=RbacAuditCoverageTest`: 26 passed (no regression). | `app/Http/Middleware/EnsureMfaVerified.php`, `app/Http/Kernel.php`, `config/soc.php`, `routes/web.php`, `.env.example`, `tests/Feature/Auth/MfaEnforcementTest.php` | [#38](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/38) | 2026-07-11 |
| **CONN-UNBOUNDED-FILE** | External review finding (High): `log-connector-{cloudtrail,guardduty,gcp-audit}` called `os.ReadFile()`/gzip `io.ReadAll()` with no compressed or decompressed size ceiling — one oversized export file, or a small highly-compressible file that expands to gigabytes once decompressed (a compression bomb), could exhaust memory and restart-loop the connector. New `internal/boundedfile.Read(path, maxBytes)` per connector (duplicated, matching this repo's package-duplication convention): reads via `io.LimitReader(f, maxBytes+1)` so it never buffers more than `maxBytes+1` regardless of the file's actual on-disk size, `ErrTooLarge` if exceeded, `maxBytes<=0` disables the bound. Each parser package (`internal/cloudtrail`, `internal/guardduty`, `internal/gcpaudit`) gained `ParseBounded(data, Limits{MaxExpandedBytes, MaxRecordBytes})` alongside the existing `Parse` (now a thin wrapper calling `ParseBounded` with zero limits — fully backward compatible, all pre-existing tests unchanged): gzip decompression goes through `io.LimitReader` capped at `MaxExpandedBytes` (`ErrExpandedTooLarge` if exceeded — defeats compression bombs without ever fully decompressing them), and any single record/NDJSON-line over `MaxRecordBytes` is skipped and counted rather than silently dropped — for the NDJSON formats (guardduty/gcp-audit) this check happens on the raw line **before** any JSON unmarshal is attempted, avoiding wasted work on the oversized line specifically. `main.go` wiring: a file exceeding `MAX_FILE_BYTES`/`MAX_EXPANDED_BYTES` is **quarantined** — left in place untouched (never deleted/moved), recorded in a new append-only `<watch-dir>/.<service>-connector-quarantine.jsonl` (`path`/`reason`/`quarantined_at` per line, satisfying the finding's "durable rejection record suitable for operator recovery" constraint), and never re-attempted on a later scan or after a restart (the quarantine log is reloaded at startup, mirroring the existing processed-files state-file mechanism — this is the finding's required "retry policy": no infinite re-read/re-reject hot loop on the same oversized file). A single oversized record within an otherwise-acceptable file is handled more leniently — skipped and counted in a new `oversized_records_skipped` metric, but the rest of the file's records are still parsed and forwarded normally, since one pathological event shouldn't sink an entire export. Malformed (non-JSON) files/records are a deliberately separate, unchanged concern — not a resource-exhaustion attack, so they keep the pre-existing retry-every-scan (file-level) / skip-poison-line (NDJSON-level) behavior rather than being quarantined, per the finding's own distinction between "oversized" and "malformed." Caught and fixed during implementation: guardduty/gcp-audit's own file-extension filters already accept `.jsonl`, which would have caused each connector's own new quarantine log to be picked up as a candidate export file on the next scan — added the same explicit exclusion already used for the state file. New env vars per connector: `XDR_<SERVICE>_MAX_FILE_BYTES` (default 100 MiB), `XDR_<SERVICE>_MAX_EXPANDED_BYTES` (default 500 MiB), `XDR_<SERVICE>_MAX_RECORD_BYTES` (default 1 MiB) — all `0` disables the corresponding bound, documented in all 3 READMEs, wired into `docker-compose.yml`. Tests: 6 per `internal/boundedfile` copy (under/at/over limit, zero-disables-bound, missing file, never-over-allocates-regardless-of-real-file-size) + 5 per parser package's `ParseBounded` (zero-limits matches unbounded `Parse`, gzip-expansion rejected/allowed, oversized-record-skipped-others-kept, stable across a 5000-record fixture) + 7 per connector's `main_test.go` covering every "Required tests" scenario from the finding (oversized plain file quarantined with original left in place and durable JSONL record written, gzip expansion limit quarantined, oversized single record skipped with rest of file still forwarded, malformed file followed by a valid file doesn't block the valid one, stable behavior under a 3000-record fixture) plus 2 extra proving the "retry policy" constraint specifically (a quarantined file is never re-scanned in-process; quarantine state survives a restart across a new connector instance). `go build`/`go vet`/`go test ./...` clean across all 4 connectors + `ingestion-gateway`. `docker compose config --quiet`: clean. | `services/{log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/internal/boundedfile/`, `services/{log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/internal/{cloudtrail,guardduty,gcpaudit}/*.go`, `services/{log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/main.go`, `services/{log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/main_test.go`, `services/{log-connector-cloudtrail,log-connector-guardduty,log-connector-gcp-audit}/README.md`, `docker-compose.yml` | [#39](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/39) | 2026-07-11 |
| **OBS-OTEL-TRACING** (Phase 4 — OTLP/HTTP span export for the 3 core Go pipeline services) | Remaining sub-scope after phases 1-3 (W3C traceparent propagation across all 6 hops): "every hop produces standards-shaped span context, but nothing anywhere emits or collects an actual OTel span yet." New dependency-free `internal/otlpexport` Go package (duplicated per service, matching this repo's package-duplication convention — no OTel SDK pulled in, preserving "zero external Go deps"): builds the real OTLP/HTTP+JSON `ExportTraceServiceRequest` wire format (resourceSpans → scopeSpans → spans; trace_id/span_id/parent_span_id as lowercase hex per the OTLP spec's documented JSON-encoding special-case, not the default base64 protobuf-JSON mapping — verified this matters, since internal/traceparent already produces exactly that hex format with zero re-encoding needed) and POSTs it to a configurable collector endpoint. `Exporter.Endpoint == ""` (default) makes `Export()` a fully nil-safe no-op — zero behavior/latency change until `XDR_OTEL_EXPORTER_ENDPOINT` is set; export always runs in a goroutine so an unreachable/slow collector never adds latency to the actual telemetry path. **`ingestion-gateway`**: one span per event in `publish()` (not per batch — a batch can mix events from several distinct traces, so each event's span must carry its own correct trace-id), all sharing the true wall-clock window of that publish() call including retries. **`normalizer-worker`**: `normalizeResult` gained a `span` field built inside the pool goroutine (has both the raw inbound and freshly-normalized outbound traceparent in scope simultaneously — the actual correlation problem this needed to solve, since malformed events get filtered out non-positionally between raw and normalized slices); `Worker.normalizeBatch` now returns `(normalized, malformed, spans)`; wired at both call sites (consumer-poll loop, HTTP path) via a new `exportSpans` helper. **`correlation-worker`**: `Alert` gained an unexported `otelSpan` field (encoding/json skips unexported fields, so it never leaks into the published alert payload) populated inside the existing `makeAlert()` — avoided touching any of `makeAlert`'s ~17 call sites scattered across `correlate()`/`correlateIdentityCloud()`'s many rule branches; a new `exportAlertSpans` helper collects spans from a batch of alerts and exports them, wired at both call sites (consumer loop, `correlateHTTP`). `XDR_OTEL_EXPORTER_ENDPOINT` added to all 3 services' env, `.env.example`, `docker-compose.yml` (default empty = disabled). Tests: 9 per `internal/otlpexport` copy (JSON shape round-trips into the real OTLP structure, root-span omits parentSpanId, multi-span batching into one request, disabled/empty no-ops, real HTTP POST reaching a mock collector, non-2xx/unreachable-collector error handling) + 4 on ingestion-gateway's `publish()` (one span per event, span traceId/parentSpanId correctly derived from the inbound traceparent, no export when disabled, export failure doesn't block or slow down publish — verified under 500ms against an unreachable collector) + 4 on normalizer-worker (one span per successfully normalized event, span parent matches inbound traceparent, exportSpans reaches a real mock collector, no-op for empty spans) + 6 on correlation-worker (`makeAlert` span population matching its own traceparent, `exportAlertSpans` one-span-per-alert/disabled/empty-no-ops, and a full `correlateHTTP` integration test using a real 5-event MFA-failure-burst fixture proving produced alerts actually reach a mock collector, plus a negative test confirming zero alerts means zero export calls). No live OTel collector is available in this environment (`docker info` fails) — verified against local `httptest` mock servers instead, matching the mTLS-phase-1 precedent for "build the real mechanism, verify against a local double, defer live-infra verification." `go build`/`go vet`/`go test ./...` clean across all 3 Go pipeline services, zero regressions in their existing suites. `docker compose config --quiet`: clean. **Remaining scope**: OTLP export for the 2 Python services and PHP (same pattern, not yet done), and wiring to an actual live collector (Tempo/Jaeger) once this environment has a running Docker daemon. | `services/{ingestion-gateway,normalizer-worker,correlation-worker}/internal/otlpexport/`, `services/ingestion-gateway/main.go`, `services/ingestion-gateway/main_test.go`, `services/normalizer-worker/main.go`, `services/normalizer-worker/main_test.go`, `services/correlation-worker/main.go`, `services/correlation-worker/main_test.go`, `services/correlation-worker/otel_export_test.go`, `docker-compose.yml`, `.env.example` | [#40](https://github.com/AlfiMuhtadii/enterprise-SIEM/issues/40) | 2026-07-11 |

---

## 2. Implementation & Verification Details

### Task 23.0.A: Tenant Null Creation Guard (Strict Mode Boundary)
* **Goal**: Prevent admins from accidentally creating records with `tenant_id = NULL` on store routes when strict mode is active.
* **Verification Details**:
  * Added `requireExplicitScope` parameters to `TenantContextAuthority::validateAndResolve()`.
  * Added `GLOBAL_SCOPE` ('_global') sentinel to allow explicit admin-only global scope creation.
  * Verified non-admins are spoofing-blocked from using `_global` sentinel.
  * Updated [ShadowSoakController.php](file:///D:/project/Detector/app/Http/Controllers/ShadowSoak/ShadowSoakController.php) to pass `requireExplicitScope: true`.
  * Verified unit and feature integration tests are green.

### Task 23.0.B / 23.1: Tenant Null Audit Command & Table Validation
* **Goal**: Provide a read-only CLI tool (`php artisan tenant:null-audit`) to count null records across all tenant-isolated tables, validating `--table` arguments strictly against isolated tables.
* **Verification Details**:
  * Implemented [TenantNullAuditCommand.php](file:///D:/project/Detector/app/Console/Commands/TenantNullAuditCommand.php).
  * Ensured command strictly performs read-only count queries (fully compliant with append-only database rule).
  * Added options for `--table` filter and `--output` JSON reports path.
  * Command returns exit code `0` when clean and `1` when null records are found.
  * Rejects tables not defined in `TenantBoundaryService::ISOLATED_TABLES` (such as `users` or non-existent tables) and exits with code `1`.

### Task 23.0.C / 23.2: Test Coverage & Regression Testing
* **Goal**: Create and run regression test coverage for strict store validations and audit outputs, including rejection of non-isolated tables.
* **Verification Details**:
  * Developed [TenantNullCreationGuardTest.php](file:///D:/project/Detector/tests/Feature/TenantNullCreationGuardTest.php) (24/24 tests PASS).
  * Updated [TenantIndexCreationSafetyTest.php](file:///D:/project/Detector/tests/Feature/TenantIndexCreationSafetyTest.php) (24/24 tests PASS).
  * Verified rejection of `--table=users` (unisolated) and `--table=nonexistent_table_xyz` in the command.

### Task ENV.1: Align Environment Variables in `.env`
* **Goal**: Synchronize all missing keys from `.env.example` to prevent configuration drift.
* **Verification Details**:
  * Appended the 20 missing keys to the end of [.env](file:///D:/project/Detector/.env).

### Task ENV.3: Hardening `.gitignore` for Reports and Logs
* **Goal**: Exclude dynamically generated test output report files and database dump logs to avoid bloating the Git history.
* **Verification Details**:
  * Appended exclusions for reports (`/reports/*.json`, `/reports/*.md`, etc.) to [.gitignore](file:///D:/project/Detector/.gitignore).

### Task T1 / #1: Fix Threat-Hunting Domain Count Mismatch (158 → 161)
* **Goal**: Update documentation and baseline settings from 158 to 161 domains (due to shadow domain soak harness domains).
* **Verification Details**:
  * Updated [README.md](file:///D:/project/Detector/README.md) to reference 161 query domains instead of 158.
  * Updated [DemoPlatformPackagingService.php](file:///D:/project/Detector/app/Services/DemoPlatformPackagingService.php) baseline count and descriptors to 161.
  * Updated [DocumentationFreezeTest.php](file:///D:/project/Detector/tests/Feature/DocumentationFreezeTest.php) to assert 161 domains.
  * Verified that `AGENTS.md` and `claude.md` already refer to 161 domains.

### Task T2: Rename Domain Count Test Methods
* **Goal**: Rename test cases checking threat hunting domain counts that had historical values (95, 100, 105, 110, 115, 120, 125, 130) to generic or matching names.
* **Verification Details**:
  * Renamed outdated test methods to `test_threat_hunting_supported_domains_count`, `test_threat_hunting_domain_count_is_161`, or `test_threat_hunting_service_has_161_domains` in:
    * `AdvancedHuntingInvestigationTest.php`
    * `CapacityGovernanceTest.php`
    * `ComplianceGovernanceTest.php`
    * `DistributedReliabilityTest.php`
    * `EndpointFleetHardeningTest.php`
    * `SoarOrchestrationTest.php`
    * `DemoPlatformPackagingTest.php`
    * `CodeLevelXdrMaturityTest.php`

### Task T3: Extract Advisory-Only Constraint Assertions into Trait
* **Goal**: Remove code duplication by extracting shared containment and auto-remediation checks.
* **Verification Details**:
  * Created `AssertsAdvisoryOnlyConstraints` trait under `tests/Traits/AssertsAdvisoryOnlyConstraints.php`.
  * Replaced 60 duplicate test methods across 12 separate feature test classes with a single clean trait inclusion.

### BUG: SecretsValidationService and phpdotenv Compatibility
* **Goal**: Enable direct modification of environment variables during test execution.
* **Verification Details**:
  * Updated `SecretsValidationService` to read directly from `getenv()` instead of `env()` helper because phpdotenv's ImmutableRepository caches variables at bootstrap, ignoring subsequent `putenv()` overrides in test runs.
| **CAP-TI-STIX-TAXII** | Power/capability — inbound STIX 2.1 / TAXII 2.1 threat-intel import, upgrading the flat manual IOC list into a feed-fed intel layer. New `StixTaxiiImportService` parses STIX 2.1 `indicator` SDOs: `extractFromPattern()` maps the observable-comparison grammar (`ipv4-addr:value`/`ipv6-addr:value`→ip, `domain-name:value`→domain, `url:value`→url, `file:hashes.'SHA-256'`/MD5/SHA-1/SHA-512→hash; domains/urls/hashes lowercased, IPs preserved; unsupported observables skipped), `parseIndicator()` adds lifecycle (STIX `confidence`→reputation malicious≥75/suspicious≥40/unknown, `valid_until`→`expires_at`, `name`/`labels`→threat_label, `benign` label honoured), `importBundle()` idempotently upserts into the **existing** `threat_iocs` store (no new table — integrates with the platform's IOC subsystem; preserves `ioc_id`+`created_at` on re-import, records the run in `external_ioc_feeds`), and `pollTaxii()` does an offline-safe TAXII 2.1 collection GET (`Accept: application/taxii+json;version=2.1`, optional basic-auth, never throws — returns `{objects:[]}` on any failure). New `ti:import-stix` command (offline-first: bundled `storage/app/threat-intel/sample-stix-bundle.json` default, `--file` for a local bundle, `--taxii-url` to poll). Imported IOCs + the feed record surface automatically in the existing `SocThreatIntelController` UI (reads `threat_iocs`/`external_ioc_feeds`) — no new route/view needed. **Advisory/enrichment only** — feeds detection scoring, never triggers response; zero schema changes; purely additive (no existing file edited). **Verification (Docker + detector_test live):** all `php -l` clean; sample JSON valid; **13/13 `StixTaxiiImportTest` PASS against the real detector_test Postgres** (pattern extraction incl. ipv6/hash/unsupported, confidence→reputation, valid_until→expiry, bundle upsert with `assertDatabaseHas` on threat_iocs+external_ioc_feeds, re-import idempotency preserving ioc_id, TAXII poll via `Http::fake`, graceful-failure, `ti:import-stix` command exit 0); adjacent IOC suites (`PerfIocLoopTest`, `SocTuningThreatIntelPlaybookReportTest`, 10 tests) green — no regression. | `app/Services/StixTaxiiImportService.php`, `app/Console/Commands/ThreatIntelImportStixCommand.php`, `storage/app/threat-intel/sample-stix-bundle.json`, `tests/Feature/StixTaxiiImportTest.php` | — | 2026-07-07 |
| **CAP-DETECT-BACKTEST** | Implemented a bounded, honest replay-safe historical backtest — deliberately NOT a generic rule evaluator. Prior research (this session) established `registry.v1.json` has no field-condition/expression language at all and that the only working identity/cloud/saas detection logic is hand-coded Python in `scripts/xdr_correlation_detector.py` (`detect_identity()`/`detect_cloud_saas()`), which writes directly to the **active** `security_alerts`/`security_incidents` tables via `insert_alerts()`/`expand_incidents()` — not advisory-safe, and not something a backtest can safely call into. New `DetectionBacktestService` **ports the exact thresholds** from those two Python functions into PHP (12 rules total, all confirmed 1:1 against real `registry.v1.json` rule_ids): `IDENTITY_MFA_FAILURE_BURST` (failed≥5), `IDENTITY_FAILED_LOGIN_ACROSS_SERVICES` (failed≥4 AND ≥2 distinct event_source), `IDENTITY_RISKY_IP_LOGIN` (risk_score≥0.7 + login_success), `IDENTITY_IMPOSSIBLE_TRAVEL` (≥2 distinct source_ip on login_success), `IDENTITY_PRIVILEGE_ESCALATION`, `IDENTITY_UNUSUAL_LOGIN_SOURCE` (grouped by IP, not user — ≥3 distinct users per IP), `CLOUD_UNUSUAL_API_ACTIVITY` (risk_score≥0.7, count≥3), `CLOUD_SUSPICIOUS_OBJECT_ACCESS` (count≥5), `CLOUD_MASS_DOWNLOAD` (count≥10), `CLOUD_NEW_ACCESS_KEY`, `CLOUD_SECURITY_SETTING_MODIFIED`, `SAAS_UNUSUAL_ADMIN_ACTIVITY` — evaluated read-only against `telemetry_events` (a real, durable Postgres table, confirmed queryable over arbitrary trailing windows) using the same `Collection::groupBy` idiom already established by `App\Console\Commands\XdrIdentityRiskCommand`, not a new query convention. New `detection_backtest_runs` (window/rule_ids/event-count per invocation) + `detection_backtest_matches` (append-only, one row per rule_id×actor that would have fired, with a small sample-event preview) tables — entirely separate from the fixture-pack-shaped `detection_replay_results` table, which has no window/count fields and couldn't represent this. **Never writes to `security_alerts`/`security_incidents`** — verified by a dedicated test that runs all 12 rules against 20 seeded triggering events and asserts both tables stay at zero rows afterward. New `DetectionBacktestController` (run/list/view results) + minimal Blade views, gated by the existing `soc:rules.govern` permission (same as `DetectionRuleController`) — no new permission invented. +16 tests (`DetectionBacktestTest`): one per rule's trigger threshold (at and below), window-exclusion (events outside the trailing-N-days range are excluded from both the scan and the count), unsupported rule_id rejection, the security_alerts/security_incidents non-write guarantee, and RBAC-gated HTTP routes. | `database/migrations/2026_07_14_010001_create_detection_backtest_tables.php`, `app/Models/DetectionBacktestRun.php`, `app/Models/DetectionBacktestMatch.php`, `app/Services/DetectionBacktestService.php`, `app/Http/Controllers/Detection/DetectionBacktestController.php`, `routes/web.php`, `resources/views/detection/backtest.blade.php`, `resources/views/detection/backtest-show.blade.php`, `tests/Feature/DetectionBacktestTest.php` | — | 2026-07-07 |
| **ENT-TENANCY-RISK-SCORING** | Confirmed the finding: `EntityRiskScoringService::alertsForEntity()`/`incidentsForEntity()` (and 3 of the type-specific collectors — `collectUserFactors`/`collectHostFactors`/`collectNetworkFactors`) queried `security_alerts`/`security_incidents` globally by entity key (actor_key/ip/trace_id) with zero `tenant_id` filtering — an entity key existing in two tenants (shared IP, common email) would silently aggregate and leak the other tenant's alerts/incidents into the risk factor breakdown. **Deliberately bounded scope**: `calculateRisk()` calls ~15 factor collectors total; only the 5 above (all backed by `security_alerts`/`security_incidents`, both already tenant-tagged since BACKLOG-TENANCY-019) were fixed. The other ~10 (cross-domain, active-response, UEBA, endpoint-operational, low-level-telemetry, investigation-graph, SOAR) query tables confirmed to have **no `tenant_id` column at all** (`cross_domain_correlations`, `response_executions`, `endpoint_agents`, `baseline_anomaly_scores`, `investigation_*`, `soar_*`) — fixing those would mean new migrations across a dozen unrelated tables, which is the explicit subject of separate, already-filed backlog items (`ENT-TENANCY-RESPONSE-EXECUTION`, `ENT-TENANCY-UEBA`) rather than this one. Added an optional `?string $tenantId = null` parameter threaded from `calculateRisk()` through every affected collector into the two data-retrieval helpers, using the existing manual-closure scoping convention already established by `SecurityAlertController` (`$query->when($tenantId !== null, fn ($q) => $q->where('tenant_id', $tenantId))`) rather than `TenantBoundaryService::scopeQuery()` — confirmed that method is dead code (zero callers anywhere) and type-hinted for Eloquent `Builder`, not the `Query\Builder` these raw `DB::table()` calls return, so it wasn't actually reusable as-is. Also added an entity-ownership check at the top of `calculateRisk()`: if the entity's own `tenant_id` is non-null and doesn't match the requesting tenant, returns `[]` (treated as not-found, matching how `AdvisoryFindingsController`/`DlqController` already use `assertAccess()`'s single-record pattern) — a `null` `tenant_id` on either side preserves the existing permissive/legacy behavior, deliberately not tightened, since demo/legacy rows with no tenant attribution still need to remain visible. `EntityRiskApiController::entityRisk()`/`riskHistory()` now inject `TenantContextAuthority` and resolve tenant context from the request exactly like `SecurityAlertController` already does, then apply the same entity-ownership check before returning. `getRiskHistory()`'s backing table (`entity_risk_snapshots`) has no `tenant_id` column at all — not fixed directly, but since every snapshot row belongs to exactly one already-ownership-checked `entity_id`, this is not a live leak (documented, not a new migration). +9 tests (`EntityRiskScoringTenantIsolationTest`): cross-tenant alert/incident exclusion, MFA-burst-factor and host-persistence-factor cross-tenant exclusion, null-tenant-id legacy-passthrough preservation, entity-ownership-mismatch empty-result, null-tenant-entity accessible-from-any-context, and 2 HTTP-level tests through the real `X-Tenant-ID` header + `TenantContextAuthority::grantMembership()` path. All 34 pre-existing `EntityRiskScoringTest` tests still pass unmodified (zero regression). | `app/Services/EntityRiskScoringService.php`, `app/Http/Controllers/Api/EntityRiskApiController.php`, `tests/Feature/EntityRiskScoringTenantIsolationTest.php` | — | 2026-07-07 |
| **ENT-TENANCY-ENTITY-GRAPH** | Confirmed the finding: `EntityGraphService::upsertEntity()` looked up/created rows via `Entity::firstOrCreate(['entity_type' => $type, 'entity_key' => $key], [...])` — no `tenant_id` at all — so two tenants observing the same `entity_key` (a shared IP, a common email) merged into ONE row, and `upsertRelationship()` had the identical gap. Root cause was DB-level, not just app-level: `entities` table's sole uniqueness guarantee was `entities_type_key_unique` on `(entity_type, entity_key)` — a plain multi-column `unique(type, key, tenant_id)` migration would have been unsafe, since Postgres treats NULL as distinct from NULL in unique constraints and every existing legacy/demo entity has `tenant_id IS NULL` — silently breaking dedup for all of them the moment it shipped. New migration replaces it with an expression-based `CREATE UNIQUE INDEX ... (entity_type, entity_key, COALESCE(tenant_id, '_none'))`, keeping every null-tenant row in one shared bucket (preserving today's exact behavior for legacy data) while genuinely separating any two distinct non-null tenant values. `entity_relationships` had no `tenant_id` column and no DB-level unique constraint at all (uniqueness was already only app-enforced via a plain `SELECT` in `upsertRelationship()`) — added the column only, no constraint needed. Threaded an optional `?string $tenantId = null` (added as the last parameter on both methods for full backward compatibility with existing positional callers) through `upsertEntity()`/`upsertRelationship()`, and into `projectFromAlerts()`/`projectFromIncidents()` — deriving tenant_id **per row** from each alert/incident's own `tenant_id` column (not a single global scope parameter, since one projection batch can span multiple tenants). **Deliberately bounded scope**: the 5 `projectStream*()`/`projectResponseAcknowledgement()` methods (process-chain, connection, persistence, stream-to-trace, response-acknowledgement) are unchanged — their source telemetry (`endpoint_stream_events`, `response_executions`) has no `tenant_id` column yet, the explicit subject of `ENT-TENANCY-RESPONSE-EXECUTION` and streaming-telemetry tenancy work, not this task; a `'trace'`-type entity can still theoretically merge across tenants via that untouched path, documented as a known residual gap rather than silently left unmentioned. Also closed the read-side of the same leak: `EntityApiController` had zero tenant awareness across all 6 endpoints (`index`/`show`/`timeline`/`relationships`/`alerts`/`incidents`) — added a single `resolveEntity()` helper (mirroring `EntityRiskApiController`'s ownership-check pattern) used by the 5 single-entity endpoints, so an entity from another tenant now 404s instead of being traversable by ID. +8 tests (`EntityGraphTenantIsolationTest`): same-key-different-tenant now creates 2 rows, same-tenant repeat calls still dedupe (regression guard), null-tenant rows still dedupe against each other (legacy-behavior preservation), null-tenant vs non-null-tenant distinctness, per-tenant relationship separation, per-row tenant derivation in `projectFromAlerts()`, and 2 HTTP-level 404-vs-200 tests through the real `X-Tenant-ID` header path. All 38 pre-existing `EntityGraphTest` tests still pass unmodified. | `database/migrations/2026_07_15_010001_add_tenant_isolation_to_entity_graph.php`, `app/Models/Entity.php`, `app/Models/EntityRelationship.php`, `app/Services/EntityGraphService.php`, `app/Http/Controllers/Api/EntityApiController.php`, `tests/Feature/EntityGraphTenantIsolationTest.php` | — | 2026-07-07 |
| **ENT-TENANCY-RESPONSE-EXECUTION** | Confirmed the finding: `response_executions` and its 3 append-only child tables (`response_execution_events`/`response_execution_rollbacks`/`response_execution_simulations` — none ever UPDATEd or DELETEd by the migration, only given a new nullable column) had no `tenant_id` at all — a highly-privileged subsystem (session revocation, host isolation, account disabling) with zero tenant boundary. Migration adds `tenant_id` (nullable, indexed) to all 4 tables. In `ActiveResponseExecutionService`, only `createExecution()` needed a new `?string $tenantId = null` parameter — every other lifecycle method (`appendEvent`/`runSimulation`/`initiateRollback`/`projectEntityRelationships`) already receives the `ResponseExecution $exec` model and now simply reads `$exec->tenant_id` to stamp its own child record, since tenant_id is set once at creation and every sub-resource belongs to exactly one execution (avoided threading a new parameter through 10+ method signatures). Also fixed a leak this task's own combination with the prior `ENT-TENANCY-ENTITY-GRAPH` fix would otherwise have opened: `calculateBlastRadius()`/`computeBlastRadiusEntities()`/`resolveEntityId()` looked up the target `entities` row by `entity_key` alone with no tenant filter — after entities stopped merging across tenants (previous task), an unscoped `->first()` could now resolve to the *wrong* tenant's same-keyed entity, silently miscalculating blast radius/safety scores against another tenant's observation/relationship counts. Fixed with the same null-permissive pattern used elsewhere (`where('tenant_id', $tenantId)->orWhereNull('tenant_id')` — a tenant-scoped caller can still resolve a legacy null-tenant entity, matching `EntityApiController`'s ownership semantics) — verified with a dedicated test using two same-keyed entities with deliberately different `observation_count` values per tenant, asserting each tenant's blast radius calculation picks up its *own* entity's count. `getExecution()` gained the same entity-ownership-style guard used by `EntityRiskScoringService`/`EntityApiController` (non-null mismatched `tenant_id` → treated as not-found); `getRecentExecutions()`/`getPendingApprovals()`/`getRollbackCandidates()` gained optional tenant scoping. Both consumers — `ActiveResponseController` (11 web actions) and `ActiveResponseApiController` (4 JSON endpoints) — now inject `TenantContextAuthority` and resolve tenant context on every action; the web controller's `findOrFail()` was rewritten to route through the service's now-tenant-aware `getExecution()` rather than a raw unscoped query, so a single fix covers `show`/`simulationPreview`/`blastRadiusView`/`executionTimeline`/`submit`/`approve`/`reject`/`simulate`/`requestExecution`/`execute`/`initiateRollback`/`completeRollback`/`cancel` all at once. +14 tests (`ActiveResponseExecutionTenantIsolationTest`): tenant_id persistence and inheritance across execution→event→simulation→rollback, `getExecution` cross-tenant null-vs-match, null-tenant-id legacy-visibility, tenant-scoped list/pending/rollback-candidate queries, the blast-radius wrong-entity regression test, and HTTP-level 404-vs-200 through both the web and API controllers. All 64 pre-existing `ActiveResponseExecutionTest` tests still pass unmodified. | `database/migrations/2026_07_16_010001_add_tenant_id_to_response_execution_tables.php`, `app/Models/ResponseExecution.php`, `app/Models/ResponseExecutionEvent.php`, `app/Models/ResponseExecutionRollback.php`, `app/Models/ResponseExecutionSimulation.php`, `app/Services/ActiveResponseExecutionService.php`, `app/Http/Controllers/Response/ActiveResponseController.php`, `app/Http/Controllers/Api/ActiveResponseApiController.php`, `tests/Feature/ActiveResponseExecutionTenantIsolationTest.php` | — | 2026-07-09 |
| **ENT-TENANCY-UEBA** | Confirmed the finding, and found it ran deeper than the literal wording: not just missing `tenant_id` columns, but two DB-level unique constraints (`entity_behavior_baselines`' `ueba_baseline_entity_dimension_unique` on `(entity_key, entity_type, dimension)`, and `peer_group_profiles`' bare `peer_group_key` unique) that would silently MERGE two tenants' data the moment a shared entity_key or peer-group label appeared — exactly the same class of bug as `ENT-TENANCY-ENTITY-GRAPH`'s `entities` table. Fixed both with the same expression-based `COALESCE(tenant_id, '_none')` unique index technique (preserves today's exact dedup behavior for legacy null-tenant rows, genuinely separates any two non-null tenant values). `baseline_observations`/`baseline_anomaly_scores` (both append-only — never UPDATEd/DELETEd by the migration, only given a new nullable column) had no unique constraint to begin with, so only the column was added. The peer-group half of the finding was the more subtle bug: `UEBABaselineService::deriveGroupKey()` intentionally produces global, human-readable labels like `"user_role:admin"` with no tenant component — left unchanged (still global) — but `assignPeerGroup()`'s `PeerGroupProfile::firstOrCreate()` lookup now includes `tenant_id` as a **separate** criteria field alongside `peer_group_key`, backed by the new unique index, so two tenants' admin users get two distinct group rows sharing the same human-readable key string rather than one merged row; verified with a dedicated test asserting `computePeerGroupProfile()` aggregating tenant-a's group never picks up tenant-b's member baselines even though both groups share the identical `peer_group_key`. Consistently applied the `->when($tenantId !== null, fn ($q) => $q->where('tenant_id', $tenantId))` convention (established across all 3 prior `ENT-TENANCY-*` fixes) throughout every LIST/browse read method (`getAnomalyHistory`/`getTopAnomalousEntities`/`getBaselineDriftSummary`/`getAnomalyVolumeTrend`/`buildBaselineProfile`) — a `null` tenantId (admin/unscoped caller) sees across all tenants, matching `TenantContextAuthority`'s admin-bypass semantics — while WRITE/dedup-lookup operations (`computeBaseline`, `scoreAnomaly`'s own baseline lookup, `scorePeerGroupDeviation`, `assignPeerGroup`, `computePeerGroupProfile`) deliberately use strict `where('tenant_id', $tenantId)` equality instead, since a tenant-scoped write must only ever match/update its own tenant's pre-existing rows — caught and corrected 4 places in an initial pass that used strict equality where the `->when()` list-browse semantics were actually needed. **Deliberately bounded scope, documented in the service**: `collectCurrentObservations()` (used internally by `detectAnomalies()` to source "current" values from recent activity) is NOT tenant-scoped — its source tables (`identity_provider_events`, `saas_audit_events`, `endpoint_agents`, `endpoint_process_entries`, `endpoint_behavioral_findings`, `endpoint_stream_events`) have no `tenant_id` column at all; fixing that would mean new migrations across a half-dozen unrelated tables, out of bounds for this task — the resulting anomaly SCORE is still correctly tenant-tagged, only the observation-collection window itself isn't filtered. Both consumers — `UEBAController` (7 web views) and `UEBAApiController` (8 JSON endpoints) — now inject `TenantContextAuthority` and resolve/thread tenant context through every action. +9 tests (`UEBABaselineAnalyticsTenantIsolationTest`): baseline computation isolation with deliberately divergent value sets per tenant, null-tenant legacy dedup preservation, peer group assignment isolation (same key, separate rows), the cross-tenant-aggregation regression test for `computePeerGroupProfile`, tenant-scoped vs null-sees-all anomaly history, tenant-scoped top-anomalous-entities, and HTTP-level dashboard/compute-baseline tests through the real `X-Tenant-ID` header path. All 58 pre-existing `UEBABaselineAnalyticsTest` tests still pass unmodified. | `database/migrations/2026_07_17_010001_add_tenant_isolation_to_ueba.php`, `app/Models/EntityBehaviorBaseline.php`, `app/Models/BaselineObservation.php`, `app/Models/BaselineAnomalyScore.php`, `app/Models/PeerGroupProfile.php`, `app/Services/UEBABaselineService.php`, `app/Http/Controllers/UEBA/UEBAController.php`, `app/Http/Controllers/Api/UEBAApiController.php`, `tests/Feature/UEBABaselineAnalyticsTenantIsolationTest.php` | — | 2026-07-09 |
| **CONNECTOR-FRAMEWORK (phase 1)** | Implemented the syslog + CEF half of the "generic log-ingestion/connector framework" gap — deliberately scoped to the pair the finding itself called out as covering "most network/appliance vendors," not the full LEEF/cloud-connector/parser-registry breadth. New standalone service `services/log-connector-syslog`: a pure `internal/cef` package (`Parse()`) implements the ArcSight CEF grammar — 7 pipe-delimited header fields (version/vendor/product/deviceVersion/signatureId/name/severity) plus a space-separated `key=value` extension — correctly honoring header `\|`/`\\` escaping and extension `\=`/`\\`/`\n` escaping, and discarding either an RFC3164 or RFC5424 syslog envelope preceding the `CEF:` marker rather than requiring a bare CEF line. `main.go` runs concurrent UDP and TCP listeners (TCP framing is newline-delimited; RFC6587 octet-counting framing is a documented, not silent, scope limit), maps each parsed CEF message into the existing generic `telemetry.raw` JSON contract as `telemetry_type=syslog_cef` — promoting `src/dst/spt/dpt/proto/act/suser` to the same top-level aliases (`source_ip`/`destination_ip`/...) the platform's other normalizers already recognize, while preserving the full extension verbatim under `cef_extension` so no vendor-specific field is lost — and forwards batched events to the **existing** `ingestion-gateway` `/v1/ingest` endpoint, signed with the exact same HMAC-SHA256 sigv2 scheme (`sha256=HMAC(secret, ts + "." + body)`) the gateway itself verifies, so no new trust path was introduced. A line that fails CEF parsing is not dropped — it still forwards as `telemetry_type=syslog_raw` with the raw line preserved, so an unrecognized source stays visible for analyst review rather than silently vanishing. `normalizer-worker` gained a matching `CefSyslog()` handler dispatched on `telemetry_type=syslog_cef`, marked `advisory_only: true`; `syslog_raw` needed no new handler since the existing generic fallback envelope already covers it (the connector always populates `ts`/`telemetry_type`/`event_type`). Wired into `docker-compose.yml` under the existing `strangler` profile pointing at `ingestion-gateway` by container DNS name (`docker compose config --quiet` exit 0) — no new active alert domain, no rule/registry changes, all events still land in the existing normalize→correlate shadow path. +18 Go tests: 10 in `internal/cef` (bare/RFC3164-prefixed/RFC5424-prefixed CEF, no-extension, escaped-pipe-in-header, escaped-equals-in-extension, non-CEF rejection, malformed-header rejection, non-numeric-version rejection, multi-space extension values) + 8 in `main_test.go` (field-promotion mapping, empty-Name event_type fallback, raw-fallback envelope, dispatch+blank-line skip, HMAC signature cross-checked against an independently recomputed reference value rather than just self-consistency, signed-forward-to-mock-ingestion-gateway via `httptest.Server` asserting both headers and body shape, non-2xx forward error counting, batch-size-triggered flush) — plus 1 new normalizer-worker dispatch-table case and `TestCefSyslogMarksAdvisoryAndPreservesExtension`. `go build`/`go vet`/`go test ./...` clean for both `log-connector-syslog` and `normalizer-worker` (all pre-existing normalizer-worker tests, including the Pandaproxy DLQ/commit-ordering suite, still pass unmodified). **Not done this pass, documented as open scope**: a LEEF parser (materially different grammar from CEF), a config-driven parser registry for onboarding arbitrary sources without a code change, and cloud-native connectors (CloudTrail/GuardDuty/O365/GCP). **Not run**: an actual `docker build` or a live UDP-to-pipeline smoke test — Docker daemon unavailable in this environment, the same standing limitation noted throughout this session's other infra-adjacent work. | `services/log-connector-syslog/internal/cef/cef.go`, `services/log-connector-syslog/main.go`, `services/log-connector-syslog/Dockerfile`, `services/normalizer-worker/internal/normalize/normalize.go`, `docker-compose.yml` | — | 2026-07-09 |
| **OBS-OTEL-TRACING (phase 1)** | Implemented W3C Trace Context (level 1) generation, parsing, and hop-to-hop propagation across the 3 Go pipeline services (`ingestion-gateway` → `normalizer-worker` → `correlation-worker`) — dependency-free, no OTel SDK, following this codebase's established precedent for implementing RFC-shaped protocol logic directly against stdlib (`TotpService`'s RFC 6238 implementation). New `internal/traceparent` package, duplicated identically across all 3 service modules since this codebase has no shared Go workspace and each service already duplicates small pure packages independently (same precedent as `internal/cef`): `Generate()` (fresh root span: version `00`, 16-byte trace-id, 8-byte span-id, sampled flag), `Parse()` (validates version/hex-length/non-zero trace-id and span-id per the level-1 spec, rejects uppercase hex), `NewChildSpan()`, and `Propagate(inbound string) string` (parse-or-generate-root, then mint a child span — the one function every hop calls). Investigated the existing `trace_id` field before touching anything and found it is a free-form, often domain-prefixed (`soc-`/`sla-`/`esc-`/`dlq-`/`trace-`/...) analyst-facing correlation string used across roughly 90 services/tables — reformatting it to strict W3C shape would have been an invasive, high-risk rename touching dozens of unrelated call sites for no real benefit, so `traceparent` ships as a **new, additive sibling field** instead, never replacing `trace_id`. `ingestion-gateway`'s `publish()` sets it (generate-if-absent, mirroring the existing `newTraceID()` pattern) before every Pandaproxy produce call; `normalizer-worker`'s `Event()` dispatcher was restructured (13 direct per-telemetry-type `return Foo(raw)` branches → an internal `dispatch()` capturing the result, with propagation applied once generically afterward) rather than editing all 13 normalizer functions individually; `correlation-worker`'s `makeAlert()` selects the first non-empty `Traceparent` among contributing events (mirroring its existing `TraceID` selection logic) and propagates a child span onto the outbound `Alert`. Confirmed via research that Pandaproxy's JSON v2 envelope does support real Kafka record headers, but nothing in this codebase uses them anywhere — so `traceparent` travels as a plain JSON body field, consistent with every other lineage field, rather than introducing a second, inconsistent propagation mechanism via headers. +37 new Go tests (11 × 3 services in `internal/traceparent` covering generate/parse/reject-malformed/reject-all-zero-trace-or-span-id/reject-uppercase-hex/child-span-preserves-trace-id-changes-span-id/propagate-with-valid-or-missing-or-invalid-inbound, plus 2 `normalize_test.go` end-to-end cases and 2 `main_test.go` `makeAlert` end-to-end cases). `go build`/`go vet`/`go test ./...` clean across all 3 services; every pre-existing test in all 3 suites still passes unmodified (0 regressions), including the mechanically-restructured `Event()` dispatcher (`TestEventDispatchesToEachTelemetryType` covers all 11 telemetry types unchanged). **Not done this pass, documented as open scope**: Python-side propagation (`alert-writer-service`, `incident-builder-service`), Laravel-side exposure, and — the actual observability payoff — wiring to a real OTLP collector/OTel SDK so a tool like Tempo/Jaeger can stitch spans; this phase only produces standards-shaped span context, it does not yet emit or collect spans anywhere. **Not run**: a live end-to-end trace-id-continuity smoke test across the running pipeline — Docker daemon unavailable in this environment, the same standing limitation noted throughout this session's other infra-adjacent work. | `services/ingestion-gateway/internal/traceparent/traceparent.go`, `services/ingestion-gateway/main.go`, `services/normalizer-worker/internal/traceparent/traceparent.go`, `services/normalizer-worker/internal/normalize/normalize.go`, `services/correlation-worker/internal/traceparent/traceparent.go`, `services/correlation-worker/main.go` | — | 2026-07-10 |
| **CONNECTOR-FRAMEWORK (phase 2)** | Implemented the LEEF half of the connector-framework gap left open by phase 1 — new pure `internal/leef` package in the existing `services/log-connector-syslog` service (no new service needed). `Parse()` supports both LEEF 1.0 (`LEEF:Ver|Vendor|Product|ProductVersion|EventID|Extension`, extension implicitly tab-delimited) and LEEF 2.0 (adds an explicit `Delimiter` header field before the extension, accepting either a literal single character or a hex byte written as `x09`/`0x09`), honoring `\|`/`\\` header escaping and `\<delim>`/`\\` extension escaping, and stripping a preceding syslog envelope using the same technique as `internal/cef`. `main.go`'s `processLine()` now tries CEF, then LEEF, then falls back to `syslog_raw` — the two markers are mutually exclusive by construction so trying both is safe. A parsed LEEF message maps to `telemetry_type=syslog_leef`, promoting LEEF's common extension field names (`src`/`dst`/`srcPort`/`dstPort`/`proto`/`usrName`/`cat`) to the same top-level aliases the CEF path already uses, while preserving the full extension verbatim under `leef_extension`. `normalizer-worker` gained a matching `LeefSyslog()` handler, structurally identical to `CefSyslog()`, `advisory_only: true`. Connector `/metrics` gained a `parsed_leef` counter. +23 new Go tests (13 `internal/leef` covering both LEEF versions, both hex-delimiter forms, envelope stripping, both escape classes, no-extension, non-LEEF rejection, unsupported-version rejection, malformed-header rejection for both version shapes, invalid-delimiter-field rejection; 4 `main_test.go` field-promotion/fallback/3-way-dispatch tests; 2 `normalizer-worker` tests). `go build`/`go vet`/`go test ./...` clean across both services; every phase-1 CEF/raw test still passes unmodified (0 regressions). **Still open, unchanged from phase 1**: a config-driven parser registry for onboarding arbitrary sources without a code change, and cloud-native connectors (CloudTrail/GuardDuty/O365/GCP). **Not run**: `docker build`/live UDP-to-pipeline smoke test — Docker daemon unavailable in this environment, the same standing limitation noted throughout this session's other infra-adjacent work. | `services/log-connector-syslog/internal/leef/leef.go`, `services/log-connector-syslog/main.go`, `services/normalizer-worker/internal/normalize/normalize.go` | — | 2026-07-10 |
| **OBS-OTEL-TRACING (phase 2)** | Extended W3C traceparent propagation one hop further into the Python side of the pipeline: `alert-writer-service` and `incident-builder-service`. New `traceparent.py` module, a byte-for-byte algorithmic mirror of the Go `internal/traceparent` package (`generate()`/`parse()`/`new_child_span()`/`propagate()`, stdlib `re`+`secrets` only), duplicated identically in both service directories — same precedent as `xdr_event_contracts.py` already being duplicated across both. `AlertPayload`/`WriteRequest`/`BuildRequest` gained an optional `traceparent` field. In `alert-writer-service`, `process_alerts()` mints a **new child span per hop** (`tp.propagate(alert.traceparent or traceparent)`, deliberately not a passthrough like `trace_id`) and carries it into the `alerts.created` envelope; `normalize_records()` backfills it from the batch envelope like it already does for `trace_id`. In `incident-builder-service`, `aggregate()` (which already derives `trace_id` as the first non-empty value among a group's alerts) derives `traceparent` the same way and propagates one new child span per aggregated incident into `incidents.updated`. `xdr_event_contracts.py`'s `envelope()` gained an optional `traceparent` param, deliberately excluded from `stable_event_id()`'s hash material since a fresh span-id every call would otherwise break idempotent event-id generation on replay. Deliberately scoped out this pass: no Postgres column/persistence for `traceparent` (unlike `trace_id`) — the payoff is OTLP collector consumption from the live stream, not SQL querying, so this avoided pulling in a migration and the full Laravel gate. +30 new Python tests: 28 direct-import pure-function tests for `traceparent.py` (mirroring the Go suite, no fastapi/pydantic stubbing needed) + 2 `aggregate()`-level wiring tests using `SimpleNamespace` fake alerts (bypassing the heavy `BaseModel = object` test stub, same technique `test_alert_identity.py`'s `make_alert()` helper established) confirming trace-id preservation / span-id change through the real aggregation function. Per-directory suites clean: `tests/alert_writer` 82/82, `tests/incident_builder` 52/52, zero regressions — verified the combined top-level `python -m unittest discover -s tests` failure (1 failure + 77 errors) is a **pre-existing** test-isolation issue between same-named `main` modules across service test directories (confirmed identical failure count via `git stash` against the pre-change baseline), not something this task introduced; per-directory discovery is the supported invocation, matching CLAUDE.md's own convention for `tests/endpoint_agent`. **Still open**: Laravel-side exposure and the actual OTLP collector/SDK wiring — still context-production-only, nothing consumes these spans yet. | `services/alert-writer-service/traceparent.py`, `services/alert-writer-service/main.py`, `services/alert-writer-service/xdr_event_contracts.py`, `services/incident-builder-service/traceparent.py`, `services/incident-builder-service/main.py`, `services/incident-builder-service/xdr_event_contracts.py` | — | 2026-07-10 |
| **CONNECTOR-FRAMEWORK (phase 3)** | Implemented the config-driven parser registry — the remaining half of the original proposed fix. New `internal/registry` package in `services/log-connector-syslog`: `SourceDefinition{Name, Marker, TelemetryType, EventTypeField, FieldMap}` loaded from JSON (`Load()`, empty path = empty registry = zero-config default); `Match()` finds the first definition whose `Marker` substring appears in the line; `Parse()` extracts space-separated `key=value` pairs after the marker (independent reimplementation of the same key-boundary regex technique `internal/cef` uses, deliberately not a cross-package dependency) and promotes only `FieldMap`-named keys to output fields while preserving every key found — mapped or not — verbatim in `Extension`. `processLine()` dispatch is now CEF → LEEF → registry → `syslog_raw`. `mapRegistryToEvent()` writes promoted fields under their **configured canonical names**, the same names the normalizer's existing generic fallback envelope already recognizes — so onboarding a new source through this registry needs **zero `normalizer-worker` code changes**, only a JSON entry, which is the actual "by config not code" payoff. Shipped a real working example, `parsers.sample.json`, proven via a test that loads the exact shipped file and parses a full sample line through it end-to-end. Wired `XDR_SYSLOG_PARSER_REGISTRY` env var (optional; unset = identical to phase 2 behavior) and a `parsed_registry` metrics counter. +19 new Go tests (12 `internal/registry`, 5 `main_test.go`, plus updated existing dispatch tests for the new registry parameter). `go build`/`go vet`/`go test ./...` clean across all 4 packages; every phase-1/phase-2 test still passes unmodified (0 regressions). README documents the config schema. **Still open**: cloud-native connectors (CloudTrail/GuardDuty/O365/GCP) — a materially different pull-based integration shape, a separate larger effort. **Not run**: `docker build`/live smoke test — Docker daemon unavailable in this environment. | `services/log-connector-syslog/internal/registry/registry.go`, `services/log-connector-syslog/main.go`, `services/log-connector-syslog/parsers.sample.json`, `services/log-connector-syslog/README.md` | — | 2026-07-10 |
| **DATA-TIERING (phase 2)** | Made the phase-1 gzip JSONL retention archive actually searchable — a real, bounded local read path, deliberately scoped as a stopgap rather than reattempting the ClickHouse warm tier (still out of reach without live infra). New `ArchiveSearchService::search(table, tenantId, from, to, filters, limit)`: lists candidate `.jsonl.gz` files, skips files whose filename timestamp falls outside `[from, to]` without opening them (the archive's `{Y-m-d_His_u}.jsonl.gz` naming convention doubles as a coarse time index), then linearly scans remaining files applying an exact-match filter map against decoded JSONL rows. Explicitly bounded (`MAX_FILES_SCANNED=200`/`MAX_ROWS_SCANNED=200000`/`MAX_RESULTS=500`, `truncated` reported) and honestly labelled (`is_local_archive_search: true` on every response) — not a real indexed warm tier. New `security:archive-search` Artisan command (read-only, no mutation) matching `SecurityRetentionCommand`'s existing `--archive-dir` convention/default. +11 tests (`ArchiveSearchServiceTest`): basic search, filter hit/miss, tenant scoping (single vs. all), nonexistent-dir empty result, limit-triggered truncation, date-range exclusion **and** inclusion, 2 command-level CLI tests. Full `php artisan test --parallel --recreate-databases` confirmed green: **4839 tests, 12196 assertions, 0 failures** (4828 baseline + 11 new) — purely additive, no existing file modified. **Still not done**: the real warm tier (ClickHouse) and cold tier (object storage), unchanged from phase 1. | `app/Services/ArchiveSearchService.php`, `app/Console/Commands/SecurityArchiveSearchCommand.php`, `tests/Feature/ArchiveSearchServiceTest.php` | — | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE (incident-builder-service)** | Decomposed the one Python pipeline service that had never had a seam extracted (`normalizer-worker` and `alert-writer-service` were already done in earlier passes). New `incident_aggregation.py`: `alert_entities()`/`group_key()`/`incident_id_for()`/`aggregate()` — pure alert-to-incident grouping/identity logic with zero FastAPI/Pandaproxy/DB dependency — extracted the same way `alert_identity.py` was extracted from `alert-writer-service/main.py`; `Any`-typed alert parameters avoid a circular import back to `main.py`'s Pydantic model, same technique `alert_identity.py` uses. `now_iso()` moved into the new module and imported back into `main.py` (eliminating a would-be duplication rather than introducing one) since its only real dependency was `aggregate()`'s two fallback timestamps. `main.py` 708→645 lines; dropped 2 now-unused imports (`hashlib`, `datetime`/`timezone`). This logic had **zero isolated unit test coverage before extraction** — +25 new direct-import tests (`test_incident_aggregation.py`, `SimpleNamespace` fake alerts, no stubbing needed) covering entity resolution, group-key derivation, incident-id determinism, and aggregate's severity/confidence/timeline/mitre/domain assembly — including two test-fixture bugs in the *tests themselves* caught and corrected against the real code's actual behavior during the first run (actor_key/ip are unconditionally included alongside evidence entities; `confidence`'s `default=0.5` is unreachable since `aggregate()` never receives an empty group). Also fixed a **latent Docker build bug** while checking Dockerfile parity for this task, the same bug class already caught once for the Go services' `internal/` packages earlier this session: neither `alert-writer-service/Dockerfile` nor `incident-builder-service/Dockerfile` had been updated to `COPY traceparent.py` when OBS-OTEL-TRACING phase 2 added it — the image build has been broken since that commit, never caught because the Docker daemon is unavailable in this environment. Fixed both Dockerfiles. Per-directory suites clean: `tests/alert_writer` 82/82, `tests/incident_builder` 77/77, zero regressions. **Not run**: an actual `docker build` to verify the Dockerfile fix — Docker daemon unavailable. | `services/incident-builder-service/incident_aggregation.py`, `services/incident-builder-service/main.py`, `services/incident-builder-service/Dockerfile`, `services/alert-writer-service/Dockerfile`, `tests/incident_builder/test_incident_aggregation.py` | — | 2026-07-10 |
| **META-MODULE-RATIONALIZE (bounded step)** | Investigated the literal proposed fix — "collapse StabilityEvidenceFreeze V2/V3/V4 into a single versioned service" — and deliberately did not attempt it: the three are structurally distinct (12/22/16 gates over non-overlapping phase ranges E045-E048/E049-E054/E055-..., separate tables/command/controller each), so a real merge risks silently changing gate coverage or losing per-version evidence, without the ability to run a live-pipeline verifier to catch a mistake. Implemented the safe additive slice instead: `StabilityFreezeOverviewService::overview()`, a read-only facade calling each version's existing unchanged `getLatestFreeze()` (confirmed to already share a consistent summary shape), reporting `versions.{v2,v3,v4}` plus `current` (the single most-recently-frozen run across all three). Directly addresses the credibility concern the finding named ("reviewers may read 'Final XDR Certification' as a real accreditation") via an explicit `note` field stating each version covers a different phase range and `current` is not a merged/superseding status. New `stability:freeze-overview` Artisan command (read-only). Zero behavior change to any existing freeze/command/controller. +9 tests (`StabilityFreezeOverviewTest`): all-null-when-never-run, always-advisory, single/multi-version reporting, most-recent-wins (via `$this->travel()` for deterministic ordering), an explicit write-guard test (row count unchanged after calling `overview()` twice), 2 command CLI tests. Full `php artisan test --parallel --recreate-databases`: **4848 tests, 12217 assertions, 0 failures** (4839 baseline + 9 new); the underlying V2/V3/V4 test suites (92 tests) unmodified and still passing since none of their code was touched. **Broader scope not done**: the ~32-service meta-module audit and the overlapping soak-service merge — a separate, larger effort. | `app/Services/StabilityFreezeOverviewService.php`, `app/Console/Commands/StabilityFreezeOverviewCommand.php`, `tests/Feature/StabilityFreezeOverviewTest.php` | — | 2026-07-10 |
| **OBS-OTEL-TRACING (phase 3)** | Closed the last remaining hop — Laravel HTTP middleware, explicitly named in the original finding's Component list. New `TraceparentService` (`app/Services/`), a PHP algorithmic mirror of the same `generate()`/`parse()`/`newChildSpan()`/`propagate()` contract as the Go `internal/traceparent` packages and Python `traceparent.py` modules, pure and dependency-free. Wired into the existing global `SecurityRequestLogger` middleware: reads an inbound `traceparent` request header, propagates a child span, sets a `Traceparent` response header (alongside the existing `X-Request-Id` pattern), and adds it to the structured `SecurityLogger::log()` payload — so an analyst's own browser request into the SOC console links to the same trace as the pipeline events under investigation. Deliberately placed before the middleware's internal-path early-return (mirroring `X-Request-Id`'s placement) since trace propagation is independent of the security-detector's authenticated-path exclusion — verified the header is still present on an ignored path (`/soc`). No DB schema change, consistent with the Python phase's decision to keep `traceparent` production-only. +17 tests: 13 pure unit tests (no Laravel bootstrap needed, mirrors the Go/Python suites exactly) + 4 HTTP-level feature tests (generated-when-absent, child-span-of-inbound, invalid-inbound-still-valid, present-on-ignored-path). Full `php artisan test --parallel --recreate-databases`: **4865 tests, 12246 assertions, 0 failures** (4848 baseline + 17 new). **Still genuinely open**: wiring to a real OTLP collector/OTel SDK — every hop across all 3 languages now produces standards-shaped span context, but nothing anywhere emits or collects an actual OTel span yet. | `app/Services/TraceparentService.php`, `app/Http/Middleware/SecurityRequestLogger.php`, `tests/Unit/TraceparentServiceTest.php`, `tests/Feature/SecurityRequestLoggerTraceparentTest.php` | — | 2026-07-10 |
| **CONNECTOR-FRAMEWORK (phase 4)** | First cloud-native connector — AWS CloudTrail, deliberately scoped as file-based ingestion of already-exported logs, not live S3 API polling (which needs SigV4 signing and real AWS credentials this environment can't exercise). New standalone service `services/log-connector-cloudtrail`: pure `internal/cloudtrail` package decodes CloudTrail's stable `{"Records":[...]}` format, auto-detecting gzip via magic bytes; a record that fails to re-marshal is skipped, not fatal to the batch. `main.go` recursively walks a watch directory on a poll interval, maps records onto the same canonical field names the normalizer's generic fallback envelope already recognizes — zero `normalizer-worker` changes needed, same pattern as the phase-3 parser registry. Restart-safe file tracking via an atomic-write state file. **Caught and fixed a real bug during test-writing**: the state file lives inside the scanned directory and is named `*.json`, so without an explicit skip it would be re-scanned as a candidate CloudTrail export forever, spamming parse errors — caught by a test running `scanOnce()` twice, fixed with an explicit skip + dedicated regression test. Wired into `docker-compose.yml` under `strangler` with a bind-mounted directory for an operator's `aws s3 sync` cron target. +15 Go tests. `go build`/`go vet`/`go test ./...` clean. **Still open**: GuardDuty/O365/GCP connectors, live S3 API polling. | `services/log-connector-cloudtrail/internal/cloudtrail/cloudtrail.go`, `services/log-connector-cloudtrail/main.go`, `docker-compose.yml` | — | 2026-07-10 |
| **META-MODULE-RATIONALIZE (broader audit)** | Documentation-only: `docs/architecture/META_MODULE_AUDIT.md`. Fresh survey found **33 meta-modules, not ~32** as the finding stated — all 17 named ones still present, plus 16 never named (including `StabilityFreezeOverviewService`, the bounded-step work from earlier this pass) — **the sprawl grew, it did not shrink**. Classified all 33: only 2 are a genuine merge-candidate pair (`PilotReadinessService` + `EnterprisePilotReadinessMatrixService`), 1 is a lowest-value relabeling candidate (`DemoPlatformPackagingService`), 2 carry naming risk without a runtime problem (`CommercialReadinessService`, `FinalXdrCertificationService` — already advisory-gated at runtime, but the class names alone risk the "reviewers may read this as real accreditation" concern the finding quoted). The rest legitimately answer different questions despite sharing near-identical scaffolding. Confirmed the 7 soak services are distinct-purpose, not redundant, and recommended (not built) a `SoakOverviewService` mirroring the already-shipped `StabilityFreezeOverviewService` read-only pattern rather than a literal merge — generalizing the existing precedent instead of re-litigating it per-service. Surfaced 4 Controller/Service naming-drift pairs. Quantified migration footprint (24 literal-term hits, ~36 including hardening/pilot/demo tables). Explicitly recommends but does not execute the merge/build/rename — each is a separate follow-up. Documentation-only, no test run per CLAUDE.md policy. | `docs/architecture/META_MODULE_AUDIT.md` | — | 2026-07-10 |
| **DATA-TIERING (phase 2b)** | Closed the CLI-only gap from phase 2 — new `ArchiveSearchController` (`GET /archive-search`, RBAC-gated via the existing `soc:search.view` permission, reused from `SiemSearchController` rather than inventing a new one) and a matching Blade view. **Caught and fixed a real tenant-isolation design mistake before it shipped**: the controller was first written with a free-text `tenant_id` query parameter — the exact class of bug this session's `ENT-TENANCY-*` fixes spent multiple passes closing elsewhere. Caught during design review, not by a failing test, and rewritten to derive tenant scope exclusively from `TenantContextAuthority::validateAndResolve()` (the `X-Tenant-ID` header validated against real memberships), with the free-text field removed entirely. Deliberately did not copy `SiemSearchController`'s `?? 'default'` null-fallback — that's specific to OpenSearch's tenant scoping and doesn't apply to this archive's directory-per-tenant layout, where `tenantId=null` correctly means "search all tenants." Controller hard-codes the same default archive path the CLI uses (`storage/app/archives`). Results render as raw JSON blocks per row since archived rows have no fixed schema across tables. +7 tests (`ArchiveSearchControllerTest`): auth-required, RBAC check, empty-query OK, results render for a real archived row, `filters=` applied, no-match message, summary counters line. Full `php artisan test --parallel --recreate-databases`: **4872 tests, 12258 assertions, 0 failures** (4865 baseline + 7 new); `ArchiveSearchServiceTest`/`SiemSearchTest` re-confirmed unaffected. **Still open**: the actual warm/cold tier. | `app/Http/Controllers/ArchiveSearchController.php`, `resources/views/archive-search/index.blade.php`, `routes/web.php`, `tests/Feature/ArchiveSearchControllerTest.php` | — | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE (ThreatHuntingService)** | Single biggest decomposition win found this session: `ThreatHuntingService.php` was 2524 lines, ~1150 of which (46%) was a single security-critical `DOMAIN_FIELDS` allowlist constant plus `SUPPORTED_DOMAINS` and `validateQueryFilters()` — pure data + pure validation, zero Eloquent/DB dependency — extracted verbatim into new `ThreatHuntQueryAllowlist`. `ThreatHuntingService` 2524→1145 lines (55% reduction); `validateQueryFilters()` kept as a 1-line delegating passthrough so every caller's signature is unchanged. **Found and preserved a real backward-compatibility hazard before it could break anything**: `DOMAIN_FIELDS` was `private` (zero external refs, safe to move outright), but `SUPPORTED_DOMAINS` was `public` and referenced by **25 other files** across nearly every meta-module test suite — added `public const SUPPORTED_DOMAINS = ThreatHuntQueryAllowlist::SUPPORTED_DOMAINS;` (PHP class-constant-references-another-class's-constant, verified at runtime not just parse time) so zero of the 25 call sites needed to change. Extraction performed via `sed` + a small Python rewrite script given the ~1370-line scale, every step verified with `php -l` and the real suite. **Had zero isolated unit test coverage before extraction** — +11 new direct-import tests (`ThreatHuntQueryAllowlistTest`, plain PHPUnit, no DB) including two structural sanity checks over the entire allowlist (every `DOMAIN_FIELDS` key maps to a real `SUPPORTED_DOMAINS` entry; every operator list is non-empty) that weren't practically writable before this was its own reviewable unit — both passed cleanly. Full `php artisan test --parallel --recreate-databases`: **4883 tests, 13231 assertions, 0 failures** (4872 baseline + 11 new), including all 25 external `SUPPORTED_DOMAINS` call sites and the full 44-test `ThreatHuntingQueryEngineTest` suite. | `app/Services/ThreatHuntQueryAllowlist.php`, `app/Services/ThreatHuntingService.php`, `tests/Unit/ThreatHuntQueryAllowlistTest.php` | — | 2026-07-10 |
| **META-MODULE-RATIONALIZE (correction)** | Attempted the audit's own follow-up #1 (merge `PilotReadinessService` into `EnterprisePilotReadinessMatrixService`) and retracted it after investigation, before writing any merge code — the same discipline already applied to `StabilityEvidenceFreeze` V2/V3/V4, this time catching a mistake in the audit's own earlier classification. `PilotReadinessService` is operational execution/tracking; `EnterprisePilotReadinessMatrixService` is a generic gate/evidence scorecard evaluated from evidence manually attached via `linkEvidence()` — not hard-coded to `PilotReadinessService` at all. Grep confirmed zero references between the two services. Updated `docs/architecture/META_MODULE_AUDIT.md` in place: reclassified both from "Merge-candidate" to "Keep," added §3a documenting the correction and the generalized lesson (name-similarity alone was exactly the mistake to avoid), updated the merge-candidate count from 2/33 to 0/33. No merge attempted, no facade needed either — unlike V2/V3/V4 these don't cover overlapping evidence ranges. Documentation-only, no test run per CLAUDE.md policy. | `docs/architecture/META_MODULE_AUDIT.md` | — | 2026-07-10 |
| **META-MODULE-RATIONALIZE (SoakOverviewService)** | Built follow-up #2 from the broader audit: `SoakOverviewService`, mirroring `StabilityFreezeOverviewService`'s read-only pattern but deliberately not the same shape, since the 7 soak services are confirmed structurally distinct (no uniform "latest run" interface exists across them). Calls each service's own existing read method as-is: `DomainSoakHarnessService::getSummary()`, `DomainSoakSimulationService::getSimulations()->first()`, `EndpointSoakPlanService::getLatestPlan()`, `Phase1SoakExecutionService::getLatestRun()`, `Phase1SoakEvidenceFreezeService::getLatestFreeze()`, `RealDomainSoakPlanService::getLatestPlan()`, `SoakChaosValidationService::dashboardStats()` — each tagged with its own `kind` string rather than a false uniform status, `note` explicitly states this is a per-service survey, not a merged status. New `soak:overview` Artisan command. +14 tests (`SoakOverviewTest`): all 7 keys present, always-advisory, zero-vs-null distinction for aggregate vs. latest-run services, a real-run seeding test for each of the 5 `getLatest*`-style services (calling each service's own producing method, not a mock), a write-guard test, 2 command CLI tests — all 14 passed on the first run. Full `php artisan test --parallel --recreate-databases`: **4897 tests, 13252 assertions, 0 failures** (4883 baseline + 14 new). | `app/Services/SoakOverviewService.php`, `app/Console/Commands/SoakOverviewCommand.php`, `tests/Feature/SoakOverviewTest.php` | — | 2026-07-10 |
| **CONNECTOR-FRAMEWORK (phase 5)** | Second cloud-native connector — AWS GuardDuty, same file-based-not-live-API scope decision as CloudTrail. New standalone service `services/log-connector-guardduty`. **Not structurally identical to the CloudTrail connector despite both being "AWS, file-based, gzip-aware"**: GuardDuty's export feature writes NDJSON (one finding object per line), a materially different shape from CloudTrail's single `{"Records":[...]}` array per file, so this needed its own `internal/guardduty` parser; poison-line isolation, not fatal to the file. GuardDuty findings also have a genuinely harder field-mapping problem — no single `sourceIPAddress`-equivalent field exists, since `Service.Action.*` shape varies by finding `Type` — so `Finding.RemoteIPAddress()` does best-effort extraction across the known common action shapes (`NetworkConnectionAction`, `AwsApiCallAction`, `DnsRequestAction`, `KubernetesApiCallAction`, `PortProbeAction`) and returns empty rather than guessing, documented explicitly as best-effort in both docblock and README. Maps onto the same canonical field names the normalizer's generic fallback envelope already recognizes. **Proactively fixed the state-file-self-scan bug class this session already found once in CloudTrail** — written correctly from the first draft this time, still locked in with the identical dedicated regression test. Wired into `docker-compose.yml` under `strangler`. +21 Go tests (11 `internal/guardduty`, 10 `main_test.go`). `go build`/`go vet`/`go test ./...` clean. **Still open**: O365/GCP connectors, live AWS API polling for either AWS connector. | `services/log-connector-guardduty/internal/guardduty/guardduty.go`, `services/log-connector-guardduty/main.go`, `docker-compose.yml` | — | 2026-07-10 |
| **CONNECTOR-FRAMEWORK (phase 6)** | Third cloud-native connector — GCP Cloud Audit Logs, same file-based-not-live-API scope decision as CloudTrail/GuardDuty. New standalone service `services/log-connector-gcp-audit`. A third distinct payload shape: GCP log sinks write NDJSON (like GuardDuty) but the audit payload is nested inside `protoPayload` typed as `google.cloud.audit.AuditLog` — genuinely different from both AWS formats, so `internal/gcpaudit` is its own parser. `result` derivation follows GCP's own convention (empty `protoPayload.status` = success, populated = failure) rather than a direct field. `user`/`source_ip`/`cloud_account` extracted via a shared nil-safe nested-map-dig helper, mirroring the technique the GuardDuty connector established. State-file-self-scan fix applied proactively a third time (correctly from the first draft), locked in with the same regression test. Wired into `docker-compose.yml` under `strangler`. +23 Go tests (13 `internal/gcpaudit`, 10 `main_test.go`). `go build`/`go vet`/`go test ./...` clean. **CONNECTOR-FRAMEWORK's originally-named cloud-native set is now 3 of 4 done** — only O365 remains, and it's a genuinely different integration shape (pull-based Management Activity API + subscription/continuation-token polling, not file-export-to-object-storage), so it can't reuse this phase's directory-watcher skeleton and needs real OAuth credentials this environment doesn't have. | `services/log-connector-gcp-audit/internal/gcpaudit/gcpaudit.go`, `services/log-connector-gcp-audit/main.go`, `docker-compose.yml` | — | 2026-07-10 |
| **CODE-STRUCT-DECOMPOSE (ReportExportService)** | Second-biggest decomposition win this session: `ReportExportService.php` was 994 lines, ~550 of which (55%) was pure JSON/Markdown/HTML rendering/templating logic — zero DB/Eloquent dependency — extracted into new `ReportRenderer`, all methods converted from instance to static since nothing held instance state. `ReportExportService` 994→438 lines (56% reduction); `ReportRenderer` 574 lines. **Caught and fixed a boundary mistake mid-extraction, not after**: the first line-range pull accidentally included `finalize()` (an export-lifecycle concern, not rendering) — caught by reading the extracted file back before wiring anything up; removed it from `ReportRenderer` and rewrote `ReportExportService::finalize()` to call `ReportRenderer::render()`. All 43 pre-existing `ReportExportTest` tests (all 4 report types × all 3 formats) **passed unmodified** — the strongest possible confirmation of byte-identical behavior. Had zero isolated unit test coverage before extraction — +14 new direct-import tests (`ReportRendererTest`, plain PHPUnit, no DB) covering JSON validity, Markdown/HTML structure, unsupported-format exception, a dataProvider-driven check across all 4 report types, and conditional row presence — running in 0.81s vs. the DB-backed suite's 19.86s, a direct measurement of the isolation this extraction bought. Full `php artisan test --parallel --recreate-databases`: **4911 tests, 13273 assertions, 0 failures** (4897 baseline + 14 new). | `app/Services/ReportRenderer.php`, `app/Services/ReportExportService.php`, `tests/Unit/ReportRendererTest.php` | — | 2026-07-10 |
